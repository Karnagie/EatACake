# Upstream queue — candidates for RobloxTemplate

> Append-only capture (rule U1): one row per candidate, added as part of the
> Post-Task Checklist. Judging happens at harvest
> (`docs/recipes/harvest-to-template.md`), not at capture — when unsure,
> capture anyway. Statuses: `pending` → `harvested <date>` /
> `rejected: <reason>` (rejected rows stay — they prevent re-proposing).
>
> Types: `fix` (bug also present in template) | `feature` (game-agnostic
> feature/util) | `contract` (API/GUI/logging contract improvement) |
> `recipe` (pattern used 2+ times) | `research` (evidence from
> research-scout) | `docs` (rule/gotcha worth a doc)

| Date | Type | What | Evidence (files, why it's generic) | Status |
|---|---|---|---|---|
| 2026-07-12 | research | Automate U1 capture via a Claude Code Stop hook (session-end script nudges/append queue check) | Practice from compounding-engineering community (Stop hook → knowledge capture). Risk: bash hooks fragile on Windows — needs a proven low-noise setup before adoption | pending (watch) |
| 2026-07-12 | research | copier/cruft-style template update tooling instead of git cherry-pick for pull-down | cruft.github.io, copier docs — PR-based template propagation. Python-ecosystem tools; git-remote approach adopted instead for now. Revisit if game count grows past ~5 | pending (watch) |
| 2026-07-16 | contract | Shared per-player rate limiter for resync-answering remotes (Claim*/Request* reply with full payloads on invalid input — amplification) | Review finding #11 of feature-library batch; codes/group have cooldowns, daily/time/shop don't | pending |
| 2026-07-16 | contract | jsdotlua useEffect deps must never contain nil — add to roblox-ui-kit skill pitfalls | Feature-library review CRITICAL #3: `{ nil }` has length 0, positional deps compare never re-runs the effect; cost a dead countdown ticker | pending |
| 2026-07-16 | fix | **AppRoot.Set cannot clear fields**: `Set({ openPanel = nil })` is a silent no-op (`pairs` skips nils) — every panel close/toggle in the template is broken. Fix: `AppRoot.Open(panel?)` assigns directly + new `AppRoot.Clear(key)`; onClose/toggle call them; CodesSubsClient `Set({codesStatus=nil})` too | EatACake: found in Studio verification (panels would not close); fixed in `modules/AppRoot.lua`, `subscriptions/CodesSubsClient.lua` | pending |
| 2026-07-16 | docs | Kit pitfall: `(1,0)+UIAspectRatioConstraint` list cells collapse to ZERO height in a plain Frame list (FitWithinMaxSize, max height 0) — pattern works only inside AutomaticCanvasSize ScrollPane. Fix: cell takes explicit `size`; panel passes `RowHeight / ContentHeightInCell` | EatACake: UpgradesPanel/QuestsPanel rendered empty; fixed in UpgradeRow/QuestRow/panels. Add to skill `references/patterns.md` pitfalls | pending |
| 2026-07-16 | docs | rbxasset placeholder sounds: `swoosh.wav` is NOT approved (silent error); validate ids via play-mode `ContentProvider:PreloadAsync` + `TimeLength > 0` (`action_falling.mp3`, `splat.mp3`, `victory.wav`, `action_jump.mp3` are good) | EatACake JuiceConfig placeholders | pending |
| 2026-07-16 | docs | studio-verifier: `get_console_output` truncates — read the tail via `LogService:GetLogHistory()` in execute_luau; AND execute_luau has a SEPARATE require cache (module state invisible — verify via attributes/instances/remotes, not requires) | EatACake Studio verification session | pending |
| 2026-07-16 | recipe | Player-attribute replication channel for cheap all-clients per-player state (StomachFill morph lerp, EquippedPets followers, AutoEat/AutoGym perk flags) — zero remotes, smooth local interpolation | EatACake `BodySubs`/`PetSubs`/`ShopSubs` + client controllers | pending |
| 2026-07-16 | contract | ADR-0002 extension: `rawAmount` escape on currency reward kinds so PAID packs bypass earn multipliers (gems packs vs GemsMult) | EatACake RewardGrantSubs "gems" handler | pending |
| 2026-07-17 | recipe | "Keycap grid" destructible-surface look from plain Parts: column = cell minus gap (dark groove), deterministic `math.noise` shade jitter, per-layer Reflectance, columns CanCollide/CanQuery so collision+raycasts match visuals 1:1 | EatACake CakeRenderer parts path (user-approved reference look) | pending |
| 2026-07-17 | docs | Native `SurfaceType.Studs` on all faces = free LEGO/candy wall look (no textures); code-built prop kit (gumball/lollipop/mint/cookie/bar/cupcake) from primitives ≤6 parts each | EatACake MapService | pending |
| 2026-07-17 | docs | EditableMesh production notes (probe-verified; (b) CORRECTED same day): (a) height-banded looks need a PALETTE TEXTURE (EditableImage 1×256 + per-vertex UVs) — vertex colors smear across tall quads; (b) runtime-edited meshes need RenderFidelity=**Precise** (no LODs, live edits render at any distance) — **Automatic** swaps in LODs generated from CREATION-time content (stale geometry by distance/quality); create at final max extent, never flatten post-create (culling bounds come from creation geometry); (c) `CreateMeshPartAsync` parts render vertices at RAW mesh coords relative to part.CFrame (NO bbox recentering) — mesh y=0 lands at part.Position.Y; (d) recompute-normals rebuilds need two passes; (e) grid hulls: an analytic "+1 ring" of a rounded rect diverges from the discrete footprint at corner staircases — derive skirt/hull faces from 3×3-neighborhood tests, never a second analytic shape; (f) camera-inside-mesh + backface culling masquerades as "mesh not rendering", and HIGH cameras hide a floating-mesh gap — verify grounding from a LOW outside Scriptable camera | EatACake CakeRenderer (the "white/black/invisible/floating cake" saga; grounding fixes flow doc) | pending |
| 2026-07-18 | research | EditableMesh budget: a DYNAMIC EditableMesh reserves WORST-CASE budget (60k verts/20k tris) regardless of content — ~8 fit a desktop client, fewer on laptops; budget enforced in Studio play-test; `CreateEditableMesh`/`CreateEditableImage` return **nil** (no throw, pcall alone is NOT a check), `CreateMeshPartAsync` throws. Pattern: build ONE dynamic scratch → `CreateEditableMeshAsync(Content.fromObject(em), {FixedSize=true})` clones (cost = actual complexity; source vertex/normal ids stay valid on clones — probe-verified) → `scratch:Destroy()`; never Destroy an editable still linked to a MeshPart (wipes render); FixedSize enforcement is runtime-only (AddVertex on a fixed mesh does NOT error in Edit mode). Published games need ID-verify + Mesh/Image API toggle — keep a non-editable fallback renderer. Sources in flow doc | EatACake CakeRenderer `ensurePool` (fixed the "renders as rectangles on the laptop" failure); flow `2026-07-18_layer-meshes-crust-feel.md` | pending |
| 2026-07-18 | recipe | Budget-resilient deformable-surface renderer ladder: N per-layer FixedSize slab meshes (own Material/Transparency/texture) → ONE slab + height-palette texture → visible part grid; lazy pool sized per composition, retry-on-next-reset, every downgrade warns (R8). Per-layer XZ-planar EditableImage carries skin+decals (cracks) with per-cell repaint only on state transitions (cached) | EatACake CakeRenderer (assignBands/ensurePool/paintBandImage); game-agnostic for any heightfield/destructible surface | pending |
| 2026-07-18 | contract | Client subs hooking the character: capture Humanoid AND HumanoidRootPart via wait-or-warn, and after every yield bail if `player.Character ~= character` (fast-respawn stale-hook overwrite); one subscription OWNS each Humanoid event domain (two Landed handlers double-fired FX) | EatACake CakeFeelSubsClient review findings; generic character-lifecycle gotcha for the template's subs pattern | pending |
| 2026-07-18 | docs | EditableMesh gotchas, round 2 (probe-verified): (a) NEITHER per-vertex color alpha (AddColor/SetColorAlpha/SetFaceColors) NOR EditableImage texture alpha renders on a MeshPart — both display fully opaque; never design around alpha-hiding parts of an editable mesh; (b) fixed-topology stacked slabs: hide eaten-through sheets by TUCKING them ~2 studs under the local surface (below a translucent layer's BOTTOM when it owns the surface) — dropping to y=0 hangs curtain quads through side cuts, clamping at band.bottom floats films over craters; tuck depth must exceed every downward visual offset (squish+cracks+wobble); (c) discrete-footprint staircases pleat skirts at rounded corners — project ring/boundary verts onto the analytic outline (expanded half a cell so straight edges stay on the collision boundary) | EatACake CakeRenderer iteration 2 (`2026-07-18_layer-meshes-crust-feel.md`); generic for any editable-mesh destructible surface | pending |
| 2026-07-18 | recipe | ALWAYS-VISIBLE deformable "cracking crust/wax shell" over a heightfield surface, as GEOMETRY not texture: ONE MeshPart of organic VORONOI pieces (seed points → Sutherland-Hodgman clip by bisectors → clip to the loaf outline for a clean edge). Each piece owns its boundary verts (on the exact Voronoi edge → neighbours MEET, no gap = SMOOTH at rest) + a raised centre (dome). Ride the surface by re-reading mirror heights per frame; HIDE pieces where eaten; under the foot a per-piece dent 0..1 DENTS+TILTS+SPREADS-toward-centroid so cracks open ONLY underfoot (the layer below shows through). Key insight from ~9 rejected iterations: the requirement was a FIELD that cracks in place, NOT an effect spawned/anchored to the player (texture-reveal, follow-cluster, and stamped-patch attempts all failed) | EatACake `CakeWaxShell` (`2026-07-18_wax-shell.md`); game-agnostic destructible-surface skin | pending |
| 2026-07-18 | docs | Async EditableMesh build (CreateEditableMeshAsync/CreateMeshPartAsync YIELD) called lazily from RenderStepped MUST have a re-entrancy guard: the next frame's Step fires during the yield and re-enters, appending geometry to a second scratch → corrupt state (symptom: "works if you stand, breaks if you run immediately; only the pieces you spawn on work"). Set a `building` flag before the first async call; block re-entry until built/failed | EatACake CakeWaxShell (user-reported spawn race) | pending |
| 2026-07-18 | docs | `EditableImage:WritePixelsBuffer` throws "expects buffer length to equal size.X*size.Y*4 bytes" if the passed buffer is not EXACTLY that size — a reused/over-allocated scratch buffer fires it every call. Size the buffer to the exact write region; reallocate only when the region dimensions change | EatACake CakeRenderer (earlier texture-reveal attempt) | pending |
| 2026-07-18 | docs | **Character JOINT posing on modern avatars (probe-verified, corrects a same-day assumption):** many avatars use `AnimationConstraint` joints, NOT `Motor6D` (no `RootJoint` Motor6D; shoulders etc. are AnimationConstraints). The Animator OVERWRITES any joint `.Transform` every frame at EVERY `RenderStepped`/`BindToRenderStep` priority (incl. `Character+1` and 2000) — so you CANNOT layer a pose via Transform. The channel that HOLDS is the joint's STATIC offset: `Motor6D.C0`, or the constraint's `Attachment0.CFrame` (Animator never touches it) — but only the ROOT (un-animated) offset gives a clean whole-body rotation; offsetting an ANIMATED joint's attachment did not move the limb. Also: runtime Humanoid `BodyWidthScale`/etc. only rescale the character when set on the **SERVER** (client writes are reverted by replication — probe: client=4→no change, server=3→armspan 2.1→5.9; `ApplyDescription` from client can fail; direct part `.Size` is fought by the auto-scaler). Set BodyScale server-side for morphs | EatACake eat-gesture + body-morph/roll (the "no animation" + "body won't scale" causes); generic for procedural posing/scaling | pending |
| 2026-07-18 | recipe | Procedural "consume/use an item" gesture with NO uploaded animation asset and NO joint posing (rig-agnostic): a pooled piece flies source→hand→mouth along a two-arc Bézier (waypoints re-read live from Head/Hand part positions so it tracks a moving body), shrinking at the end; speed derived from an action-rate stat. Pool built once at Init (ChunkDebris pattern). The flying piece alone reads as "eating/using" — avoids the fragile per-rig joint posing above | EatACake `EatGestureController`; game-agnostic hand-to-mouth/use gesture | pending |
| 2026-07-18 | recipe | Fill-driven "grow only the TORSO" MORPH + forward TUMBLE, on the REAL avatar (no proxy meshes, no added parts — user rejected a welded ball AND whole-body scaling): grow ONLY the torso by lerping each torso part's `OriginalSize` (BodyScale left at 1 → auto-scaler grows only the torso and it HOLDS; arms/legs/head keep natural size), SERVER-SIDE (client writes revert), snap-to-target on final approach; capture true natural OriginalSize ONCE per part (re-reading the scaled value compounds — a weak-key cache silently dropped it → torso blew to 112 studs; use strong table + prune). Separately TUMBLE the whole body past a threshold by rotating the ROOT joint's static offset (Motor6D.C0 / AnimationConstraint Attachment0) by distance, settling upright when stopped; client-side, VISUAL ONLY. Caveat: huge torso can engulf the 3rd-person camera at close zoom | EatACake `BodySubs` torso morph + `BallRollController` tumble (supersedes the welded-ball + whole-body-scale attempts); generic fat-belly-that-rolls | pending |
| 2026-07-18 | fix | ProcessReceipt not idempotent for CONSUMABLE dev products — only oneTime items dedup (IsOneTimeOwned), no PurchaseId ledger → Roblox receipt re-delivery double-grants gems/pets | EatACake ShopSubs.processReceipt; template feature-library shop. Fix: persisted bounded PurchaseId FIFO section, check before grant, gate on ProfileStore LastSavedData | pending |
| 2026-07-18 | fix | Net.Remote/Net.Update use WaitForChild with NO timeout — a missing/renamed remote yields forever inside a subscription Start(); the pcall'd bootstrap loop yields with it, later subs never wire, only engine "Infinite yield" (not Log-routed) | EatACake Net.lua; core template infra. Fix: bounded WaitForChild + Log.Warn/GraceOnce, or a boot-time resolver returning a nil-safe stub | pending |
| 2026-07-18 | fix | R8: unloaded-profile silent early-returns in remote handlers (BuyUpgrade/ClaimQuest/DoRebirth/EquipPet + ShopSubs offline-payer) — no Log while sibling CakeSubs logs Log.Once('eat-preload') | EatACake fixed locally in 5 subs; template feature-library has the same silent returns. Every IsLoaded/GetPlayerByUserId early-return on a money/action path must Log.Once | pending |
| 2026-07-18 | docs | ProfileSchema sanitize must guard `type(x) ~= "table"` before `pairs(x)` — deepReconcile only fills nils, never repairs a corrupt non-table field; UpgradesSection lacked the guard its siblings have | EatACake UpgradesSection vs QuestsSection/PetsSection; add to add-profile-section recipe gotchas | pending |
| 2026-07-18 | fix | One-time consumption ordering: promo `redeemed[code]=true`+Save BEFORE the grant is confirmed (HasHandler ≠ "will grant"); a declining reward (amount<=0 / bad boostId) eats the redemption while status='ok' | EatACake CodesSubs.TryRedeem; same class as shop grantableList. Grant BEFORE consume, or validate args in the pre-guard | pending |
| 2026-07-18 | fix | Dead `reward.kind=="gold"` amount>0 guards in ShopSubs.grantableList + GroupRewardSubs (no "gold" kind exists — currency is gems) so the intended amount<=0 pre-validation never runs for real currency | EatACake ShopSubs/GroupRewardSubs; template loot-grammar residue from a gold→gems rename. Rename guards to "gems" or drop | pending |
