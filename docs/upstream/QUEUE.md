# Upstream queue — candidates for RobloxTemplate

> Append-only capture (rule U1): one row per candidate, added as part of the
> Post-Task Checklist. Judging happens at harvest
> (`docs/recipes/harvest-to-template.md`), not at capture — when unsure,
> capture anyway. Statuses: `pending` → `harvested <date>` /
> `rejected: <reason>` (rejected rows stay — they prevent re-proposing).
>
> Types: `fix` (bug also present in template) | `feature` (game-agnostic
> feature/util) | `contract` (API/GUI/logging contract improvement) |
> `recipe` (pattern used 2+ times) | `research` (evidence from
> research-scout) | `docs` (rule/gotcha worth a doc)

| Date | Type | What | Evidence (files, why it's generic) | Status |
|---|---|---|---|---|
| 2026-07-26 | recipe | **GUI automation failing is not evidence the task is manual — look for the API underneath.** Two passes (~25 turns) were burned trying to DRAG a Toolbox model into a place through screenshots; the Studio Toolbox dock steals input in ways clicking cannot undo. The working path was never the GUI: right-click → **Copy Asset ID**, then `InsertService:LoadAsset(id)` from the command bar, strip scripts, reparent. Worked first try. ⚠ `LoadAsset` only resolves assets with distribution rights ("User is not authorized to access Asset" otherwise), and free models carry scripts — strip `BaseScript`/`RemoteEvent`/`RemoteFunction` BEFORE they enter a template library. | EatACake `Workspace.Items` (donut 71594297607550); generic to any Studio asset-sourcing task | pending |
| 2026-07-26 | contract | **A collection set the player cannot SEE is not a collection set.** EatACake tracked 9 discoverable find KINDS and celebrated each first discovery, but exposed no progress anywhere — most of the completionist pull was invisible. Surfacing it needed no UI at all: a count of the set on the existing leaderstats board, replacing a joke stat (biggest belly) that nobody could act on. Template rule: every countable-set mechanic needs a visible denominator ("6/9"), and leaderstats are the zero-cost surface for one; also prefer a stat a player can ACT on over a stat that is merely funny. | EatACake `ProgressService.CountFindKinds` + `LeaderboardSubs` | pending |
| 2026-07-26 | recipe | **A sim that omits a subsystem returns a confident WRONG answer, not a small error.** EatACake's pacing scenario modelled `CakeOps.ApplyBite` + the layer gate but not the two forfeiting sweeps (they live in a different module), and on that basis reported a cake-height change as costing +0.0% food. With the sweeps ported in the real cost was −4.7% food and +2.1pp waste, and a separate published playtime figure was off by 5x (354 min vs 63 min). Rule: before trusting a balance sim, enumerate every subsystem that MUTATES the same state and confirm each is either modelled or provably irrelevant — and treat "the measurement says this tuned change is free" as the specific moment to ask what the model cannot see. Also: absolute-distance cleanup rules (stud thresholds) silently change meaning when the thing they measure against is resized. | EatACake `tools/headless-sim/pacing_scenario.lua`, `CakeConfig.maxTotalHeight` | pending |
| 2026-07-26 | docs | **After changing a documented constant, grep for the OLD value.** Moving EatACake's `maxTotalHeight` 330→170 left four trailing references: `room.wallHeight` still SIZED for 330 (a fresh clone would build a 2x-too-tall room), plus three prose restatements of "every cake is exactly 330 studs" in a sibling config comment, a feature doc and an ADR. Only the first is functional; all four make the next reader wrong. Cheap rule for any template with tuned constants: the change is not done until `grep <oldvalue>` is clean, and cross-file dependencies get a ⚠ naming the constant they must track. (Bonus finding: I had refused an adjacent fix as 'forbidden authored geometry' without checking — it was plain config. Verify the constraint before honouring it.) | EatACake `MapConfigData.wallHeight` / `CakeConfig.grid` / `features/cake-cycle.md` / ADR-0011 | pending |
| 2026-07-26 | feature | **Onboarding-funnel instrumentation belongs in the template.** EatACake had zero analytics, so 'retention' was unmeasurable even AFTER publishing — the wall was not the missing telemetry service, it was that nobody had wired one. `AnalyticsSubs`: ONE module owning `AnalyticsService` (R4), a 6-step onboarding funnel pushed IN from the domain subs via the registry, **per-place-leg minutes** on `PlayerRemoving` (see the `session_minutes` row below — do NOT port this as a single session metric), and loop counters as denominators. Every caller treats it as OPTIONAL and every call is pcall-wrapped + self-disabling after one failure (unpublished places throw on every call). Generic to every game the template produces, and it is the difference between shipping blind and shipping measurable. | EatACake `AnalyticsSubs` + `features/analytics.md` | pending |
| 2026-07-26 | fix | **Never prepare user-authored assets in place.** EatACake's find library ran its prepare step (uniform rescale, anchor, strip collision, DESTROY child Scripts) directly on the models the user authored, while the boot log told them to save the place to keep the library — so one save permanently rewrote their source art. Prepare an UNPARENTED CLONE instead (clones-of-unparented work and cost no replication). Generic to every 'authored template library' feature; pairs with the ADR-0007 place-authored-assets pattern. ⚠ It shipped with a comment calling the in-place mutation a deliberate tradeoff, which is why it survived a review pass — a self-authored 'this is intentional' comment is not evidence that it is. | EatACake `TreasureService.buildLibrary` | pending |
| 2026-07-26 | recipe | **Measure before declaring a change risky.** EatACake's cake read as a candy-striped TOWER (330 studs on a 90×78 footprint) and the height was treated as untouchable because ADR-0011 had tuned pacing around it. One scenario in the headless harness — mow a whole cake with the REAL bite math at 330/170/110 — showed bites +0.5% and food +0.0%: height was a PURE VISUAL knob the whole time, because clear time is area-driven and the per-band density formula absorbs thickness exactly. The general move: when a tuned system blocks a cosmetic change, write the ONE experiment that decides it instead of deferring; also record the boundary you found (below ~130 the density/thickness CLAMPS engage and invariance breaks). | EatACake `tools/headless-sim/pacing_scenario.lua`, `CakeConfig.maxTotalHeight` | pending |
| 2026-07-26 | docs | **A DECAL id on a `Texture`/`TextureContent` renders BLANK — you need the underlying IMAGE id** (`InsertService:LoadAsset(decalId)` → the loaded Decal's `.Texture`). Already known here, but it REGRESSED: a branch shipped with the unresolved decal ids back in `render.wrapper.textures`, so the cake's outer wall drew a red/white diagonal stripe instead of a cake photo and the whole silhouette read as a candy cane for an unknown length of time. Worth a template rule that asset-id constants carry a comment naming which KIND of id they are, so a revert or merge cannot silently swap them. | EatACake `CakeConfig.render.wrapper.textures` | pending |
| 2026-07-26 | docs | **A hidden-object test must sample the object's SILHOUETTE, not its bounding box.** EatACake's buried finds were freed by a MAX over the square bounding footprint sized off the model's LONGEST side — for a 13x5 tilted prop that is metres of cake it never touches, so a find the player could see was fully dug out stayed uncollectable behind one un-eaten corner cell. Three-part rule: sample a CIRCLE, size its radius from the AREA-EQUIVALENT extent (geometric mean of the horizontal extents), and complete on a FRACTION of cleared cells rather than all of them. Symmetrically, the 'first visible' test wants MIN cover, not MAX — MAX waits for the whole footprint to drop and announces late. | EatACake `TreasureService.coverStats`; generic to any dig/reveal/occlusion check over a heightfield or grid | pending |
| 2026-07-26 | contract | **A core loop with no progress signal.** EatACake hid its cake-% bar during eating (it barely moves) — and eating is ~all of the playtime, so the main loop ran with NO on-screen progress at all. Fix was not a new component but repurposing the SAME bar for a countable per-run goal (`FINDS n/N`), which is what carries long sessions in the genre (Drain the Lake's discoverable chests: 'give each run a goal'). Template rule: if a HUD element is hidden during the dominant activity, that slot is a hole, not a saving — fill it with the goal that activity serves. | EatACake `AppRoot.cakeBarModel` + `TreasureService.FindCounts` | pending |
| 2026-07-26 | feature | **Headless verification harness** (`tools/headless-sim/`): a Roblox-API stub + a Python bundler that inline every real `src/` module into ONE luau chunk and runs it on the standalone Luau CLI. Lets a mechanic be MEASURED, not just reviewed, when Studio is unavailable — it caught 4 bugs code review missed on its first use (absolute-vs-relative `Model:ScaleTo`, `GetBoundingBox` being pivot-framed not world-axis-aligned, a constant edge margin that let wide props poke out, elongated props being 1 stud tall). Two constraints must be ported with it: shim `require` as a LOCAL declared before the inlined bodies (never shadow the CLI builtin), and keep `Parent` in `_parent` (Lua `__newindex` only fires while a key is ABSENT, so raw-setting `Parent` once silently kills child-list maintenance). Stubs must reproduce REAL engine semantics, not convenient ones. | EatACake `tools/headless-sim/`, `2026-07-26_buried-item-finds.md`; generic to every Rojo/Roblox project | pending |
| 2026-07-26 | docs | **A core loop's completion beat must be audible.** EatACake's layer gate stepped down 28-42 times per cake — the game's actual rhythm — and did it in complete silence; the only celebrations were the whole-cake clear and the boss. One line (broadcast an announce on the gate step) + a reused sample turned it into a beat every ~1.4 min. Template rule: whenever a progression counter advances a STEP (not just when it completes), ask whether the player can hear/see it; silent steps read as no progress. | EatACake `CakeSimulationSubs` `layer-cleared` | pending |
| 2026-07-26 | contract | **`luau-compile` is the pre-engine syntax gate; `rojo build` is not.** `rojo build` packages files without parsing Luau, so a stray `end` ships and only explodes at `require` (cascading into every dependent module). The official Luau CLI (`luau-lang/luau` releases → `luau-compile.exe`) parses+compiles real Luau (`+=`, `::`, if-expressions, string interpolation) and ran the whole 203-file `src/` tree in seconds. Template should document it as the standard pre-Studio check (and it is the ONLY check available when the Studio MCP is down). | EatACake buried-finds task; generic to every Rojo project | pending |
| 2026-07-26 | feature | **Authored MODEL LIBRARY pattern**: a `Workspace.<Name>` folder the user drags art into, migrated on boot to `ReplicatedStorage.Assets.<Name>`, each child prepared ONCE (uniform scale to a target size with max/min extent clamps, anchored + inert, original transparency stamped as an attribute, child Scripts stripped), then cloned per instance. Consumers pin a specific model by child NAME or get one round-robin, so adding art is a drag-and-drop with zero code change; missing library degrades to a primitive + warn (R8). Generalises ADR-0007 from 'the scene' to 'any set of interchangeable props'. | EatACake `TreasureService` (ADR-0012); useful for any template feature with swappable art (pickups, pets, shop props) | pending |
| 2026-07-26 | docs | **A 'force-complete' fallback must test the state you ACTUALLY need, not a proxy for it.** Draft buried-find code freed a find when `activeFloor <= find.bottom` ("its band has been consumed") — but while that band is ACTIVE the gate floor already equals its bottom, so the fallback fired the moment the find became reachable and handed out the reward for zero work. The fix was not a better predicate but removing the need for one: clamp placement so stranding is impossible by construction. Rule: prefer making the bad state unrepresentable over adding a rescue branch whose trigger condition is easy to get inverted. | EatACake `TreasureService.Tick` / `SpawnForCake` | pending |
| 2026-07-26 | recipe | **Balance from a ported SIMULATION, not from a spreadsheet model.** For any game whose pace comes from a spatial/stateful loop, port the REAL mechanic (here: `CakeOps.ApplyBite` + the three `ScanStats` sweeps + the layer gate) into a throwaway JS sim, add a plausible agent policy (mowing lanes, belly, gym trips, greedy purchases) and CALIBRATE against it. It caught four things a formula never would: shipped Easy was 18 min (target 40) and swung 2.6× on the composition roll; the difficulty multiplier moved clear time 2%; a cleanup sweep was forfeiting 25% of every cake; a 56k-cost upgrade was worth 10%. Also: re-run the sim after ANY mechanic change (an adaptive bite-reach tweak moved clear time 5%). | EatACake `2026-07-26_cake-pacing-rebalance.md`; generic to any template game with a tunable core loop | pending |
| 2026-07-26 | docs | **A cleanup/QoL sweep can silently eat the design space.** EatACake's remnant sweep collapsed any cell bitten >1 stud below its band TOP so the eaten area reads clean — correct intent, but on chunky bands one nick deleted the rest of the cell for free, which (a) forfeited ~25% of the cake's calories, (b) made clear time independent of the player's bite stats, and (c) left no lever to pace anything. Rule: any "snap the remainder away" helper must be measured from the TARGET state (the floor), never from the ORIGINAL state (the top), or it scales with the wrong quantity. Worth a template gotcha wherever an anti-tedium sweep exists. | EatACake `CakeFieldService.ScanStats` `eatenEpsilonStuds` → `nearFloorStuds` (ADR-0011) | pending |
| 2026-07-26 | contract | **A feature gated behind unauthored content is a feature that does not exist.** The hex upgrade tree lived in the lobby partition and opened only from an authored `UpgradeStation` prompt that was never placed — so the game's only calorie sink was reachable from nowhere, and every balance number that assumed in-run purchases was fiction. Template rule: every player-facing system needs ONE opener that depends on nothing authored (a HUD button), with world/prompt openers as an addition; and a partition-scoped feature should sit in `common` unless it genuinely cannot. | EatACake: `UpgradeService`/`UpgradeSubs` lobby→common, `LobbyUiData`→common `UpgradesUiData`, HUD Upgrades button | pending |
| 2026-07-26 | docs | Studio sync gotcha (extends the 2026-07-20 row): rojo can be syncing the file TREE (new/deleted modules appear correctly) while a whole branch's SOURCE stays stale — here every `src/shared/**` ModuleScript kept its pre-edit source while `src/server/**` and `src/client/**` were current. Always verify a distinctive NEW value in the actual `.Source` before trusting a Studio playtest, and fall back to the localhost HTTP push (`python -m http.server` + `HttpService:GetAsync` → set `.Source`). | EatACake pacing rebalance verification | pending |
| 2026-07-20 | recipe | **Code-built scene → editable place-authored assets, cloned (R5)**: move a procedurally-`Instance.new`-built static scene into editable models under `ReplicatedStorage.Assets` that `MapService` CLONES at boot. Pattern: (a) keep the old geometry ONLY as private generators `buildX(parent)`; (b) `GenerateAssets()` populates `ReplicatedStorage.Assets` for any MISSING template (self-heal → a fresh repo clone still boots a working scene, but runtime-generated models don't persist — author + save the place); (c) `Build()` clones the templates, resolves parts, degrades + warns (R8) on anything missing; (d) DYNAMIC parts (a height-tracking platform) resolve by NAME and code positions them — author the LOOK, keep the names (single BaseParts); (e) per-skin recolor is OPT-IN via a `BiomeRole`-style attribute the generator sets, so a re-authored part keeps its own colour while the skin feature survives. Place-authored (not Rojo-synced) chosen when the user prefers editing directly in Studio over an rbxmx round-trip; code stays Rojo-synced + resolves by name. ADR-0007 | EatACake `MapService` rewrite (`2026-07-20_editable-scene-assets.md`); generic for any template's "let me edit the models" ask | pending |
| 2026-07-20 | docs | Studio MCP gotcha: `execute_luau` caches `require` ACROSS calls in an Edit session — after you edit a ModuleScript's source (rojo synced), a later `execute_luau require(thatModule)` returns the STALE cached module (your edit doesn't run). Force a fresh require by CLONING the ModuleScript (`m:Clone()`, parent it, `require(clone)`, destroy) — a different instance = a fresh require path. (Add to studio-verifier notes alongside the known "separate require cache = module state invisible".) | EatACake Task 1 asset authoring (re-authoring picked up the old generator until cloned) | pending |
| 2026-07-12 | research | Automate U1 capture via a Claude Code Stop hook (session-end script nudges/append queue check) | Practice from compounding-engineering community (Stop hook → knowledge capture). Risk: bash hooks fragile on Windows — needs a proven low-noise setup before adoption | pending (watch) |
| 2026-07-12 | research | copier/cruft-style template update tooling instead of git cherry-pick for pull-down | cruft.github.io, copier docs — PR-based template propagation. Python-ecosystem tools; git-remote approach adopted instead for now. Revisit if game count grows past ~5 | pending (watch) |
| 2026-07-16 | contract | Shared per-player rate limiter for resync-answering remotes (Claim*/Request* reply with full payloads on invalid input — amplification) | Review finding #11 of feature-library batch; codes/group have cooldowns, daily/time/shop don't | pending |
| 2026-07-16 | contract | jsdotlua useEffect deps must never contain nil — add to roblox-ui-kit skill pitfalls | Feature-library review CRITICAL #3: `{ nil }` has length 0, positional deps compare never re-runs the effect; cost a dead countdown ticker | pending |
| 2026-07-16 | fix | **AppRoot.Set cannot clear fields**: `Set({ openPanel = nil })` is a silent no-op (`pairs` skips nils) — every panel close/toggle in the template is broken. Fix: `AppRoot.Open(panel?)` assigns directly + new `AppRoot.Clear(key)`; onClose/toggle call them; CodesSubsClient `Set({codesStatus=nil})` too | EatACake: found in Studio verification (panels would not close); fixed in `modules/AppRoot.lua`, `subscriptions/CodesSubsClient.lua` | pending |
| 2026-07-16 | docs | Kit pitfall: `(1,0)+UIAspectRatioConstraint` list cells collapse to ZERO height in a plain Frame list (FitWithinMaxSize, max height 0) — pattern works only inside AutomaticCanvasSize ScrollPane. Fix: cell takes explicit `size`; panel passes `RowHeight / ContentHeightInCell` | EatACake: UpgradesPanel/QuestsPanel rendered empty; fixed in UpgradeRow/QuestRow/panels. Add to skill `references/patterns.md` pitfalls | pending |
| 2026-07-16 | docs | rbxasset placeholder sounds: `swoosh.wav` is NOT approved (silent error); validate ids via play-mode `ContentProvider:PreloadAsync` + `TimeLength > 0` (`action_falling.mp3`, `splat.mp3`, `victory.wav`, `action_jump.mp3` are good) | EatACake JuiceConfig placeholders | pending |
| 2026-07-16 | docs | studio-verifier: `get_console_output` truncates — read the tail via `LogService:GetLogHistory()` in execute_luau; AND execute_luau has a SEPARATE require cache (module state invisible — verify via attributes/instances/remotes, not requires) | EatACake Studio verification session | pending |
| 2026-07-16 | recipe | Player-attribute replication channel for cheap all-clients per-player state (StomachFill morph lerp, EquippedPets followers, AutoEat/AutoGym perk flags) — zero remotes, smooth local interpolation | EatACake `BodySubs`/`PetSubs`/`ShopSubs` + client controllers | pending |
| 2026-07-16 | contract | ADR-0002 extension: `rawAmount` escape on currency reward kinds so PAID packs bypass earn multipliers (gems packs vs GemsMult) | EatACake RewardGrantSubs "gems" handler | pending |
| 2026-07-17 | recipe | "Keycap grid" destructible-surface look from plain Parts: column = cell minus gap (dark groove), deterministic `math.noise` shade jitter, per-layer Reflectance, columns CanCollide/CanQuery so collision+raycasts match visuals 1:1 | EatACake CakeRenderer parts path (user-approved reference look) | pending |
| 2026-07-17 | docs | Native `SurfaceType.Studs` on all faces = free LEGO/candy wall look (no textures); code-built prop kit (gumball/lollipop/mint/cookie/bar/cupcake) from primitives ≤6 parts each | EatACake MapService | pending |
| 2026-07-17 | docs | EditableMesh production notes (probe-verified; (b) CORRECTED same day): (a) height-banded looks need a PALETTE TEXTURE (EditableImage 1×256 + per-vertex UVs) — vertex colors smear across tall quads; (b) runtime-edited meshes need RenderFidelity=**Precise** (no LODs, live edits render at any distance) — **Automatic** swaps in LODs generated from CREATION-time content (stale geometry by distance/quality); create at final max extent, never flatten post-create (culling bounds come from creation geometry); (c) `CreateMeshPartAsync` parts render vertices at RAW mesh coords relative to part.CFrame (NO bbox recentering) — mesh y=0 lands at part.Position.Y; (d) recompute-normals rebuilds need two passes; (e) grid hulls: an analytic "+1 ring" of a rounded rect diverges from the discrete footprint at corner staircases — derive skirt/hull faces from 3×3-neighborhood tests, never a second analytic shape; (f) camera-inside-mesh + backface culling masquerades as "mesh not rendering", and HIGH cameras hide a floating-mesh gap — verify grounding from a LOW outside Scriptable camera | EatACake CakeRenderer (the "white/black/invisible/floating cake" saga; grounding fixes flow doc) | pending |
| 2026-07-18 | research | EditableMesh budget: a DYNAMIC EditableMesh reserves WORST-CASE budget (60k verts/20k tris) regardless of content — ~8 fit a desktop client, fewer on laptops; budget enforced in Studio play-test; `CreateEditableMesh`/`CreateEditableImage` return **nil** (no throw, pcall alone is NOT a check), `CreateMeshPartAsync` throws. Pattern: build ONE dynamic scratch → `CreateEditableMeshAsync(Content.fromObject(em), {FixedSize=true})` clones (cost = actual complexity; source vertex/normal ids stay valid on clones — probe-verified) → `scratch:Destroy()`; never Destroy an editable still linked to a MeshPart (wipes render); FixedSize enforcement is runtime-only (AddVertex on a fixed mesh does NOT error in Edit mode). Published games need ID-verify + Mesh/Image API toggle — keep a non-editable fallback renderer. Sources in flow doc | EatACake CakeRenderer `ensurePool` (fixed the "renders as rectangles on the laptop" failure); flow `2026-07-18_layer-meshes-crust-feel.md` | pending |
| 2026-07-18 | recipe | Budget-resilient deformable-surface renderer ladder: N per-layer FixedSize slab meshes (own Material/Transparency/texture) → ONE slab + height-palette texture → visible part grid; lazy pool sized per composition, retry-on-next-reset, every downgrade warns (R8). Per-layer XZ-planar EditableImage carries skin+decals (cracks) with per-cell repaint only on state transitions (cached) | EatACake CakeRenderer (assignBands/ensurePool/paintBandImage); game-agnostic for any heightfield/destructible surface | pending |
| 2026-07-19 | feature | Hexagon TREE UI kit primitive: `HexUtil` (redblobgames axial ToPixel/FromPixel/Round/Dimensions) + `HexNode` (flat-top hex SPRITE stacked Outer/Rim/Face, UIGradient tint per state — the kit bevel on a shape UICorner can't make) + `HexTreeOverlay` (scrim + auto-fit SQUARE canvas + rotated-Frame connectors + hover tooltip + nav-stack drill-down). Game-agnostic; only the tree GRAPH is per-game config | EatACake `shared/HexUtil`, `UIKit/Components/HexNode`+`HexTreeOverlay`, `Theme.HexTree`, `config/UpgradeTreeConfig`, `client/LocalUpgradeTree`; ADR-0005. Sourced (redblobgames, DevForum) | pending |
| 2026-07-19 | docs | Skill exception: a HEXAGON (or any non-rounded-rect shape) needs a SPRITE — UICorner only rounds rects. Recipe: white flat-top hex PNG (System.Drawing), upload via Studio MCP `upload_image` (serve over localhost http first), store id in Theme like `Hud.Icons`; stack Outer/Rim/Face ImageLabels + UIGradient for the candy bevel; keep OutlinedText labels. Prefer an uploaded texture over runtime EditableImage where the Editable* budget is tight. Add to `roblox-ui-kit` style-rules as the documented chrome-image exception | EatACake HexNode (`rbxassetid://125037319877300`) | pending |
| 2026-07-19 | recipe | Auto-fit honeycomb layout: author nodes in AXIAL (q,r), lay on a UNIT hex grid, normalise the tree's bounding box into a SQUARE canvas (uniform scale, centred) → compact and spread trees both fill the view; square canvas keeps Scale positions + rotated connector bars undistorted (non-square Scale skews `Rotation`). Nav-stack + per-tree rebuild for drill-down (no tween — kit forbids animations) | EatACake `LocalUpgradeTree.BuildTree` | pending |
| 2026-07-19 | recipe | "Repackage-keep-power" progression rework: collapse N linear upgrade levels into ~5 discrete TIERS where tier value = old formula AT the boundary and tier cost = Σ of the levels it replaces → end-game power + total grind preserved, remote contract unchanged (buy = level+1). Persistence: `levels[id]`=tier, version bump + migration rescales old saves (`round(oldLvl/oldCap*maxTier)`), StatsService clamps reads (`min(tier,#tiers)`) so over-cap can't inflate | EatACake UpgradeConfig tiers + UpgradesSection v2 + StatsService | pending |
| 2026-07-19 | recipe | Open a menu from a WORLD ProximityPrompt client-side (no remote): server builds the prompt (unique name), client `ProximityPromptService.PromptTriggered` filtered by name → open the UI; opening a menu is local, needs no server trust (R4/R6). DISABLE the prompt while open (it uses E → E-to-close would re-open it, and it hides the overlapping prompt UI); a `BlurEffect` dims the world | EatACake UpgradesSubsClient + MapService UpgradeStation | pending |
| 2026-07-19 | recipe | Honeycomb UX pass (PC + touch parity, "a child gets it"): (a) DENSE packing — each branch a wedge-clump via angular-sector assignment (atan2 + nearest-sector), hexes edge-to-edge `nodeFill=1`, no connectors; (b) MODAL open — freeze camera (`CameraType=Scriptable`, save/restore), `PlayerModule Controls:Disable/Enable`, disable ALL co-located prompts; reconcile on `CharacterAdded` (respawn/Reset-Character would strand the modal); (c) TAP-to-detail — tap a node → bottom card (name/desc/green Buy, dimmed when unaffordable) instead of hover, same on mouse+touch; (d) category NOTIFIER badge when an affordable child exists; (e) reset nav-stack + focus on CLOSE (not a post-open effect) to avoid a 1-frame stale-tree flash; gate the view-model build to open-only | EatACake LocalUpgradeTree(packSectors)/HexTreeOverlay/HexNode/UpgradesSubsClient(setModal)/AppRoot; 2nd adversarial pass | pending |
| 2026-07-19 | recipe | Pan/zoom a UI graph under clickable nodes (React): nodes are NON-interactive visuals in a `World` frame whose Position/Size are `useBinding` pan/zoom (update via binding setters = no re-render) inside a clipped square Viewport; ONE transparent full-screen Active surface ABOVE the nodes / BELOW the controls detects press-START (so a pan must begin in the graph), and drag/pinch/END run on `UserInputService.InputChanged/InputEnded` (global) — because `GuiObject.InputEnded` is NOT delivered when the release lands on a higher-Z Active control (leaks the input map → soft-lock). Taps (press that moved < threshold) HIT-TEST to the nearest node by screen pos. zoom-around-cursor keeps the point fixed; pan clamped; reset zoom/pan/focus + clear input refs on close/nav. Detail popup is Active (inert to the pan surface). Verified: drag ending on a control no longer breaks the next drag | EatACake HexTreeOverlay; 3rd adversarial pass (CRITICAL leak found+fixed) | pending |
| 2026-07-18 | contract | Client subs hooking the character: capture Humanoid AND HumanoidRootPart via wait-or-warn, and after every yield bail if `player.Character ~= character` (fast-respawn stale-hook overwrite); one subscription OWNS each Humanoid event domain (two Landed handlers double-fired FX) | EatACake CakeFeelSubsClient review findings; generic character-lifecycle gotcha for the template's subs pattern | pending |
| 2026-07-18 | docs | EditableMesh gotchas, round 2 (probe-verified): (a) NEITHER per-vertex color alpha (AddColor/SetColorAlpha/SetFaceColors) NOR EditableImage texture alpha renders on a MeshPart — both display fully opaque; never design around alpha-hiding parts of an editable mesh; (b) fixed-topology stacked slabs: hide eaten-through sheets by TUCKING them ~2 studs under the local surface (below a translucent layer's BOTTOM when it owns the surface) — dropping to y=0 hangs curtain quads through side cuts, clamping at band.bottom floats films over craters; tuck depth must exceed every downward visual offset (squish+cracks+wobble); (c) discrete-footprint staircases pleat skirts at rounded corners — project ring/boundary verts onto the analytic outline (expanded half a cell so straight edges stay on the collision boundary) | EatACake CakeRenderer iteration 2 (`2026-07-18_layer-meshes-crust-feel.md`); generic for any editable-mesh destructible surface | pending |
| 2026-07-18 | recipe | ALWAYS-VISIBLE deformable "cracking crust/wax shell" over a heightfield surface, as GEOMETRY not texture: ONE MeshPart of organic VORONOI pieces (seed points → Sutherland-Hodgman clip by bisectors → clip to the loaf outline for a clean edge). Each piece owns its boundary verts (on the exact Voronoi edge → neighbours MEET, no gap = SMOOTH at rest) + a raised centre (dome). Ride the surface by re-reading mirror heights per frame; HIDE pieces where eaten; under the foot a per-piece dent 0..1 DENTS+TILTS+SPREADS-toward-centroid so cracks open ONLY underfoot (the layer below shows through). Key insight from ~9 rejected iterations: the requirement was a FIELD that cracks in place, NOT an effect spawned/anchored to the player (texture-reveal, follow-cluster, and stamped-patch attempts all failed) | EatACake `CakeWaxShell` (`2026-07-18_wax-shell.md`); game-agnostic destructible-surface skin | pending |
| 2026-07-18 | docs | Async EditableMesh build (CreateEditableMeshAsync/CreateMeshPartAsync YIELD) called lazily from RenderStepped MUST have a re-entrancy guard: the next frame's Step fires during the yield and re-enters, appending geometry to a second scratch → corrupt state (symptom: "works if you stand, breaks if you run immediately; only the pieces you spawn on work"). Set a `building` flag before the first async call; block re-entry until built/failed | EatACake CakeWaxShell (user-reported spawn race) | pending |
| 2026-07-18 | docs | `EditableImage:WritePixelsBuffer` throws "expects buffer length to equal size.X*size.Y*4 bytes" if the passed buffer is not EXACTLY that size — a reused/over-allocated scratch buffer fires it every call. Size the buffer to the exact write region; reallocate only when the region dimensions change | EatACake CakeRenderer (earlier texture-reveal attempt) | pending |
| 2026-07-18 | docs | **Character JOINT posing on modern avatars (probe-verified, corrects a same-day assumption):** many avatars use `AnimationConstraint` joints, NOT `Motor6D` (no `RootJoint` Motor6D; shoulders etc. are AnimationConstraints). The Animator OVERWRITES any joint `.Transform` every frame at EVERY `RenderStepped`/`BindToRenderStep` priority (incl. `Character+1` and 2000) — so you CANNOT layer a pose via Transform. The channel that HOLDS is the joint's STATIC offset: `Motor6D.C0`, or the constraint's `Attachment0.CFrame` (Animator never touches it) — but only the ROOT (un-animated) offset gives a clean whole-body rotation; offsetting an ANIMATED joint's attachment did not move the limb. Also: runtime Humanoid `BodyWidthScale`/etc. only rescale the character when set on the **SERVER** (client writes are reverted by replication — probe: client=4→no change, server=3→armspan 2.1→5.9; `ApplyDescription` from client can fail; direct part `.Size` is fought by the auto-scaler). Set BodyScale server-side for morphs | EatACake eat-gesture + body-morph/roll (the "no animation" + "body won't scale" causes); generic for procedural posing/scaling | pending |
| 2026-07-18 | recipe | Procedural "consume/use an item" gesture with NO uploaded animation asset and NO joint posing (rig-agnostic): a pooled piece flies source→hand→mouth along a two-arc Bézier (waypoints re-read live from Head/Hand part positions so it tracks a moving body), shrinking at the end; speed derived from an action-rate stat. Pool built once at Init (ChunkDebris pattern). The flying piece alone reads as "eating/using" — avoids the fragile per-rig joint posing above | EatACake `EatGestureController`; game-agnostic hand-to-mouth/use gesture | pending |
| 2026-07-18 | recipe | Fill-driven "grow only the TORSO" MORPH + forward TUMBLE, on the REAL avatar (no proxy meshes, no added parts — user rejected a welded ball AND whole-body scaling): grow ONLY the torso by lerping each torso part's `OriginalSize` (BodyScale left at 1 → auto-scaler grows only the torso and it HOLDS; arms/legs/head keep natural size), SERVER-SIDE (client writes revert), snap-to-target on final approach; capture true natural OriginalSize ONCE per part (re-reading the scaled value compounds — a weak-key cache silently dropped it → torso blew to 112 studs; use strong table + prune). Separately TUMBLE the whole body past a threshold by rotating the ROOT joint's static offset (Motor6D.C0 / AnimationConstraint Attachment0) by distance, settling upright when stopped; client-side, VISUAL ONLY. Caveat: huge torso can engulf the 3rd-person camera at close zoom | EatACake `BodySubs` torso morph + `BallRollController` tumble (supersedes the welded-ball + whole-body-scale attempts); generic fat-belly-that-rolls | pending |
| 2026-07-18 | fix | ProcessReceipt not idempotent for CONSUMABLE dev products — only oneTime items dedup (IsOneTimeOwned), no PurchaseId ledger → Roblox receipt re-delivery double-grants gems/pets | EatACake ShopSubs.processReceipt; template feature-library shop. Fix: persisted bounded PurchaseId FIFO section, check before grant, gate on ProfileStore LastSavedData | pending |
| 2026-07-18 | fix | Net.Remote/Net.Update use WaitForChild with NO timeout — a missing/renamed remote yields forever inside a subscription Start(); the pcall'd bootstrap loop yields with it, later subs never wire, only engine "Infinite yield" (not Log-routed) | EatACake Net.lua; core template infra. Fix: bounded WaitForChild + Log.Warn/GraceOnce, or a boot-time resolver returning a nil-safe stub | pending |
| 2026-07-18 | fix | R8: unloaded-profile silent early-returns in remote handlers (BuyUpgrade/ClaimQuest/DoRebirth/EquipPet + ShopSubs offline-payer) — no Log while sibling CakeSubs logs Log.Once('eat-preload') | EatACake fixed locally in 5 subs; template feature-library has the same silent returns. Every IsLoaded/GetPlayerByUserId early-return on a money/action path must Log.Once | pending |
| 2026-07-18 | docs | ProfileSchema sanitize must guard `type(x) ~= "table"` before `pairs(x)` — deepReconcile only fills nils, never repairs a corrupt non-table field; UpgradesSection lacked the guard its siblings have | EatACake UpgradesSection vs QuestsSection/PetsSection; add to add-profile-section recipe gotchas | pending |
| 2026-07-18 | fix | One-time consumption ordering: promo `redeemed[code]=true`+Save BEFORE the grant is confirmed (HasHandler ≠ "will grant"); a declining reward (amount<=0 / bad boostId) eats the redemption while status='ok' | EatACake CodesSubs.TryRedeem; same class as shop grantableList. Grant BEFORE consume, or validate args in the pre-guard | pending |
| 2026-07-18 | fix | Dead `reward.kind=="gold"` amount>0 guards in ShopSubs.grantableList + GroupRewardSubs (no "gold" kind exists — currency is gems) so the intended amount<=0 pre-validation never runs for real currency | EatACake ShopSubs/GroupRewardSubs; template loot-grammar residue from a gold→gems rename. Rename guards to "gems" or drop | pending |
| 2026-07-19 | recipe | Server-authoritative "return to station" teleport (F via ContextActionService + HUD button → args-less remote; destination is a server-owned CFrame, never client-supplied; IsLoaded/char guards + short debounce anti-rag-doll; textbox-focus guard so typing the hotkey doesn't fire). Generic QoL for any "go back to base/shop/gym" flow | EatACake `ReturnToCheckpoint` (CakeSubs + BodySubsClient); no keybind system existed before | pending |
| 2026-07-19 | recipe | Height-tracking platform beside a heightfield surface: a plate on 4 auto-resizing legs (floor→plate bottom) whose top follows the surface's current top BAND (from the field scan's `topBandIndex`), stepping down as layers are consumed. Dedup guard skips redundant same-height re-asserts (no per-tick re-replication of anchored parts). Generic for any layered/destructible-surface game needing a stable ledge at eating height | EatACake `MapService.SetCheckpointHeight` + CakeSubs scan | pending |
| 2026-07-19 | recipe | **Baseline-drain "convert a stored pool" session** (replaces a timed mash minigame): on start capture a start amount + a single `progress` 0..1; the live value is `start×(1-progress)` and banked = `floor(start×rate×progress)` credited as a MONOTONE integer delta each tick (`bankTarget − bankedSoFar`) → total exactly `floor(start×rate)`, no drift/double-count even if a tap overshoots. Advance via passive `speed×dt` + per-tap `perTap` (base 0.10 = 10 taps to clear = intuitive); an `instant` upgrade just SEEDS progress (max tier = 1.0 = done on start). Server re-checks proximity each tick to STOP on walk-away; any external "empty the pool" (rebirth/auto/reward) must end the session first or the baseline rewrite re-inflates it. No tap anti-cheat needed — taps only drain the player's OWN bounded pool | EatACake `GymService`/`BodySubs` fat-drain rework (`2026-07-19_gym-fat-drain-rework.md`); generic for any tap-to-convert loop | pending |
| 2026-07-19 | recipe | Mobile RIGHT-THUMB action overlay: a full-screen layer whose ROOT frame is NOT Active (so the joystick/walk still works — the player can leave to end the action), with the big round action button anchored bottom-RIGHT (~0.82, 0.70) and a progress bar/label stacked above it; drives a `useBinding` fill eased ~30 Hz toward a server-streamed fraction so it reads smoothly between ~8 Hz pushes | EatACake `GymOverlay` + `Theme.GymOverlay` | pending |
| 2026-07-19 | feature | Top-down LAYER GATE for a layered heightfield: bites clamp to the current top band's bottom (`activeFloorUnits`), advancing down as each band is leveled/auto-swept (reuse the field scan's `topBandIndex`; advance one lower IN the sweep scan so the fresh floor isn't spuriously "locked"). Server clamp is authoritative; `activeBandIndex` rides snapshot meta + the cycle update so the client's bite PREDICTION clamps to the SAME floor (no phantom crater); aiming at a spot at the floor pops a client cue + skips the bite (debounced, HELD-input only so auto-eat doesn't nag). Config flag toggles it. Generic for any strata-eating/mining game where layers should clear one at a time | EatACake `CakeConfig.layerGate` + CakeFieldService(ApplyBite/ScanStats/ResetCake) + CakeStateData + LocalCakeField + CakeSubs(Client) (`2026-07-19_layer-gate.md`) | pending |
| 2026-07-19 | recipe | Balance a consume-loop by MODELLING it off-engine before touching config: replicate the real per-action math (crater vol ≈ depth·πR²/2 / hardness) in a throwaway JS/Lua sim, then a time-stepped loop+economy pass (eat→full→drain→bank→buy greedily→repeat) to read out clear-time, action-vs-travel time split, per-fill duration, and upgrade cadence; binary-search the resource size for a target clear-time and sweep the one co-op knob. Two decoupling insights it surfaced: (a) clear-time & cadence depend on the income/cost RATIO, so the ABSOLUTE currency scale is free (rescale income+costs together for readable prices without touching pace); (b) a steeper TOP tier spreads cadence across the run. Beats guessing — caught a 1-bite-full and a maxed-at-32% surplus before any playtest | EatACake `scratchpad/model3.js` + `2026-07-19_easy-mode-balance.md`; generic tuning method for any eat/mine/idle loop | pending |
| 2026-07-19 | docs | Balance gotcha: when belly/inventory CAP is a flat number but the per-action amount grows as depth·radius² (or any super-linear stat product), the two diverge and one upgrade makes a single action fill the whole cap ("1 bite = full", loop becomes travel-dominated). Keep the cap holding MANY actions at EVERY tier (sanity-check bites-per-fill at base AND max on the extreme layer), and flatten the action-size growth — don't let a linear cap chase a super-linear action | EatACake capacity 150→2600 + biteDepth 26→1.8 (`UpgradeConfig`); generic for any capacity-vs-yield economy | pending |
| 2026-07-19 | recipe | Sublinear co-op scaling for a SHARED depleting resource without changing its footprint: scale the resource's HEIGHT/depth (not area) by `1 + perScale·(n-1)` for n players present, clamped to a ceiling — more mouths drain it faster but a bigger pool keeps it from trivially fast (solo ~40 min → 4p ~18, not ~10). Wire the already-passed `playerCount`; height is often the only free lever when a grid caps the footprint | EatACake `CakeConfig.composition.perPlayerScale` + `CakeCycleService.RollComposition`; generic for shared boss/lake/vein resources | pending |
| 2026-07-19 | fix | **Vestigial per-cell `EditableImage` = mobile CPU spike.** When a runtime-painted texture (per-layer/per-cell EditableImage) ends up filled with a UNIFORM color — e.g. after the decorative detail it carried (crust) is moved to a separate overlay — it becomes a pure cost: a `WritePixelsBuffer` upload per surface per full rebuild (~86 ms single-frame stall on mobile, paced by the reset cadence) AND a `DrawRectangle` per changed cell per edit (per-bite churn/GC), for ZERO visual gain vs `part.Color`. Fix: render flat `part.Color`; keep an EditableImage ONLY where it holds genuine per-cell variation (e.g. the height-palette FALLBACK slab). AUDIT any `paintBandImage`/`updateCellPixels`-style texture path — if every cell writes the same color, delete the texture. Diagnostic method: profile in Studio play (client datamodel) — a full rebuild self-logs its ms; a synthetic same-index snapshot re-broadcast forces a clean non-first rebuild to measure; watch that Studio's shared-process 1 Hz SERVER work (full-field scan) doesn't masquerade as a client spike | EatACake `CakeRenderer` (assignBands drops multi-band image; ~153 ms→6–7 ms rebuild); template CakeRenderer lineage (recipe rows 2026-07-18 "per-layer EditableImage" #30) likely carries the same trap once slabs render body-only + a wax overlay | pending |
| 2026-07-19 | recipe | Bare HUD "icon column" menu button (no background): a standalone icon with the label just BELOW it, the WHOLE icon+label rectangle a transparent TextButton (big phone tap target). Self-constrains with `UIAspectRatioConstraint DominantAxis=Height` so it fills the layout row height and derives its own (narrower) width, left-aligning under a vertical `UIListLayout` (the constraint touches only the cross axis, so stacking stays correct). Icon `ScaleType=Fit` self-fits a square in its zone; optional notification Badge on the icon corner; per-entry icon map in Theme (placeholder until real art). Generic HUD menu style for any simulator | EatACake `HudMenuButton` + `Theme.HudMenuButton`/`AppHud.MenuIcons`, driven by AppRoot menu loop (replaces blue `Theme.MenuButton` text rows); the kit demo `Hud.lua` already had the pattern un-exported | pending |
| 2026-07-19 | recipe | Proximity-gate a "teleport to station" HUD button so it only shows when you're AWAY from the station: client sub (existing RenderStepped) throttled to ~5 Hz reads the station part's LIVE XZ footprint (`|dx| ≤ Size.X/2 + margin`, same Z; part built unrotated so Size maps to world axes), pushes a `far` boolean to the HUD state ONLY on flips (no per-tick re-render). Use the FOOTPRINT not a radius when the station abuts another walkable surface (here the plate is only edgeGap off the cake edge — a radius can't separate them, the footprint can). Margin/threshold in the UI theme/data module (R1), not the sub. `Log.GraceOnce` if the part never replicates | EatACake `BodySubsClient` checkpoint proximity → `AppRoot.checkpointFar`; pairs with the ReturnToCheckpoint teleport recipe | pending |
| 2026-07-19 | contract | A shared HUD "bar" widget reused across phases (progress / boss HP / countdown / reward) needs a `visible` prop so a game can hide it in ONE phase without losing the others — gate visibility in the composing root by phase (`phase ~= "eating"`), don't delete the widget. Cheap optional prop (`Visible = props.visible ~= false`), backwards-compatible | EatACake `CakeBar.visible` + AppRoot `cakeVisible`; generic for any multi-purpose HUD meter | pending |
| 2026-07-19 | feature | **Kit-wide UI "juice" primitive** `Interaction.lua` (`usePressable`→(scaleRef,handlers) hover/press UIScale bounce via TweenService; `pressLayer` center-anchored `Content` wrapper; `merge`; `useFillGlide`; `ZeroFill`) + `Theme.Feel` (all timings). Applied to Button/HudMenuButton/CloseButton/IconButton (press), PanelShell (open/close pop), Badge (pop-in), BellyBar/CakeBar (fill glide), Toggle (knob slide). Load-bearing contract (ADR-0006): a ref-tweened property is NEVER also written by React on re-render (HUD re-renders ~14×/s → React clobbers the tween) — pass NO prop (UIScale has no Scale) or a CONSTANT (`ZeroFill`/`KNOB_INITIAL`, value-equal so the reconciler skips it); center-origin needs the UIScale on a `(0.5,0.5)`-anchored inner frame, not the top-left-anchored/grid-positioned root. Game-agnostic; backward-compatible (drives off existing props). Supersedes skill iron rule 8 for sanctioned press feedback (still bars ad-hoc per-component animation) — update `roblox-ui-kit` skill (`references/components.md` + rule 8) | EatACake `shared/UIKit/Interaction.lua`, `Theme.Feel`, 8 components; ADR-0006; Studio-verified (hover 1.05, panel pop 0.85→1, toggle slide, no clobber). Prior kit had ZERO interaction animation | pending |
| 2026-07-19 | recipe | Hide a shared world ProximityPrompt PER-PLAYER when it would no-op: the client mirrors the server's action guard by setting `prompt.Enabled` LOCALLY (a client write on a server-owned instance is per-client and holds unless the server writes Enabled). Resolve the prompt by CLASS (`machine:FindFirstChildWhichIsA("ProximityPrompt")`) to avoid coupling to the server-only prompt name; drive it from a throttled client loop + the relevant state update, writing only on change; server guard stays as defense-in-depth. ⚠ If ANOTHER client owner also toggles that prompt's Enabled (e.g. a fullscreen modal that disables all world prompts so its close-key can't re-trigger them), the steady gate must DEFER while that owner is active (skip the write) or the two fight — a per-frame re-enable defeats the modal and reopens the exploit (caught in adversarial review; needs a shared "is-modal-open" read — a getter on the UI state module) | EatACake `BodySubsClient` hides `GymPrompt` ("Burn it off!") while belly `fill < minStartFill` (mirrors `BodySubs` guard), skips while the upgrade-tree modal owns the checkpoint prompts (`AppRoot.GetOpenPanel()`); generic for any "prompt does nothing in state X" (empty gym, full inventory, on-cooldown) | pending |
| 2026-07-19 | recipe | HUD icon menu as a config-driven GRID not a fixed column: `Theme.AppHud.MenuColumns` knob, container `UIGridLayout` (`FillDirectionMaxCells=cols`, cell size/padding as fractions of the menu frame), rows=`ceil(count/cols)`, frame sized `cols·w+(cols-1)·gapX × rows·h+(rows-1)·gapY`. Keeps the button's biggest-tap-area fill (no per-button size), stops the column running to screen-bottom. Cell math must close to 1.0 and `FillDirectionMaxCells` MUST be set (float-rounding wrap, pitfall #6); works because `HudMenuButton` has NO aspect constraint (aspect-in-grid collapses cells, pitfall #2) | EatACake `AppRoot` menu loop + `Theme.AppHud` (8 buttons → 2×4); generic for any simulator HUD icon menu | pending |
| 2026-07-20 | fix | **Destructible-heightfield collision = a physics-broadphase mobile spike; radius-limit it.** A grid of CanCollide "column" parts mirroring an edited surface (walking collision) that resizes every changed column per frame is a hidden PHYSICS cost, not a Lua/mesh one: resizing many CanCollide parts per frame re-indexes the broadphase (Studio `Workspace.Heartbeat`/physics-step 60+ ms while editing, settling when the surface stabilizes — reads exactly like a mysterious "eat/dig spike that recovers in 5-10 s"). Lua profiling of the mesh step shows < 1 ms and misleads you. FIX: only refresh the collision columns within a RADIUS of the local player each frame (they can't collide with distant terrain); the VISUAL mesh still updates everywhere (cheap Lua, no physics). Distant stale columns are safe — a drop there snaps down before the player arrives; big rises only come from a full snapshot which snaps ALL. Diagnostic notes: read `game.Stats.Workspace.Heartbeat`/`Contacts` to separate physics from Lua; and a FULL consumable cap (e.g. belly `isFull`) silently blocks the action → your "stress test" does nothing (drain it first). Prior spike in this lineage was a vestigial texture (row 2026-07-19); this one is the collision grid | EatACake `CakeRenderer.updateCollisionNearPlayer` (`2026-07-20_mobile-eat-spike-physics.md`); physics 60.6→6.4 ms; generic for any eat/mine/dig heightfield with per-cell collision parts | pending |
| 2026-07-20 | recipe | **Comfortable collision on a destructible HEIGHTFIELD you eat/dig while moving** (fix "constant bounce / falls through / catches on craters"): (a) ONE walkable surface — the coarse SERVER safety grid sits at the **MIN** of its block (min-of-NxN ≤ every fine client column in it) so it can never poke above the fine client-column collision and block a descent into a fresh crater, yet still catches a falling player on join (keeps CanCollide, no collision-group join-hole); (b) rate-limit the column RISE only — a bite DROPS collision instantly (fall into the hole) but cake oozing/refilling back rises at a capped studs/s (`stepCollisionRise` each frame), so refilling terrain never punts the player up: they stay partly BURIED and jump to climb out; a full snapshot/new-surface SNAPS (never rate-limit a legit full rebuild → no fall-through); (c) FLAT-WHILE-ACTING — suppress the per-layer landing bounce + cap the jump-boost while the player is performing the core action (a shared client `LocalEatState` flag the input sub sets and the feel sub reads), so running-while-eating goes straight instead of bouncing; keep a TONED-DOWN feel when just moving | EatACake `CakeCollisionService` (min slabs) + `CakeRenderer` (rate-limited column rise) + `CakeFeelSubsClient`/`LocalEatState`/`CakeConfig.feel` (`2026-07-20_collision-rework.md`); generic for any eat/mine/dig heightfield | pending |
| 2026-07-20 | contract | **Dedicated TOUCH action button replaces "touch anywhere = action"**: on mobile a raw finger (movement joystick / camera drag) must NOT trigger a gameplay action — drive it from an on-screen HOLD button instead. Extend `Interaction.usePressable` with optional `onPressStart`/`onPressEnd` HOLD callbacks (a tap fires both) and make press FINGER-AWARE: refcount the Touch InputObjects that began on the button (+ a `mouseDown` bool); `onPressStart` on first-down, `onPressEnd` on LAST-up. This makes hold multi-touch-correct (a 2nd finger tapping the same button can't cancel the first finger's hold), catches drag-off release (GuiObject delivers InputEnded for a touch that began on it), and the disabled/hidden reset fires `onPressEnd` so a button hidden mid-hold releases (no stuck-on). Show the button only on TOUCH (`TouchEnabled and not KeyboardEnabled`); PC keeps mouse-hold-anywhere. Backwards-compatible — existing buttons pass only `onActivated`, so the new refcount/reset branches are no-ops. Wire the button through the composed root's `SetCallbacks` (onXDown/onXUp) and have the domain sub fire ONE immediate action on down (a tap can begin+end within a frame) then auto-repeat while held | EatACake `Interaction.usePressable` + `UIKit/EatButton` + `Theme.EatButton` + `CakeSubsClient` input rewire (`2026-07-20_touch-eat-button.md`); generic for any hold-to-act mobile control (eat/mine/shoot/mash) | pending |
| 2026-07-20 | recipe | **Window-render a layered/strata destructible heightfield: draw only the CURRENT + NEXT stratum, hide the bulk behind a textured wall.** When a top-down LAYER GATE (queue row 2026-07-19 "Top-down LAYER GATE") means the player can only ever see the active band + the one below it, render ONLY those 2 as slab meshes (`assignWindowBands`/`windowFor` = `{activeIndex-1, activeIndex}` bottom-up), sliding the window DOWN one band as the gate advances (`rewindow`, polling the client's active-band index in the render step; reuse the 2 pooled slabs — no mesh creation, no yield; guard against overlapping a yielding full rebuild). Hide everything below with a SEPARATE textured "wrapper" wall module (one FixedSize-clone MeshPart on the loaf outline, born full-height so culling bounds cover the ride, top ring `SetPosition`/`SetUV`'d down to the lowest rendered slab's bottom as layers finish; random texture per resource by index; reads the mirror itself = no renderer coupling). DECOUPLES layer count from the EditableMesh vertex budget (was hard-capped at ~8 slabs) → arbitrarily many/taller strata at fixed 2-slab cost, and re-enables per-layer image textures (only 2 slabs = cheap static `TextureContent`). Correctness RESTS on the gate being ON (free-dig would expose the void). ADR-0008 | EatACake `CakeRenderer` window + NEW `CakeWrapper` (`2026-07-20_taller-lazy-layers-wrapper.md`); generic for any eat/mine/dig strata heightfield with a layer gate | pending |
| 2026-07-20 | recipe | **Same-pace PROPORTIONAL rebalance when scaling a depleting resource ×N.** To make a resource N× bigger WITHOUT changing clear-time or economy, scale the coupled levers together: resource size ×N, per-action yield ×N (e.g. crater volume ∝ depth·radius² → bump the depth stat ×N), capacity ×N (holds the same NUMBER of now-bigger actions → same fill duration), and reward-per-unit ÷N (income/sec and income/resource stay flat). Upgrade COSTS and any co-op scale knob stay UNCHANGED (the pace depends on the income/cost RATIO, which is unmoved — see queue row "clear-time & cadence depend on the ratio, absolute scale is free"). Net: same clear-time, same income, same progression — the resource is just visually N× bigger. Verify the extremes in play (bites-per-fill at base AND max) since it's proportional, not sim-searched | EatACake 3× cake: `biteDepth` ×3 + `capacity` ×3 + layer `calories` ÷3, costs/`perPlayerScale` untouched (`UpgradeConfig`/`CakeConfig`, `2026-07-20_taller-lazy-layers-wrapper.md`); generic for any eat/mine/idle resource scale-up | pending |
| 2026-07-20 | docs | Image-asset texture on a WORLD MeshPart uses `part.TextureContent = Content.fromUri("rbxassetid://ID")` (mapped by the mesh UVs) — the counterpart to `Content.fromObject(editableImage)`; both are the modern `Content`-typed `TextureContent` API (the legacy `MeshPart.TextureID` is being retired). Wrap the assignment in `pcall` + `Log.Once` (R8) so a bad/decal id degrades to `part.Color` with a console signal instead of a stark untextured part. Prior CakeRenderer only ever used `fromObject` (procedural EditableImage); asset textures were a new pattern. ⚠ CAVEAT (found next day): `TextureContent = Content.fromUri` displayed the image only where the mesh UVs are 0..1 (the frosting slab) — on a wall mesh with TILING UVs (0..N) it showed NOTHING. For a TILED image on a runtime part, a plain Part + `Texture` instances (per-face, `StudsPerTileU/V`) is the reliable path (next row) | EatACake `CakeRenderer` layer textures (worked) + `CakeWrapper` wall (did not) | pending |
| 2026-07-20 | docs | RELIABLE tiled image on a runtime-sized part = a plain **Part + `Texture` instances** (one per visible `NormalId` face, `StudsPerTileU/V` for density that holds as the part resizes), NOT a MeshPart with `TextureContent = Content.fromUri` + tiling UVs (showed no texture) and NOT an EditableMesh (reserves worst-case budget for a wall you don't need to deform). Cheaper (no mesh budget, no async build), and a `Texture` renders even on a `Transparency=1` part (to hide, park the part off-screen, don't just set Transparency). Trade: a Block can't match a rounded-rect footprint — square corners poke past the round outline (rounded corners would need EditableMesh or untextured corner cylinders) | EatACake `CakeWrapper` rewrite (EditableMesh wall → textured Part, `2026-07-20_clean-eat-textured-wall.md`); generic for any textured static cover/wall/skybox-ish part | pending |
| 2026-07-20 | docs | **A DECAL asset id does NOT render as a `Texture`/`TextureContent` — it needs the underlying IMAGE id.** User-supplied "texture" ids are usually Decal wrappers (that's what the catalog/upload gives); assigning one to `Texture.Texture`, `Decal.Texture`, or `MeshPart.TextureContent = Content.fromUri(...)` renders BLANK and `ContentProvider:PreloadAsync` returns `Failure`. Resolve it: `InsertService:LoadAsset(decalId)` → find the loaded `Decal` → its `.Texture` property IS the image id → use THAT. Always PreloadAsync (Success) + render-test on a temp part before trusting an id. Symptom that misleads: the part shows its flat `Color` (which may look plausibly on-theme), so "it kind of works" hides the total texture failure — verify with a control texture (`rbxasset://textures/face.png` tiles instantly if the pipeline is sound) | EatACake cake textures: all 6 user ids were dead decals; resolved to image ids via LoadAsset (`2026-07-20_clean-eat-textured-wall.md`, env memory); generic for ANY "use this texture id" task | pending |
| 2026-07-20 | recipe | **CLEAN-CUT eating for a layered destructible heightfield** (fix "messy tiny leftover pieces, hard to eat"): (a) a bite CLEARS its footprint TOWARD the current layer's floor — `newH = floor + (h-floor)·(1 - clamp(falloff·(depthStuds/clearRefDepth)/hardness))` — a scoop that reaches the floor at the center and tapers to a soft rim, so one side of a layer clears completely while the other stays full (a clean cut edge), instead of a shallow paraboloid dent that strands crumbs. The action's DEPTH stat becomes the clear STRENGTH (widens the full-clear core), the RADIUS stat the scoop size; shrink the radius (~½) to keep volume/bite + pace when switching from shallow-dent to clear-to-floor. (b) A periodic SLIVER SWEEP collapses any active-layer cell within a small threshold of the floor to the floor — the top-down layer gate clamps bites AT the floor, so sub-floor slivers are otherwise impossible to eat and look messy. Shared bite math so client prediction matches (pass the ref-depth as a param). ⚠ Balance is proportional in theory but tune clear-time by feel; watch settle-ooze vs sweep at the cut edge (reads as drips) | EatACake `CakeOps.ApplyBite` + `CakeFieldService.ScanStats` sliver sweep + `CakeConfig.sim` (`2026-07-20_clean-eat-textured-wall.md`); generic for any eat/mine/dig strata surface with a layer gate | pending |
| 2026-07-22 | docs | **`BasePart:PivotTo()` moves descendant BaseParts** (unlike `part.CFrame = ...`, which moves ONLY that part). So an authored VISUAL built as child parts of a collider Part (a treadmill Model under an invisible `GymMachine` Part) rides a moving anchored parent for free via `PivotTo`. Pitfall: adding a manual "also move descendants by delta" loop AROUND a `PivotTo` DOUBLE-moves them (the visual ended at ~2× the intended height). Rule: to reposition a Part-with-child-visual, just `PivotTo` — never `.CFrame =` + a descendant loop. | EatACake `MapService.SetCheckpointHeight` (treadmill/station ride the plate); generic for any code-positioned machine/prop authored as child parts | pending |
| 2026-07-22 | recipe | **Mount-a-player-on-a-machine + animate** (treadmill / chair / vehicle / minigame station): server (1) teleports the HRP to a mount CFrame + **anchors** it (locks the player in place — client control can't move an anchored root), (2) plays a looping animation SERVER-side on the Animator (replicates to everyone; no client-ownership ambiguity while the char is server-anchored) — prefer the rig's own `Animate.<state>` id, config fallback. RELEASE must be bulletproof: a per-tick SAFETY NET unmounts any player who is mounted-but-has-no-session (so ANY external state-ender can't leave them stuck anchored), plus `CharacterRemoving` (death/reset) + `PlayerRemoving`. Capture the pre-anchor `Anchored` to restore it exactly. | EatACake `BodySubs` treadmill run + `MapService.GetGym{Mount,Dismount}CFrame`; generic for any "stand here and do X" station | pending |
| 2026-07-22 | docs | Studio MCP verify when the **Rojo plugin is DISCONNECTED** (Studio shows stale source even though `rojo serve` is running, and you can't click Connect headlessly): serve the repo over local HTTP (`python -m http.server`) and, in an `execute_luau` (Edit), `HttpService:GetAsync` each changed file → set the target ModuleScript `.Source`, then playtest. Bypasses the plugin; also validates syntax on boot. (Complements the clone-to-force-fresh-require gotcha.) | EatACake 2026-07-22 verify (plugin was disconnected); generic Studio-MCP workflow | pending |
| 2026-07-22 | docs | **Mixed line endings in one repo** (some files CRLF, some LF) silently break multi-line exact-match edits (the Edit tool matched single lines but failed every multi-line CRLF block because the pattern used LF). Detect per file before a multi-line edit, or do byte-exact replaces with a script. Worth a note in the editing/tooling guidance. | EatACake (BodySubs/CakeWaxShell CRLF, MapService LF) | pending |
| 2026-07-22 | docs | **Hide/clip a surface-riding decoration by its AREA, not its vertices.** A mesh piece (wax plate, decal, overlay) that RIDES a deforming heightfield and should vanish over a hole/crater must test the surface across its FOOTPRINT, not just its boundary vertices: a piece whose verts sit on the rim while its body spans the hole hangs a flat SHELF that vertex-sampling misses. Sample each fan-TRIANGLE centre ((centroid+2 verts)/3, precomputed once) and hide if any is below the hole threshold. | EatACake `CakeWaxShell` straddle hide (`2026-07-22_eat-beneath-cleaner-crater.md`); generic for any surface-riding overlay that clips to eaten/dug terrain | pending |
| 2026-07-22 | fix | **Autosave doc drift: `~30s` is WRONG for vendored ProfileStore** — real `AUTO_SAVE_PERIOD=300`, and freshly-loaded profiles skip autosave for the first 150s (`ProfileStore.luau:170,2126`). The template's persistence docs almost certainly carry the same wrong `~30s`. Fix the number AND add explicit milestone `Save()` at high-value earning moments — a short session/round persists ONLY on leave otherwise, so a crash wipes it. | EatACake 2026-07-22 (verified against the vendored file); template is the source of the copied doc | pending |
| 2026-07-22 | contract | **Partition-aware bootstrap loader** — generalize `loadFolder` to a `collectPartitions()` that either treats root as one flat partition (template default) or merges child `Common`/`Lobby`/`Game` folders, each with the same `data`/`services|modules`/`subscriptions`. Duplicate names warn (R8). Backward-compatible (flat = today). Lets one repo build multiple places (multi-place universe) with a byte-identical bootstrap file; routing decided purely by which partition folders a place's project.json maps. | EatACake lobby/game split (ADR-0009); generic template capability for any hub↔activity game | pending |
| 2026-07-22 | contract | **`PushInitialState` hook pattern** — replace `PlayerLifecycleSubs`'s hardcoded list of sibling `Send*` requires with per-sub `PushInitialState(player)` hooks discovered from the merged `subscriptions` registry (passed as `Start`'s 3rd arg). A sub absent in this place is simply skipped → the SAME lifecycle file runs in any place. Also decouples feature onboarding (add a hook, not a line in Lifecycle). | EatACake split; generic — the template's `PlayerLifecycleSubs` has the same hardcoded coupling | pending |
| 2026-07-22 | contract | **Cross-sub coordination via the `subscriptions` registry, not `require(script.Parent.X)`** — a sub that calls another sub resolves it from the registry at `Start` top (guarded for optional/cross-place). Makes subs location-independent (survive being moved to another partition) and extends the R3/R4 "coordinate via subscriptions" rule to sub↔sub calls. | EatACake split (9 subs converted); generic | pending |
| 2026-07-22 | fix | **`intentionalRelease` flag on `PersistenceService.Unload`** — `OnSessionEnd` kicks `session-taken` whenever a session ends while the player is still present; a deliberate pre-teleport release then kicks the player mid-hop. Add a `PlayerProfileData.releasing` flag set by `Unload(userId, true)` and consumed (cleared) in `OnSessionEnd` to suppress the kick. Prereq for ANY cross-place teleport; harmless when never passed. | EatACake split (verified kick at `PersistenceService.OnSessionEnd`); template PersistenceService needs it before it can support teleport | pending |
| 2026-07-22 | research | **Two-place lobby↔game universe + Option A ProfileStore handoff** — WATCH: adopt only when a game needs a separate hub/activity place. One universe (shared lock); source `Save`→`Unload(intentional)`→wait-for-release→`TeleportAsync`, dest runs unchanged `LoadProfile`, NEVER `Steal`; re-read profile on arrival; verify on PUBLISHED places only. Repo = `src/{server,client}/{common,lobby,game}` + per-place project files, `default` = combined build. Keep single-place-with-zones the template default. | EatACake split (ADR-0009); evidence in the split design workflow | pending (watch) |
| 2026-07-22 | contract | **Data-safe GROUP teleport across places needs commit proof, destination-preserving retry, and bounded recovery.** `OnSessionEnd` fires before ProfileStore's ending write commits, and a generic `OnAfterSave` can belong to an overlapping save. Put a unique handoff nonce in `RobloxMetaData`, intentionally `EndSession`, then read through the store until the same persisted version has BOTH that nonce and `Session == nil`; only then call one group `TeleportAsync` while a replicated attribute freezes source movement. If `TeleportInitFailed` affects only some members of a reserved-party hop, retry the exact returned `TeleportOptions` so they target the original reservation. On exhaustion, re-acquire each profile concurrently with token/deadline cancellation plus a hard watchdog; never let one unbounded DataStore retry strand the group or publish a late session. | EatACake `PersistenceService.VerifyReleased`/bounded `LoadProfile`, `TeleportSubs.SendGroup`, `TeleportRetrySubs`, `Teleport/Recovery`, `TeleportControlSubsClient`, ADR-0010; implemented against verified vendored ProfileStore event ordering, with published cross-place verification still required; generic to any multi-place ProfileStore party teleport | pending |
| 2026-07-22 | fix | **Profile migrations must be transactional and fail closed.** Run the entire schema pipeline on a deep copy and commit only after every section succeeds. A migration that mutates then throws, a missing required step, or a throwing sanitizer must abort load and end the unchanged session; never continue, save the partial mutation, or stamp that version complete. | EatACake `PersistenceService.applySchema`/`migrateSection`; adversarial review reproduced the copied template's permanent half-migration path | pending |
| 2026-07-22 | fix | **Automatic-canvas list cells need explicit nonzero height seeds.** A row sized `(1, 0)` can remain zero even with `UIAspectRatioConstraint` inside an `AutomaticCanvasSize` scrolling frame, leaving a populated window blank. Store normalized section/row cell heights in the UI theme and pass them into each cell; keep the aspect constraint for proportional resizing. | EatACake `ShopPanel`/`ShopRow`; live lobby verification reproduced 15 populated rows with zero `AbsoluteSize`, then verified the explicit-height pattern used by Quests/Upgrades | pending |
| 2026-07-26 | feature | **Icon registry module** — `src/shared/UIKit/Icons.lua` (flat `name -> rbxassetid`) exposed as `Theme.Icons`, plus `Theme.Icon(name)` which warns ONCE on an unknown name and returns a visible fallback glyph instead of a blank ImageLabel. Keeps ~150 asset ids out of Theme.lua (R7) while components still take an icon NAME and never inline an id (iron rule 2). Includes the upload pipeline gotcha: the Studio MCP `upload_image` tool rejects local file paths (**http/https only** — serve the sprite folder with `python -m http.server`), and batches over ~15 time out | EatACake squishy re-theme; every template copy needs per-game art and today ships 5 placeholder ids + 8 copies of one menu placeholder | pending |
| 2026-07-26 | contract | **Squash/stretch as an opt-in extension of the `Interaction` primitive** — `Theme.Feel.Squish` poses + `usePressable{squash=...}` returning a third `squashRef`, `pressLayer(..., squashRef)`. Rides `Content.Size`, the one ADR-0006-safe carrier: React writes it once with the constant `Interaction.FullSize` then diffs it away. Load-bearing details: squash config read through a REF not the memo deps (else handlers rebuild every HUD re-render), and the disabled-reset path must restore `Size` or a button gated mid-press stays flattened | EatACake `Interaction.lua`; entirely game-agnostic, all existing 2-value callers unaffected. NOTE: panel-jelly and idle-breath variants were built and then REMOVED before shipping — they had no call sites, and the panel pose names shadowed the live `Feel.PanelOpenTween` that `PanelShell` reads. Harvest the press squash only; add the others with their callers | pending |
| 2026-07-26 | recipe | **Shop archetype upgrade: sectioned scroll with per-category GRIDS on a deterministic canvas.** `window-archetypes.md`'s worked Shop example (portrait, one column) is sized for ~8 items and structurally cannot show two cells side by side; past ~15 items in 4+ categories the genre uses hero banner + per-section grids. The load-bearing part is the canvas model: sum section heights in nominal px → `canvasHeightScale` → position every cell by explicit fraction. That removes `UIListLayout`, `AutomaticCanvasSize`, `UIGridLayout` AND aspect constraints from the scroll, so pitfalls 1/2/6 cannot occur. **Measured failure of the obvious alternative**: list + one aspect constraint per cell converges on a fixed point where the height binds — 377px rows inside a 596px window, every row kind the same height. Worth folding into `window-archetypes.md` + `patterns.md` | EatACake shop rebuild; the template's ShopPanel has the same one-column limit and the same `0.96 + 0.05 = 1.01` scrollbar sum bug | pending |
| 2026-07-26 | fix | **`Theme.ShopLayout` scrollbar sums do not close**: `ScrollWindowFraction 0.96 + ScrollBarWidth 0.05 = 1.01`, so the bar track overlaps the scroll window and there is no gap between them. Kit convention elsewhere is `870 + 12 + 22 = 904` (`PetsLayout`). Template still has the 1.01 | EatACake `Theme.lua` ShopLayout (pre-rewrite); copied verbatim from the template | pending |
| 2026-07-26 | fix | **`Theme.Rarity.Common` is not a rarity**: it ALIASES `Theme.Button`'s three gradients, so (a) a Common card is pixel-identical to every button/row/chip in the kit, and (b) it has different gradient *physics* from the other tiers (6-keypoint Rim starting DARK + 7-keypoint Face vs the canonical 4/4/5 with a bright Rim kp0). Fix = restructure to the canonical form and hue-shift to a desaturated neutral — but the neutral must be checked against `HexTree.locked`'s grey (H 215°, S 0.16) or the "common" tier reads as "locked". Warm cream (H 30°) clears both. Related: `Rarity` has no `Outline`/`Text` field, so per-tier outlines are duplicated across `EquipGreen`/`EatButton`/`HexTree`, and Secret has none at all (falls back to navy, violating style-rules §4.3) | EatACake `Theme.lua`; the alias and the missing fields are template code | pending |
| 2026-07-26 | contract | **`Theme.Accents` semantic layer** — `Rarity.Rare` IS `EquipGreen`, `.Epic` IS `EatButton`, `.Legendary` IS `HexTree.available`, `.Uncommon` IS `HexTree.back`, all by reference and all undocumented. Re-hueing any rarity tier silently re-skins the gym, the eat button, the upgrade tree and every confirm button. Introduce `Theme.Accents = {Confirm, Danger, Eat, Highlight, Info, Neutral}` pointing at the same sequences and retarget the ~12 consumers, freeing `Rarity` to move independently. NOT applied in EatACake: it is a refactor of working code with no player-visible change, so it belongs to a harvest pass with review, not a feature task | EatACake rarity work (the coupling is why only Common/Secret could be re-hued); pure template concern | pending |
| 2026-07-26 | docs | **Two UI gotchas worth a rule.** (1) `TextScaled` implies `TextWrapped`, so any label copy longer than its zone silently wraps to two lines and shrinks to unreadable instead of truncating — cap card/row copy at ~26 chars or give the zone two lines on purpose. (2) Art packs lie about their shape: the `Ribbon*.png` "ribbons" are square 257×257 rosettes, so a 4:1 tag box with `ScaleType.Fit` renders a centred blob. Check `Image.size` + `getbbox()` before designing a zone around supplied art, and prefer kit primitives for chrome | EatACake shop iteration 1-2; both cost a verify loop | pending |
| 2026-07-26 | docs | **`rojo build` does not parse Luau** — it packages files, so a module with a syntax error (e.g. a stray `return` left outside a function by a scripted edit) builds cleanly and fails only at `require` time, taking every dependent subscription down with it (`AppRoot: require FAILED` -> 9 subs `FAILED -> attempt to index nil with 'SetCallbacks'`). Any verification loop that treats a green `rojo build` as a syntax gate is unsound; the real gate is a play-mode boot report with zero `require FAILED` lines — which the template's R8 bootstrap logging already prints for free | EatACake squishy re-theme: a scripted removal of a dead helper sliced to the wrong `return`, passed 3 builds, broke the whole client UI | pending |
| 2026-07-26 | fix | **`ScaleType.Fit` icon sizing trap**: a square glyph in a short-and-wide icon zone draws at `min(zoneW, zoneH)`, so the width is thrown away. `Theme.HudMenuButton.IconSize = (1, 86/118)` on a roughly square button therefore capped every menu glyph at 73% of the button height — measured 35px drawn in a 48px cell. Fix = near-square icon zones + treat `IconInset` as the drawn size (0.16 -> 0.06 took plate fill from 68% to 88%). Also: the template's `HudMenuButton` nominal (100x118) and `AppHud.MenuButtonWidth/Height` (92/1920, 100/1080) are both too small to read on a phone; 124x132 with a 100x126 button grid is the corrected cut. Verify by measuring `min(icon.AbsoluteSize.X, icon.AbsoluteSize.Y)`, never by eye | EatACake sizing pass after user feedback "icons everywhere too small"; the zones and insets are all template values | pending |
| 2026-07-26 | fix | **`Theme.RewardsLayout` reserves a grid zone 2.7x taller than its content**: 7 columns of 118x135 `DayCard`s in a 904x360 zone = one row filling 37% of the height, i.e. 62% dead space in a full-size landscape panel. The insight worth keeping: **N columns across a fixed canvas caps the card WIDTH no matter how much vertical room exists**, so "big panel, tiny buttons" is never fixed by scaling — it needs fewer columns over more rows. Corrected cut: 4 cols x 2 rows, card re-cut LANDSCAPE 214x172 (`4*214 + 3*16 = 904`, `2*172 + 16 = 360`), reward band = art + amount instead of text alone. Caveat to document: `UIGridLayout` left-aligns a partial last row and cannot centre it, so pick a column count that divides the common card counts | EatACake rewards panel; `RewardsLayout` + `DayCard` are template code with the same numbers | pending |
| 2026-07-26 | contract | **Validate icon-registry references at load.** `AppHud.MenuIcons.X = Icons.SomeRetiredName` resolves to nil, the button falls back to the generic placeholder, and the result looks like a deliberate design choice in-game while being invisible in code review — it survived a full verify pass and was only caught in a screenshot. Add a load-time loop that `Log.Warn`s any menu-icon value that does not resolve (R8). Generalises to any name -> asset map that has a silent fallback | EatACake: swapping the squishy art pack retired two icon names and broke the collection button | pending |
| 2026-07-26 | feature | **Audio layer: place-authored samples + a code-owned key map.** `AudioConfig.sounds[key] = { asset = "<Sound instance name>", volume, pitch, cut, throttle }` over Sound instances the developer authors in `ReplicatedStorage.SFX` / `SoundService.BackgroundMusic` (same contract shape as ADR-0007 scene assets: resolved BY NAME, R8 on missing/duplicate, bank rebuilt on a miss so a late library heals). `volume`/`pitch` are MULTIPLIERS on the authored values so Studio retuning still works. Two shaping fields carry the whole design: **`cut`** (stop after N s) makes 1-2.5 s library samples usable as high-rate cues (a 2.24 s splat becomes a 0.3 s bite) — needs a per-voice GENERATION STAMP so a recycled pooled voice is not cut short by the previous cue's timer; **`throttle`** (min seconds between plays of a key) tames cues driven by high-frequency events. Plus `MusicService`: shuffled bag (first of a new bag != last played), fade in/out + gap, driven by a per-frame `Step` (no `.Connect` in a service, R4 — and the fade needs the frame anyway) | EatACake `shared/config/AudioConfig`, `client/common/modules/{SoundPool,MusicService}`, `AudioSubsClient` (`2026-07-26_audio-sfx-music.md`); template ships `JuiceConfig.sounds` with rbxasset placeholders and NO music at all | pending |
| 2026-07-26 | contract | **One injected hook makes the whole kit click.** `Interaction.SetSoundHandler(fn)` (re-exported as `UIKit.SetSoundHandler`) + `cue("press")` on the AGGREGATE press edge and `cue("hover")` on MouseEnter inside `usePressable` — every pressable component in the kit gains press/hover sound at once, with no per-component prop and no shared->client dependency (shared code must never require a client module, so the client INJECTS the handler; unset = a silent, still-correct kit). Aggregate edge means multi-touch and drag-off cannot double-click; a disabled button has no handlers so it is correctly silent. Consequence to check when adopting: DELETE any manual per-tap sound at a call site whose button is a kit pressable, or it doubles | EatACake `UIKit/Interaction` SOUND section + `AudioSubsClient`; rides the ADR-0006 press primitive that is already upstream-queued (row 2026-07-19 "Kit-wide UI juice primitive") | pending |
| 2026-07-26 | contract | **Panel open/close sound belongs to the composed root's `openPanel`, not to close buttons.** One `React.useEffect` on `openPanel` firing `callbacks.onPanelChanged(panel|nil)` — a newly added panel cannot forget to fire it, and open/close/switch all resolve correctly. Two gotchas: `primed` a ref so the effect's FIRST run (mount with nothing open) is not reported as a close, and the dep must be `openPanel or false` (jsdotlua nil-in-deps footgun) | EatACake `AppRoot` + `AudioSubsClient`; template `AppRoot` has the same `openPanel` shape | pending |
| 2026-07-26 | contract | **A settings toggle with no effect branch must WARN (R8).** The template's `SettingsSubsClient.applySetting` is a documented "fill this in per game" hook that only `Log.Info`s — so `music-enabled`/`sfx-enabled` persist, replicate, render, and do nothing, and nothing in the console says so. Make the hook a branch-per-id with a `Log.Once` fallback for unhandled ids: a setting that persists but changes nothing is a half-built feature, not a no-op. Reference wiring: mute via ONE SoundGroup volume per group (covers pooled voices, dedicated clones and loops); music additionally STOPS playback (no point streaming an inaudible track) | EatACake `SettingsSubsClient` + `SoundPool.SetEnabled`/`MusicService.SetEnabled` | pending |
| 2026-07-26 | recipe | **First-note gate for background music.** Starting music at spawn and muting it a second later when the persisted `music-enabled` lands is the bug a music-off player reports. Hold playback until the first `SetEnabled` call (the settings snapshot releases the gate), with a grace timer that starts on the default AND `Log.Once`-warns if the settings push never arrives — so a broken settings path degrades to "music plays" plus a console signal, never to permanent silence. Generalises to any saved preference that gates an immediately-perceptible effect at join | EatACake `MusicService` (`AudioConfig.music.settingsGraceSeconds`) | pending |
| 2026-07-26 | docs | Audio design rules worth carrying: (a) a cue on a per-tick/per-bite event needs `cut` AND usually `throttle`, or high action rates turn the voice pool into mush; (b) decide explicitly which events get NO cue and write it down — currency that ticks every action (calories) drones, a rare currency (gems) does not; (c) the voice pool must outlast the longest `cut` or raising `cut` silently starts truncating live cues; (d) provenance is the only objective quality signal available to an agent that cannot listen — prefer Roblox's free **Pro Sound Effects** library (ids ~91xxxxxxxx, professional recordings, descriptive catalog text like "Constant, Squishing, Mushy, Wet") over generic Toolbox uploads, and verify each pick with `TimeLength` after inserting | EatACake audio pass (`2026-07-26_audio-sfx-music.md`) | pending |
| 2026-07-26 | docs | **"Every kit button clicks" is not true until you AUDIT it — a shared press primitive only reaches components that opted in.** Hooking `usePressable` gives sound to every component that uses it and silently misses the rest. Two classes get missed: (a) components that own their own motion and deliberately skip the primitive (a Toggle's knob slide, a card's selected pose), (b) surfaces where the "buttons" are HIT-TESTED through one input surface rather than rendered as buttons (a pan/zoom graph resolving the nearest node) — that one hid an entire screen's silence. Audit mechanically: for every Components/*.lua that builds a Text/ImageButton, assert it calls either `usePressable` or the manual `Interaction.Cue(name)`; then enumerate the live GuiButtons in the mounted tree and group them by owning component so nothing is missed by static reading alone. Diagnostic that saves an hour: a silent button is usually a DISABLED button (`Active = false` because its feature is unconfigured/unaffordable) — check `Active` on the instance before hunting a missing cue, and remember the whole layer can be muted by a persisted settings toggle. | EatACake audio pass: `Interaction.Cue` added for Toggle/PetCard/DayCard/reveal-dismiss, then a second audit of all 94 live clickables caught the hex-tree nodes (`2026-07-26_audio-sfx-music.md`) | pending |
| 2026-07-29 | fix | **In a two-place template, a per-server timer is a LEG, not a session.** `AnalyticsSubs` logged `session_minutes` on `PlayerRemoving` — but with a lobby+game split (ADR-0009) every teleport ends the player on this server and starts a fresh one, so a 30-minute session reports as "3 + 26" and the headline engagement number is silently halved. Fix is naming, not plumbing: emit `place_minutes_<lobby|game>` (the game leg IS the engagement number; lobby time is queue overhead, and lobby rising while game does not = matchmaking is the leak). True cross-place session length is already free from Roblox's built-in engagement metric — rebuilding it needs the join anchor carried through `TeleportData` and is still less accurate. Generalises to EVERY per-player timer/counter in a multi-place template: ask "does a teleport reset this?" before naming it. | EatACake `AnalyticsSubs` + `features/analytics.md`; the template's analytics candidate above has the same defect | pending |
| 2026-07-29 | fix | **`os.clock()` is documented as CPU time used by Lua — do not use it for elapsed wall time.** It is the idiomatic-looking choice for "how long was this player here" and reads as monotonic, but on a mostly-idle server it can run behind wall time and under-report. Use `os.time()` for anything reported in minutes (whole seconds is ample) and reserve `os.clock()` for benchmarking a synchronous block. Worth a line in the code-style section: the template currently has no guidance and both appear in the wild. | EatACake `AnalyticsSubs.joinedAt` | pending |
| 2026-07-29 | docs | **A constant named for its STORAGE FORMAT will be read as if it were named for its subject.** `CakeConfig.grid.maxHeight` is the u16 field HEADROOM (340) with no relation to how tall a cake is, and `MapService` used `grid.maxHeight * 0.6` as a stand-in for the cake top. That was harmless until an unrelated pass lowered `room.wallHeight` — the spawn pad then sat at 214 with a 210-stud ceiling, i.e. **arrivals spawned on top of a sealed room**. Two rules: (a) name capacity//headroom constants so they cannot be mistaken for measurements (`fieldHeadroom`, not `maxHeight`), and (b) a derived position must be derived from the thing it tracks (`coreThickness + maxTotalHeight`), never from a fraction of an unrelated constant that merely happens to be in the right range. The magic-fraction form is the tell — `X * 0.6` where 0.6 has no meaning. | EatACake `MapService.SetCheckpointHeight` / `CakeSpawn`; `grid.maxHeight` is template code | pending |
| 2026-07-29 | recipe | **Stubs in a test harness must not be LOUDER than the real thing.** The headless harness stubbed `Log.Once` as "print every call", so 5 genuine warnings rendered as 36 lines that looked exactly like an R8 violation — the harness manufactured a bug report about itself. Any stub of a rate-limited/deduped/throttled API must reproduce the limiting, not just the output. Same class as the existing `GetBoundingBox` gotcha (reproduce real semantics, not convenient ones). Related harness papercut worth copying the fix for: when a builder script takes `<seed> <mode>`, an unrecognised mode that falls through to an `else` branch runs a DIFFERENT case while still printing PASS — make unknown modes an error, or document the arg loudly. | EatACake `tools/headless-sim/harness_head.lua` + `README.md` | pending |
| 2026-07-29 | docs | **A live playtest proves nothing until you prove the code under test is your code.** Rojo sat on an UNACCEPTED "Sync changes" confirmation, so Studio ran code older than disk while the plugin said "Connected", the Output looked healthy and the playtest ran fine. Caught only because a boot log line I had just written never appeared — otherwise I would have published "verified in Studio" for code that never executed. Make a sync assertion part of the verify ritual: before trusting a playtest, probe a marker string unique to the current change. ⚠ Probe `.Source`, NOT `require()` — `require` caches per Studio session, so a cached stale table is indistinguishable from an unsynced module (that cost a second wrong reading). Belongs in the `studio-verifier` agent's checklist as step 0. | EatACake pass 19; `studio-verifier` + `docs/features/*` verification sections are template-wide | pending |
| 2026-07-29 | recipe | **Studio↔agent readout via a localhost HTTP bridge beats screenshotting the Output window.** Tiny Python server: GET serves a file (push source INTO Studio), POST writes the body to disk (structured reports OUT). Studio's `HttpService` reaches localhost fine. The command bar runs `local m=Instance.new("ModuleScript") m.Source=HttpService:GetAsync(url) m.Parent=workspace require(m) m:Destroy()` — arbitrarily long scripts with no typing, no OCR, no truncation by scrollback. Three gotchas worth carrying: (a) Studio gates command-bar HttpService behind a "Dangerous Command Detected" prompt — click Continue, NEVER "Always Continue" (that permanently disables the user's safety prompt); (b) the command bar's autocomplete swallows Return, so run long lines with the Run button, not Enter; (c) a mis-aimed click lands in the script EDITOR and types into real source — `rojo serve` is one-way so disk survives, but verify with `git diff`. | EatACake `scratchpad/bridge.py` + pass 19; generic to any Studio-automation task where the Studio MCP exposes no tools | pending |
| 2026-07-29 | docs | **`InsertService:LoadAsset` reaches a strictly smaller set than the Toolbox.** 16/16 candidates from the public `toolbox-service/v1/marketplace` search failed with "User is not authorized to access Asset", across both old and new asset-id formats — so the search API returns plenty of assets only the AUTHENTICATED Toolbox insert path can fetch, and id era is not the discriminator. Downgrade the earlier "scripted Toolbox insertion" recipe from "the way" to "worth one attempt, then hand it to a human". Public search is still useful for FINDING candidates; it just cannot predict insertability. | EatACake pass 19; supersedes the optimistic framing of the 2026-07-26 `InsertService:LoadAsset` row above | pending |
| 2026-07-29 | feature | **Make rarity legible through TREATMENT, not through distinct art.** A game that needs N distinct meshes to communicate N tiers is blocked on art forever. Three art-independent cues, all config-driven: (1) `sceneryModels` — library entries that stay pin-resolvable but leave the round-robin pool, so a legendary can never dig up as a floor tile; (2) `rarityScale` — rarer = physically bigger, the only cue readable while the item is still buried; (3) `glowPulse` per rarity, ZERO for common on purpose (with 40 items on screen a pulse on everything is wallpaper — only the ones worth crossing the map for should breathe). ⚠ Hard-won constraint: **never scale a tier BELOW 1.0.** Centring the band on 1.0 shrank commons and moved one-layer finds 13/40 → 15/40, i.e. it traded away an explicit design requirement to buy a cue. Scale only upward. | EatACake `TreasureConfig.model.{sceneryModels,rarityScale}` + `rarityFx[].glowPulse`, `TreasureService`; the template's treasure/collectible layer has the same round-robin fallback | pending |
| 2026-07-29 | docs | **Config that no code reads is worse than a missing feature — it reads as done.** `highlightPulseHz` sat in `TreasureConfig` from the day the rework shipped, fully documented, and NOTHING referenced it; every reader (including me, twice) assumed the pulse existed. Cheap mechanical guard for any template with large config tables: a test/CI pass that greps each config leaf key for at least one non-config reference and reports the orphans. Same class as the icon-registry row above (a silent fallback that looks like a design choice), but worse — there is not even a fallback, just nothing. | EatACake `TreasureConfig.model.highlightPulseHz` (dead since the 2026-07-26 rework, wired in pass 19) | pending |
| 2026-07-29 | fix | **Never animate a Model's POSE per-tick unless it has a PrimaryPart — the transform compounds.** `Model:GetPivot()` on a PrimaryPart-less model returns `CFrame.new(<recomputed world-AABB centre>)`, i.e. the CENTRE with **identity rotation**. So `PivotTo` derives each part's relative offset from a frame that has already lost the previous rotation, then re-applies the new one on top: repeated calls accumulate and the model walks away from where it was placed. A one-shot `PivotTo` (a pop, a flight) is fine; a per-frame oscillation is not. Fixes: set a PrimaryPart, cache the parts' relative CFrames ONCE and write them directly, or animate a PROPERTY instead of a pose. Caught by a scenario going `40/40` → `34/40` collected — a code review would not have seen it. | EatACake `TreasureService` strain beat, pass 20; the template's clone-and-place helpers have the same shape | pending |
| 2026-07-29 | docs | **"The simulation can't be perturbed" and "the visuals can't be broken" are different claims.** Before adding a per-tick visual to the buried-find loop I verified the reveal/free logic reads only CACHED spawn values (`radiusCells`/`x`/`z`/`topUnits`), never live geometry — correctly concluding the sim was safe — and then shipped a change that broke where the models ENDED UP. The check I ran did not cover the thing that broke. Rule: when adding a visual to a simulated entity, enumerate BOTH what reads the entity's state and what reads its transform, and say which one you verified. | EatACake pass 20 | pending |
| 2026-07-29 | docs | **When a tool "stops working", suspect your own filters before the tool.** Roblox Studio's command bar appeared dead for ~8 turns. It was working and reporting the reason the whole time: `Http requests can only be executed by game server` (the bar defaults to the CLIENT context during a playtest, and clients cannot make HTTP requests) — but an Output FILTER I had set three steps earlier hid exactly that line. Compounded by a third trap: the Run button moves down as the bar grows a line, so a click at the remembered coordinate silently no-ops. Generic debugging order for any log-driven tool: **clear the filters first**, then check execution context, then re-read UI coordinates from a fresh screenshot, and only then suspect the tool. Corollary for agents driving GUIs: a filter you set is state you now have to remember you set. | EatACake pass 20, `tools/studio-bridge/README.md` | pending |
| 2026-07-29 | feature | **Ship a dev hook that forces the slow precondition of any long-loop mechanic.** EatACake's buried finds take MINUTES of real eating to surface, which made every find-related visual structurally unverifiable — two passes ended in "sim-verified, visually unconfirmed" before I stopped retrying the observation and built the instrument. `DebugUncoverNearest(position, leaveFraction)` carves the world state over the nearest pending entity and lets the NORMAL tick walk it through its real state machine (it must NOT set states directly — bypassing the path under test verifies nothing); re-firing with a smaller value escalates, which turns "does the effect ramp?" into a table of numbers. Two design notes that generalise: drive it from an **attribute the running server watches**, not a direct service call (Studio's command bar keeps its own require cache even in play mode, so `require(Service)` there returns a fresh module with empty state), and gate on `RunService:IsStudio()`. It paid for itself in under a minute by exposing a bug nothing else could see. | EatACake `TreasureService.DebugUncoverNearest` + the Studio hook in `CakeSimulationSubs` | pending |
| 2026-07-29 | fix | **Don't nest a universal cue inside a rarity gate.** EatACake's "about to come loose" strain animation was written inside `if glowPulse > 0`, and `glowPulse` is deliberately 0 for common items — so the anticipation cue was silently absent on **60 of 98 roll weight** (the two commonest finds), i.e. most of what a player actually digs up. Compiler, headless sim and code review all passed it; only putting a common item on screen revealed it. General rule: separate the signal that says WHAT something is (rarity: colour, pulse, loudness) from the signal that says WHAT IS HAPPENING to it (progress, imminence, state) — the second must fire for every instance, or the majority case gets no feedback at all. | EatACake `TreasureService` strain block; the template's rarity/FX tables invite the same nesting | pending |
| 2026-07-29 | feature | **Depth (or distance, or tier) must PAY, or there is no pull deeper.** Studying the reference games for an ASMR/incremental title, the single mechanic *Drain the Lake* is most praised for is that the same action is worth more the further down you go — "a bucket that earned a trickle near the surface can be worth far more at the bottom… the rhythm becomes a satisfying spiral". EatACake had the opposite: the cake got harder with depth and paid identically, because the reward roll took no depth argument at all. Fix is tiny and general — multiply each rarity tier's roll weight by `(1 + bias * progress)^tier` so `progress = 0` reproduces the flat distribution EXACTLY (the opening minutes stay tuned) and only the far end skews. Measured at bias 0.6: rare-or-better 14.3% → 33.7%, legendary 2.0% → 8.8%, nothing guaranteed. Template rule for any collectible layer: if the reward table is independent of how far in the player is, the back half of the content has no draw. | EatACake `TreasureConfig.spawn.depthRarityBias` + `TreasureService.weightedFind(depthFrac)` | pending |
| 2026-07-29 | docs | **Check a competitor finding against your own code BEFORE building it.** A study of three reference games produced two actionable patterns; one (per-material bite sound — *ASMR Tower*'s whole identity is "every platform type produces a unique satisfying sound") was ALREADY fully implemented in this project, and one (depth pays) was entirely absent. Verifying which was which cost two greps; assuming either way costs a duplicated system or a permanently missing one. Make "grep for it first" an explicit step in any competitor-study or research task, and record the ALREADY-DONE findings in the flow doc too — otherwise the next pass re-derives and re-builds them. | EatACake pass 22 competitor study | pending |
| 2026-07-29 | docs | **An audit that inspects containers at inconsistent DEPTHS invents work that does not exist.** My scene-inventory script walked `GetDescendants()` for one folder and `GetChildren()` for another, so every node whose art hangs one level down read as a bare primitive. That produced TWO confident false to-dos in the same session — "re-model the room" (it is 460 authored parts) and "re-model the checkpoint" (its `GymMachine` is a `Transparency = 1.0` collider wrapping a 25-part treadmill) — both reported to the user before being checked. Worse, the invisible-collider-wrapping-authored-art pattern is the CORRECT contract, so a shallow probe reads correct architecture as missing work. Rules: use one traversal depth for the whole audit, print `Transparency` and descendant counts for anything you are about to call "a bare box", and never promote an audit result to a to-do without opening the node. | EatACake passes 19+23 scene inventory | pending |
| 2026-07-30 | contract | **A lifecycle that only exposes "now push to the client" cannot express "mutate before anyone is told".** `PlayerLifecycleSubs` discovered exactly one hook, `PushInitialState`, fired after load AND `ClientReady` — and those hooks run in ALPHABETICAL order, so a sub that needed to reset profile state on join (EatACake's run-scoped upgrade wipe) had no safe seat: from its own push hook it would race `EconomySubs`/`UpgradeSubs`, which sort earlier, and the client would keep the stale values forever with no correction. Added a second discovered hook, `OnProfileLoaded(player)`, fired right after `LoadProfile` and BEFORE the flag that opens the push gate. The elegant part is what it removes: because it lands first, every domain's existing `PushInitialState` then sends already-mutated state, so join-time mutation needs zero re-push code anywhere. Generic to any template feature that conditions a profile at join (seasonal resets, migrations-on-load, first-session grants, per-run scoping). Port the `task.spawn` → `task.defer` fix for the already-present-players sweep with it: this module sorts before several subs whose hooks it discovers, and a spawned body runs immediately, so it could reach a hook before its owner armed. | EatACake `PlayerLifecycleSubs` + `RunResetSubs` (ADR-0013) | pending |
| 2026-07-30 | fix | **A reward you can only show AFTER granting it cannot be advertised — split the roll.** EatACake's boss fight is the climax of a 40-minute run and was a blind tap race: `PetService.Roll` decided AND granted in one call, so there was no way to display the prize you were fighting for. Split into pure `Preview` (decides, mutates nothing) + `Grant(id)` (commits), with `Roll = Preview + Grant` so every existing caller is untouched — then pre-roll at fight start, show it, and commit that exact id on a win. Two non-obvious requirements: any rarity FLOOR (rainbow-cake "guaranteed Epic+") must be applied at PREVIEW time or the advertised prize can disagree with the granted one, and the pending intent belongs in RUNTIME state, not the profile, because it is not owned yet. The template's reward/egg services have the same fused shape, and "show the stake before the fight" applies to every boss/timer/gamble mechanic. | EatACake `PetService.Preview`/`Grant`, `CakeCycleSubs.BeginBoss`, `CakeStateData.pendingPetRolls` | pending |
| 2026-07-30 | fix | **An inset root ScreenGui makes every "modal" scrim a lie — and the fix has a second half.** `UiRoot` shipped `ScreenInsets = CoreUISafeInsets`, which shrinks the WHOLE React tree below Roblox's ~36 px topbar, so the hex-tree dim, panel shells and overlay scrims all stopped short of the top of the screen and the world stayed bright in that strip. Correct shape: root gui FULL-BLEED (`DeviceSafeInsets` keeps notch safety) plus a `Hud` child frame offset by `GuiService:GetGuiInset()`, which reproduces the old coordinate space EXACTLY so no HUD element moves. The second half is the trap: any code mixing pointer coords with `AbsolutePosition` silently assumes an inset root — `ScrollPane` did `GetMouseLocation() - GetGuiInset()` and would have been off by the topbar height. Template rule: never write that subtraction; read `InputObject.Position` off the event, which is in `AbsolutePosition` space in EITHER mode (and picks up touch, which `MouseButton1Down` never fires for). Grep `GetGuiInset` before flipping a root gui. | EatACake `UiRoot`, `AppRoot` `Hud` layer, `ScrollPane` | pending |
| 2026-07-30 | recipe | **A balance model that holds the player's stats FIXED cannot measure a game with mid-run upgrades.** EatACake's pacing harness measured the upgrade curve's two ENDPOINTS (fresh eater, maxed eater) and the documented target — "solo easy 40 min" — was taken from neither: it came from a model where nobody bought anything. The run people play is the ramp between them, and measured properly it was **54.6 min owning 21 of 44 tiers**, matching a playtest that reported 1 h 01 m against that 40. Rule: if a game has in-run progression, the model must simulate EARNING and SPENDING, and the reported number must be that run — endpoints are diagnostics, not the target. Two knobs to expose because they are coupled and easy to tune one at a time by mistake: cost scale (when the tree completes, as a % of content) and work/content scale (total clear). Also: publish the purchase POLICY (this one buys cheapest-first, which maximises tier count) — the answer depends on it. | EatACake `tools/balance-model/pacing.py`, ADR-0013 | pending |
| 2026-07-30 | fix | **A units error inside a measurement tool is worse than no tool: every number it published was wrong and confidently sourced.** `pacing_scenario.lua` multiplied `CakeOps.ApplyBite`'s return — which already includes `* cell * cell` — by `cellArea` a SECOND time. Food read 2.25× high, so belly→gym TRIPS read 2.25× high ("one cake = 126 min fresh" was really ~94), and the forfeited fraction read 2.25× LOW (6.8% published vs ~17% actual) because the sibling `forfeited` line correctly applied `cellArea` once to a raw unit count. Those figures had been copied into a feature doc and an ADR. Found only by porting the same math to a second implementation and comparing. Template rules: a function that returns a VOLUME should say so in its name or type, harness code must never re-apply a scale factor its callee already applied, and when a harness and an independent implementation disagree by a suspiciously round factor (2.25 = 1.5², i.e. `cell²`), suspect units before either model. | EatACake `tools/headless-sim/pacing_scenario.lua`; the template's harness has the same call shape | pending |
| 2026-07-30 | docs | **Check which PLACE a button is in, not just that it exists.** EatACake's upgrade tree was reported "reachable in BOTH places" and documented that way, but `AppRoot`'s menu frame is `Visible = showLobby` — so the HUD button existed only in the LOBBY, i.e. the one place with nothing to spend, and was absent from the match the whole pacing is built around. A previous pass had moved the service, the subs and the UI data to `common/` and correctly concluded the FEATURE worked in both places; the button's own visibility gate was never re-read. Rule for a partitioned codebase: "moved to common" is a claim about the module, not about the entry point — verify each entry point's own place gate, and state which place each one renders in. | EatACake `AppRoot` menu / `features/upgrades.md` | pending |
| 2026-07-30 | contract | **A "reset progression" feature needs an explicit META vs RUN inventory, checked against every producer.** Before wiping EatACake's calorie balance per run I had to confirm nothing valuable rode on it: the leaderboard and the `burn-calories` quest read `progress.lifetimeCalories` — a DIFFERENT field, one grep away from being wrongly reset — and no live reward grants raw calories (daily/time/codes/finds all pay gems, eggs or boosts) while the shop sells calorie MULTIPLIERS, not balances. Any of those being otherwise would have made the reset destroy earned or PAID-FOR value. Template rule: a reset feature ships with a two-column table of every profile field (resets / survives) plus the greps that justify each row, because the dangerous case is a field whose name merely resembles a permanent stat. | EatACake ADR-0013, `features/economy.md` | pending |
| 2026-07-30 | docs | **When a doc says a feature is disabled/missing, grep the code before building a replacement.** `features/upgrades.md` said the HUD button was "the guaranteed entry point" and added "(and re-enabling the game checkpoint computer's prompt)" — which reads as if that ProximityPrompt were disabled. It was not: `MapService.buildCheckpoint` constructs it enabled with `HoldDuration = 0` and the client has always listened for it. On the strength of that one parenthetical I built a second, redundant entry point into the same overlay, which the user then had to remove. Two greps (`grep -rn UpgradeStation src/`) would have settled it. The general trap is that a doc's ⚠ notes age badly precisely BECAUSE they describe transient states ("pending", "needs re-enabling", "not wired yet") — so a stale one is maximally misleading. Template rules: (a) any doc line asserting something is absent/disabled carries the grep that proves it, or it does not go in; (b) before ADDING an entry point to an existing feature, enumerate the existing ones from CODE. Note this is the same failure as the sibling row about place gates, one level up: I verified WHERE the button was and never asked WHETHER one was needed. | EatACake `features/upgrades.md` / `MapService.buildCheckpoint` / `AppRoot` | pending |
| 2026-07-30 | fix | **`ProcessReceipt` must live in EVERY place, or a receipt that surfaces in the wrong one is dropped silently.** EatACake armed it in the lobby only, because that is where the shop WINDOW is - but a receipt is not a UI event: Roblox re-delivers until it sees `PurchaseGranted`, the in-experience Store can complete a purchase in any server, and a purchase finished across a teleport surfaces on the destination. In the other place there was no handler at all, so those receipts vanished with no console trace (nothing stolen - Roblox retries forever - but the player saw nothing and the boot report said nothing). Two rules that generalise to any multi-place template: (a) money handlers are COMMON, full stop; (b) a grant kind only ONE partition registers must be DEFERRED with `NotProcessedYet`, not failed, so the retry delivers it where the handler exists - and boot must log which kinds are affected in the current place. The trap that hides all of this: a combined dev project maps every partition flat, so the missing handler is invisible in Studio. | EatACake ADR-0014, `ShopSubs`/`ShopService` moved lobby -> common | pending |
| 2026-07-30 | fix | **A consumable purchase needs a PurchaseId ledger - `oneTime` flags do not cover it.** Roblox hands the SAME `receiptInfo.PurchaseId` to the next server if the granting one dies before returning `PurchaseGranted`. EatACake's one-time products were protected by their own owned-flag, so the hole was invisible; every gem pack, egg and boost double-granted on one payment. Fix is small and belongs in the template's shop: a bounded ARRAY of recent PurchaseIds in the profile section (array, not a set - bounding needs an order), written in the SAME no-yield stretch as the grant and BEFORE the save, with the tuning depth in the data module and a separate, larger corruption ceiling in the section's `sanitize`. Adding the field needs no version bump (reconcile fills a default). | EatACake `ShopSection.receipts`, `ShopService.IsReceiptHandled/MarkReceiptHandled`, ADR-0014 | pending |
| 2026-07-30 | fix | **Never grant a paid reward while a session release is in flight - the save is a no-op and the receipt is consumed.** `PersistenceService.Save` deliberately does nothing while a teleport release nonce is set (ADR-0009's handoff), so a receipt arriving in that window returned `PurchaseGranted`, took the Robux and released the session with the grant unsaved: real money, zero delivery, receipt permanently spent. The receipt path must consult the SAME state the save guard consults. Generic rule for any template with a save-suppressing state (handoff, shutdown drain, maintenance): enumerate every writer that assumes its write will persist and make each one check the suppressor - a guard that only lives inside `Save` protects the data and silently loses the transaction. | EatACake `ShopSubs.processReceipt` vs `PersistenceService.Save`'s `releaseNonces` guard | pending |
| 2026-07-30 | fix | **A validation guard for a kind the game does not have is not a guard.** EatACake's receipt validator tested `reward.kind == "gold"` - a kind from the ORIGINAL template that this game never adopted - so `{ kind = "gems", amount = 0 }` passed, both handlers returned nil, and the grant loop still reported success: `PurchaseGranted` for nothing. Replace the single hardcoded special case with a per-kind `descriptorValid` whose ELSE branch REJECTS unknown kinds, so adding a kind to the catalogue without deciding what valid means for it fails loudly instead of being waved through. This is the copy-a-template-and-drift failure mode in its purest form: the guard kept compiling and kept looking deliberate. | EatACake `ShopSubs.descriptorValid`; the template's shop ships the same `gold` check | pending |
| 2026-07-30 | fix | **A gamepass bought MID-SESSION must re-apply its perk attributes, not just flip the shop cell.** EatACake set the runtime ownership flag on `PromptGamePassPurchaseFinished` and re-pushed the catalogue - so the card said OWNED - while the client gates auto-eat/auto-gym on player ATTRIBUTES that only the join-time ownership sub writes. A 399 R$ pass therefore did nothing until the next place transition. Fix: export the attribute-application half of the ownership sub (`ApplyPerkAttributes`, re-derives from the cache with no new web call) and call it from the purchase handler. Template rule: every perk has TWO representations (the cache the server reads and the attribute the client reads); a purchase path that updates one is a half-delivered product. | EatACake `PassOwnershipSubs.ApplyPerkAttributes` + `ShopSubs` | pending |
| 2026-07-30 | recipe | **Roblox Open Cloud can now CREATE dev products and game passes - and the icon the dashboard demands is optional there.** Since 2025-12-04: `POST https://apis.roblox.com/developer-products/v2/universes/{u}/developer-products` and `POST https://apis.roblox.com/game-passes/v1/universes/{u}/game-passes`, `multipart/form-data`, only `name` required (`description`, `price`, `isForSale`, `imageFile` optional), x-api-key auth, 3/s and 5/s. That turns "15 dashboard forms each needing artwork" into one idempotent script, which is exactly the chore that keeps a template copy's monetization at id = 0. Four facts to carry: the `/cloud/`-prefixed paths some docs show are 404 (verified live; the un-prefixed ones 403 without auth, proving the route); there is NO DELETE endpoint, so a script must be dry-run by default and match existing items by name; a game pass 404s on the web until the experience is published (working as intended, not moderation); and cross-experience sales were disabled 2026-05-29, so the universe id must be verified first via `GET /universes/v1/places/{placeId}/universe`. | EatACake `tools/monetization/`; every new game from the template needs this on day one | **superseded 2026-07-31 — harvest the corrected rows below instead.** Two claims in this row were falsified by actually running it: (a) auth is a `.ROBLOSECURITY` cookie + XSRF handshake, NOT `x-api-key` — these are not `/cloud/` routes, so an API key is not the credential they accept; (b) there is no `imageFile` part and no dev-product icon endpoint AT ALL. Also the count is 11, not 15, and `price` is only literal Robux with `isRegionalPricingEnabled=false`. |
| 2026-07-30 | fix | **A per-frame follower loop belongs to the DOMAIN's subscription, not to whichever sub happened to already have a RenderStepped.** EatACake's equipped-pet followers were stepped from the body/gym sub, which early-returns without the game partition - so pets silently never appeared in the LOBBY, the place where players spend most of their idle time and where the collection UI lives. Nothing errored; `Init` still ran and still created the (empty) workspace folder, which is exactly what made it look wired. Two rules: (a) put the step in the sub that owns the domain, and log an armed line so the console can answer "did it subscribe?"; (b) when moving it, DELETE the old call site - a combined dev project maps both partitions, so two callers double every clock the loop advances (bob rate, phase) with no error. | EatACake `PetsSubsClient` / `BodySubsClient` / `PetFollowers` | pending |
| 2026-07-30 | fix | **Do not weld an arbitrary authored model into a rigid assembly - `PivotTo` the model instead.** Prepping pet followers, the textbook optimisation (unanchor every part, `WeldConstraint` them to one anchored root, then write ONE CFrame per frame) failed on models that nest parts inside other PARTS (`Planet Texture` under `Planet Texture`, `Torus` under `planete`): the welds reported `Active = true` and the meshes still sat at their authored map coordinates while only the root flew. Visible as one follower stranded hundreds of studs away while its siblings behaved. At 1-6 parts per model `Model:PivotTo` costs nothing and cannot break. Reserve welding for models whose structure you authored. Related trap in the same prep step: scale BEFORE measuring any pivot offset, and strip animation-editor leftovers (`AnimSaves`, `InitialPoses`, `AnimationController`, `Motor6D`) or they fight the transform. | EatACake `PetFollowers.prepare` | pending |
| 2026-07-30 | feature | **A shop cell built from the BUTTON recipe reads as a button, not as a product - repurpose the RIM as a white border and let the FACE carry the colour.** EatACake's shop tiles and pack cards pointed Outer/Rim/Face at `Theme.Button.*`, so every product rendered as the same blue button with a price glued on; the user's complaint was exactly "the item frames look bad because they use a button-style design". The fix keeps the kit's three-layer recipe intact and changes what the layers MEAN: Rim becomes the white inner border the genre uses, which frees Face to carry a strong per-item colour, and the content restacks to title-top / big art / one perk line / green price button along the bottom. Zero new palette - every accent is an existing kit set. Two structural notes: the per-item colour is DATA in the catalogue module (which product is which colour is a catalogue decision), so it needs threading through the payload whitelist; and pick column counts so every ROW IS FULL (two card sizes beat one shared count that always leaves an orphan). | EatACake `UIKit/ShopCard` + `Theme.ShopCard*`; the template's shop ships the button-style cells | pending |
| 2026-07-30 | fix | **A corner tag over the ART is the bug the reserved band existed to prevent - put it in a band that is EMPTY on the cells that use it.** Moving EatACake's shop cells to a card layout, I dropped `ShopPack`'s reserved ribbon band (it cost 32 px on every card for a tag one cell wears) and floated the tag over the art plate instead. The screenshot showed BEST VALUE covering a quarter of the gem cluster - precisely the failure the band's own comment documented. Better third option: place it in the strip between the art and the price, which is empty on every cell that carries a tag (currency packs have no perk line), and have the component give that strip to the tag when both are supplied. Costs no height, covers nothing, and the exclusivity is a stated rule rather than an accident. | EatACake `ShopCard` ribbon band | pending |
| 2026-07-30 | fix | **Recolouring a button's three layers does not make a card — measure the ink split.** EatACake's shop cells were rebuilt once, keeping the kit's Outer/Rim/Face recipe and only swapping the face colour, and the user's verdict was "basically the same buttons, only worse". Measured, the two were the same object: chrome ink split 18.4/5.5/76.0 % vs `Theme.Button`'s 20.3/4.9/74.8 (INSIDE the button family's own spread), the default accent was the button gradient BY REFERENCE, the product art drew at 10.50 % of the cell against the buy button's 10.47 %, and the cell CONTAINED a second pressable firing the same callback. Two cheap diagnostics worth stealing for any design-system argument: (a) compute the % of area each chrome layer takes and compare it to the element you are claiming to differ from; (b) compute the product art's area vs the CTA's — if the thing you are selling is not the largest object, it is not a product card. The fix is composition, not colour: a recessed art WELL (gradient inverted dark-top→light-bottom, derived from the accent's own dark Outer so contrast is guaranteed) plus a full-bleed CTA SHELF flush to the card's edges, replacing the nested button. | EatACake `UIKit/ShopCard` + `Theme.ShopCard*`/`ShopShelfStates`; the template ships the button-style shop cells | pending |
| 2026-07-30 | fix | **`local a, b = if cond then f() else g()` silently TRUNCATES the call to one value in Luau.** An if-expression yields exactly one value, so the second variable is nil — no error, no warning, no type complaint. It shipped in EatACake's shop card selecting between two `(Position, Size)` pairs; `Size` came back nil and the price label rendered at ZERO HEIGHT, i.e. an invisible price on a shop card, found only by screenshotting. Rule for the template's style guide: never put a multi-return call inside an if-expression — use an if/else statement, or wrap the call so it returns a single table. Worth a grep across any kit that adopted if-expressions for brevity. | EatACake `ShopCard.lua` price-zone selection | pending |
| 2026-07-30 | docs | **Roblox's `UIGradient` rotation artefact needs THREE conditions at once — design them out rather than testing for them.** A square element + a 45°-multiple `Rotation` + a keypoint at exactly t = 0.5. Any diagonal sheen on a square product icon hits all three by default. Cheap structural mitigation that costs nothing: make the face deliberately non-square (262x260), rotate at 115°/128° rather than 45°/135°, and place peak keypoints at 0.41/0.57 rather than 0.5. Also worth carrying: `Theme.lua` had ZERO `NumberSequence` before this change, so the kit had no transparency-ramp vocabulary at all and every `roundedFrame` helper (six copies) hard-coded `Rotation = 90` — a gloss band is impossible until one of them takes a rotation and a transparency argument. | EatACake `Theme.ShopCardSheen`, `ShopCard.roundedFrame` | pending |
| 2026-07-30 | docs | **When the user supplies REFERENCE IMAGES and says "use them, do not invent", that instruction outranks any research you commission.** EatACake's shop cards took three attempts. The brief said "use references as much as possible and do not invent anything from scratch" and shipped three competitor screenshots; I instead ran research agents over the genre and built what the research recommended — a recessed art well, a full-bleed CTA shelf, two gloss bands, a contact shadow, a premium halo. Every one of those measurably improved the card against the button it was being compared to, and the user rejected it in one line: "over complicated... a mess". The references were SIMPLER than my first version, let alone the second: colour, white border, title, icon straight on the colour, one line, a rounded price button. Two rules: (a) reproduce the supplied reference FIRST and only then argue for additions, one at a time; (b) a shop grid is scanned, not studied — the metric is elements-per-cell, not zones-per-cell, and the diagnosis "this is structurally a button" does not license adding five layers to prove it is not. The specific recurring error worth naming: putting ANY container behind a product icon (a plate, then a well) — both times it was the thing the user pointed at. | EatACake `UIKit/ShopCard`, three passes | pending |
| 2026-07-31 | contract | **A design system with only a BUTTON recipe will produce cards that look like buttons — add the CARD recipe explicitly.** `style-rules.md` §2 is the kit's single generative layer recipe and it says "bottom is always ~2x thicker — the weight that makes elements look physically thick". Two separate passes built EatACake's shop cells by following it, and both shipped an 8px-top / 30px-bottom outline (**3.75x**) under a flat colour field at 0.95 aspect. That lip is the strongest "pressable slab" signal the kit owns, so the user's verdict both times was "stretched-out buttons". The missing half, now §2b: **a button is a SLAB (one colour field, bottom-weighted outline, squat); a card is a FRAME (EVEN outline ≤1.5x, internal ZONES, portrait 0.78-0.85)** — and inside a card only the price button keeps the button recipe, because it is the only part that is a button. Generalises past shops: any "recolour the button recipe" element (inventory slot, quest row, location card) inherits the same wrong silhouette. | EatACake `style-rules.md` §2b + SKILL.md iron rule 3; `Theme.ShopCard*`, `UIKit/ShopCard` | pending |
| 2026-07-31 | fix | **Per-item colour on the CARD BODY destroys grid hierarchy; put it in the art window instead.** EatACake's six game passes wore six saturated rarity hues across the whole cell. Every card shouted equally, which is precisely what the user meant by "cluttered" — the complaint was never density. Fix that generalises to any catalogue grid: ONE neutral body for the whole grid + the accent confined to the product's art window. A row then reads as one object with N coloured windows, the strongest local contrast lands around the art (where the eye should go), and rarity is still colour-coded. Costs nothing and needs no new palette. | EatACake `Theme.ShopCardBody` + `ShopCard`; the template ships per-item body colour | pending |
| 2026-07-31 | contract | **The "nothing behind the icon" rule was over-generalised — a full-width art WINDOW is required, a badge PLATE is not.** Two rejected EatACake attempts put a badge under the glyph (a white circle; then a well + shelf + gloss + shadow + halo at once) and the recorded lesson became "nothing goes behind the icon", i.e. *remove structure*. The next pass removed the plate, kept the button bevel, and shipped a flat field — worse. The real distinction: a container that is smaller than the cell reads as a badge stuck on; a container that IS the cell's top zone at full content width reads as a card. And the window is load-bearing, not decoration: `ScaleType.Fit` draws at the SHORTER side of its box, so art of different aspect ratios (tall flame / wide egg cluster / square pack) placed straight on a face renders at wildly different visual sizes — one window normalises them all. Measured: icon area 13.5% -> 22.0% of the cell, identical across both card sizes. Template rule: when a rejection produces a rule, write down WHICH PROPERTY was rejected, not the whole layer. | EatACake `Theme.ShopCard.Art*`, `features/shop.md` | pending |
| 2026-07-31 | fix | **A categorised panel whose canvas exceeds ~2 window-heights needs TABS, not a longer scroll.** EatACake's shop stacked five sections into 2046 nominal px inside a 367 px window — **5.6 screens**, whose first screen was the balance chips, one section header and one banner. Four tabs cut the worst tab to 1.9 screens and let two of four render with no scroll at all. Three details that made it free rather than costly: (a) the tab row's 56px is paid for by moving the balance chips into the header band, which is empty either side of a centred title in every kit panel; (b) a single-section tab draws NO section header (the tab already names it) — and a tab must never be LABELLED the same as one of its own sections, which reads as a rendering bug; (c) switching tabs must REMOUNT the scroll (put the tab id in the child's React key) because a `ScrollingFrame` keeps `CanvasPosition` across a re-render, so a 2-row -> 1-row jump lands past the new content on blank canvas. Bonus: only the active tab's cells render, which cut the tree from ~16 cells to ≤6. | EatACake `UIKit/ShopTab`, `ShopPanel` tabs prop, `Theme.ShopLayout` | pending |
| 2026-07-31 | fix | **`TextScaled` binds on WIDTH, so long copy in a narrow cell SHRINKS — it does not wrap-and-truncate, and the doc that said "wraps to two lines" pointed at the wrong mechanism.** "One squishy, better odds" in a 176px zone rendered at ~8px: unreadable, no warning, no overflow. Because both axes are fitted, zone HEIGHT is not the lever — only character count is. Any catalogue module whose strings land in scaled zones should publish the limit per zone next to where the copy is written (EatACake: ~22 chars on the 246px card, ~15 on the 176px card). Same pass, same family of bug: the price label read `"R$ {n}"` while the button ALSO drew the Robux glyph, so every card said "⬡ R$ 199" — a currency string and a currency icon are two representations of one fact and something must own which is used. | EatACake `ShopData` desc header, `price-robux-short`, `features/shop.md` gotchas | pending |
| 2026-07-31 | docs | **Verify chrome by MEASURING live instances, not by eyeballing a whole-panel screenshot.** The 30px bottom lip survived two passes of "verified visually in Studio" because at panel scale it reads as a drop shadow; it is only obvious when one cell fills the frame or when the number is printed. Cheap, general check to add to any UI ship gate: `Face.AbsolutePosition - Outer.AbsolutePosition` versus the same at the bottom edge, plus `min(icon.AbsoluteSize.X, icon.AbsoluteSize.Y)^2 / (cell area)` for how much of the cell the product actually occupies. Both are one `execute_luau` call and both turn a design argument into a number (EatACake: outline 3.75x -> 1.50x, art 13.5% -> 22.0%). | EatACake SKILL.md verification checklist | pending |
| 2026-07-31 | recipe | **Iterate kit UI in EDIT mode with a React preview harness — a playtest per screenshot is the reason visual passes stop early.** Clone `ReplicatedStorage.Shared.UIKit`, require the CLONE, and `ReactRoblox.createRoot` into a `CoreGui` ScreenGui: it renders in the edit viewport, `screen_capture` sees it, and mock props exercise states (owned / disabled / premium / ribbon) that live data cannot — EatACake's ids are all 0, so a playtest can only ever show the SOON state. Two traps that cost real turns: the clone must be parented INSIDE `Shared` or every `script.Parent.Parent.Log` require dies with "Log is not a valid member of ReplicatedStorage"; and the clone is the whole point, because `execute_luau`'s require cache persists across calls and would otherwise hand back the pre-edit Theme forever. Turnaround ~5s vs ~40s, and it made a 6-iteration visual pass cheap. | EatACake shop redesign; belongs next to `tools/studio-bridge/` | pending |
| 2026-07-31 | fix | **A hover/press SQUASH on a grid cell fights the scroll window that clips it — grid cells need an X-safe pose.** The kit's card squash combines a uniform UIScale with a `Content.Size` pose: `1.05 * 0.970 = 1.019` on hover and `0.93 * 1.10 = 1.023` on press, growing about the cell's CENTRE. A deterministic grid packs its columns to the canvas edges exactly (`3*282 + 2*12 = 870`) and a `ScrollingFrame` clips, so there is ZERO horizontal slack: every hover shaved ~3px off the outer columns' dark outline and ~10px off a full-width banner's, i.e. the outline the whole card redesign was about. Vertically the same growth ate the ribbon's top overhang — the canvas top pad has to clear `(halfHeight + ribbonOffset) * hoverY - halfHeight` (17.4px here), not the static offset. Fix that keeps the motion: a `GridCell*Pose` pair whose X never exceeds 1.0, with `hoverScale = 1, pressScale = 1` (the UIScale is uniform, so it can never be part of an X-safe deform) — the squash still reads, it just runs on Y. Worth auditing anywhere the template puts a pressable inside a clipping grid; PetCard has the same shape. Was invisible in EatACake because every product id is 0, so the cells are disabled and `usePressable` attaches no handlers at all — the bug arms itself the day monetization is configured. | EatACake `Theme.Feel.Squish.GridCell*Pose`, `ShopCard`/`ShopBanner`/`ShopHeroCard`, `ShopLayout.CanvasTopPadPx` | pending |
| 2026-07-31 | fix | **`React.memo` on a panel is a no-op unless the CALLBACKS are memoised too — and the comment claiming otherwise is worse than no memo.** EatACake's `ShopPanel` was wrapped in `React.memo` with a comment asserting its inputs "are already memoised upstream", which was true of the data props and false of the two handlers: AppRoot passed `onActivated = function(rowId) ... end` and `onClose = function() ... end`, fresh closures every render, so the shallow compare failed every time and the whole tree reconciled at bite rate exactly as before. The memo cost a compare and bought nothing, and the comment would have stopped the next person profiling it. Rule: a `React.memo` boundary is a CONTRACT on the call site — either `useCallback` every function prop crossing it (empty deps are safe when the closure only reads module-level tables) or do not wrap. Cheap check: grep the call site for `= function(` inside the memoised element's props. | EatACake `AppRoot` shop props + `ShopPanel` memo | pending |
| 2026-07-31 | fix | **An R8 warning about MISSING data must be deferred until the data has demonstrably arrived, or it fires on the first render and lies forever.** Adding "this section is empty / this tab is hidden" logging to EatACake's shop produced eight confident warnings on every join — because the first render happens before `ShopUpdate` lands, when every section is legitimately empty, and `Log.Once` fires immediately and never re-checks. The general shape: for a UI fed by an async payload, an emptiness warning needs a WITNESS that the payload arrived (here: at least one live tab) before it means anything; the "nothing arrived at all" case is the separate `Log.GraceOnce` that already exists for exactly this. Same trap applies to any `GraceOnce` whose `stillBroken` closure captures a render-scoped value instead of reading a ref — it would report the state at the moment of the first bad render, not at expiry. | EatACake `ShopPanel.reportEmpties` / `no-live-tabs` GraceOnce | pending |
| 2026-07-31 | contract | **A timed buff needs a per-stat plan: a stat READ per use needs nothing, a stat PUSHED once needs an expiry re-apply — and an expiry fires no event.** EatACake's boosts multiplied `calories`/`gems` for months with no machinery at all, because the next read simply stops finding the buff. Adding `walkSpeed` (written onto the Humanoid), `capacity` (rides a push payload) and `biteRadius` (predicted client-side) broke that: nothing rewrites a pushed value when a timestamp quietly passes, so a speed buff would never wear off — a movement exploit that ships silently. Pattern worth templating as a `BoostSubs`: one idempotent `Apply(player)` that re-derives everything a live buff feeds, called on grant AND armed as a generation-stamped one-shot `task.delay` on the SOONEST expiry, with a cheap signature poll (`id@expiresAt` sorted+joined, compared against the last seen) as the BACKSTOP rather than the mechanism. The timer is what makes the common case exact; the poll is what makes it correct. The signature read must also PRUNE, so the poll both detects and effects the expiry. | EatACake `subscriptions/BoostSubs`, `StatsService.BoostSignature/NextBoostExpiry`; the template ships timed boosts with read-per-use stats only | pending |
| 2026-07-31 | contract | **Mirror a server-side multiplier to client PREDICTION through a player ATTRIBUTE, not a remote.** EatACake's client predicts bite craters from its own copy of the replicated upgrade levels, which know nothing about a timed bite buff — so a 15-minute boost meant 15 minutes of every predicted crater disagreeing with the authoritative one. Attributes replicate by themselves: `player:SetAttribute("BiteRadiusMult", …)` on grant/expiry, `math.max(1, tonumber(GetAttribute(...)) or 1)` on read, and there is no payload, no subscription and no ordering problem. Two rules that came with it: floor the client value at the un-boosted number (under-prediction snaps away cake the player never saw go; OVER-prediction makes cake visibly pop back, which is the worse failure), and treat the attribute write as part of the same Apply that handles expiry — a mirror written only on grant is worse than no mirror. | EatACake `BoostSubs.Apply` + `LocalStatsService.BiteRadiusMult`; generic to any predicted-then-reconciled client value | pending |
| 2026-07-31 | fix | **A descriptor validator must prove the id RESOLVES, not that the string is non-empty.** EatACake's reward grammar (ADR-0002) validated `{kind="boost", boostId=s}` as "is a non-empty string", while the grant function answers an unknown id with `false`. Two things then went wrong for free: the daily table shipped a boostId naming a FIND, and the shop's four product keys deliberately differ from the four def ids with exactly one collision. On a Robux receipt that is a deferral; on an in-game-currency path it is money taken for nothing. Rule: every descriptor field that is a KEY INTO A TABLE is validated against that table at the point of validation, and the whole catalogue is swept ONCE at boot so a typo costs a startup line instead of a purchase. Cheap and general — the template's grant registry has the same shape for every kind it ships. | EatACake `ShopSubs.descriptorValid`, `RewardsSubs.grantable`, boot `badBoost` report | pending |
| 2026-07-31 | contract | **When your own server is the cashier, the retry contract inverts — write the compensating refund.** A Roblox receipt is re-delivered forever, so "defer and let it come back" answers almost every failure and the hard problem is not granting twice. An in-game-currency purchase has no re-delivery at all: the hard problem is CHARGING FOR A NO-OP, and deferral is not an available answer. The four rules EatACake had to write by hand, none of which the receipt path needs: validate the WHOLE grants list before spending; spend through an ATOMIC check+deduct inside the service (a read-then-subtract in the handler lets a second click pass the same balance check); no yields between the spend and the grants; and if a pre-validated grant still declines, REFUND the price and say so loudly — "should be impossible" is not a guarantee a currency path may rest on. Plus: refuse outright (not defer) while a teleport release is in flight, because the save is a no-op there and nothing will bring the purchase back. The refund is all-or-nothing at the PRICE, so a multi-grant product would refund in full while keeping what succeeded — warn at boot if one appears. | EatACake `ShopSubs` RequestGemPurchase + ADR-0015; the template ships only the Robux path | pending |
| 2026-07-31 | fix | **A catalogue-driven tool must hard-exit on a key it can no longer resolve — which means one stale entry BRICKS it.** EatACake's Open Cloud monetization tool reads prices out of `ShopData.lua` and exits on the first key with no `devProductId`; that reader runs before EVERY branch of main, including `--verify` and the dry run. When four products became gem-priced (no dashboard id by design), the four stale entries in the tool's copy of the catalogue did not create four unwanted products — they disabled the only supported way to create the ids the shop was blocked on, for every other product too. The exit is right; the coupling is the bug. Two fixes worth carrying: an explicit positive test for the disqualifying case (a `currency = "gems"` key listed here at all → exit naming the reason), and a header on the copy saying it is Robux-only and why. General rule: whenever a tool holds a SECOND copy of a catalogue, deleting from the first is a documented step, and the tool must assert the invariant it depends on rather than dying on a lookup. | EatACake `tools/monetization/create_monetization.py` PRODUCT_COPY / `read_shopdata_prices` | pending |
| 2026-07-31 | docs | **THIRD repetition: label-length arithmetic for `TextScaled` zones is a kit-level tool, not a per-card comment.** The shop cards, the hero bundle chips and now the gem boost row have each had the same paragraph written by hand — "this zone is N px wide, Fredoka advances ~0.75 × font size per glyph, so M characters render at N/(0.75M) px, capped at the zone height". It has already decided real copy three times: "Extra Bite Size" (15 chars) renders at 15.6px, SMALLER than its own 16px perk line, which inverts the card's hierarchy, so the requested name ships as "Extra Bite". Three uses is a helper: `Theme.FitsAt(zoneWidthPx, text, capPx)` returning the rendered size, plus the same numbers published beside where the copy is AUTHORED (the data module), not only where it is rendered. Pairs with the existing §2b card-vs-button rule — a card is a FRAME with internal zones, and every zone in it has a character budget. | EatACake `ShopData` boosts header, `Theme.ShopHeroItem`, `.claude/skills/roblox-ui-kit/references/style-rules.md` §2b | pending |
| 2026-07-31 | docs | **A reward whose COUNT is roster-independent must pay PER HEAD, or co-op silently pays a quarter.** EatACake's buried finds are seeded from cake VOLUME (a fixed footprint and height for any population) and each one is consumed by whoever reaches it first — so 4 players share the same 40 finds, ~10 each. Calories already had a per-head term; gems did not, so a 4-player cake paid each player a quarter of the number the entire boost price was set against ("one cleared cake buys one boost" became one per four cakes). The test to run at balance time, for every reward stream: does its COUNT scale with the roster, or only its DIVISION? Scale exactly the streams whose answer is "division only". Second rule from the same fix: apply the per-head term but NOT the difficulty premium to anything a fixed SHOP PRICE is set against, or the price means something different on every difficulty. | EatACake `CakeConfig.composition.coopFinds`, `CakeCycleService.ScaleFindReward`, `features/treasures.md`; the template's reward tables are all flat | pending |
| 2026-07-31 | contract | **Make a granted entry SELF-DESCRIBING and deleting its config def needs no migration.** EatACake stores a live boost as `{id, stat, mult, expiresAt}` rather than `{id, expiresAt}` plus a lookup, so every reader takes the numbers from the ENTRY and the def table is consulted only at grant time. Deleting a def therefore costs nothing: an entry already granted from it keeps working and expires normally — no version bump, no migration, no dead-lookup branch. (The re-grant path re-stamps `stat`/`mult`, so a RETUNED def still reaches a player holding the old one — that is the other half of the rule.) Generalises to any persisted instance of a config-defined thing: copy the values you must honour, key only what you must look up. Same shape at a bigger scale in the same session: deleting two whole profile SECTIONS needed nothing either, because the schema loader iterates the SCHEMA and preserves unknown top-level keys verbatim. | EatACake `StatsService.GrantBoost` + `ProgressSection.activeBoosts`; `PersistenceService.applySchema` | pending |
| 2026-07-31 | fix | **Listing pagination on the Roblox creator APIs is `pageSize`/`pageToken`/`nextPageToken`, NOT `cursor`/`nextPageCursor`.** The wrong param names do not error — the endpoint returns page 1 and no `nextPageCursor`, so the loop terminates and the caller believes it has seen everything. In an idempotency check that matches live items by name, a truncated listing means "this does not exist yet" and the tool creates a PERMANENT duplicate (neither dev products nor game passes can be deleted). The reference project contained BOTH forms — the wrong one only in throwaway probe scripts that never had more than one page, which is exactly how it got copied. Template rule: a paginating client must be tested against >1 page, or assert that the response carried a recognised token key. | EatACake `tools/monetization/create_monetization.py` `list_products`/`list_passes`; generic to any Roblox creator-API client | pending |
| 2026-07-31 | contract | **For a resource that cannot be deleted, the LOCAL LEDGER is the idempotency key — never the remote listing.** EatACake's monetization tool decided "does this exist?" by matching the dashboard NAME in the live listing, and treated any unrecognised 200 as an empty universe. Both failure modes mint a permanent duplicate of the entire catalogue: a name Roblox normalises or moderation rewrites, and a response-shape change. Rule: (a) a key with a truthy id stamped with the same target wins outright and skips the remote lookup; (b) name matching is only the fallback; (c) an unrecognised 200 from a listing is FATAL, never "empty"; (d) the ledger file is COMMITTED, because it is the only record of ids that can never be recovered by key. Generic to every create-once remote resource (products, passes, badges, assets). | EatACake `tools/monetization/{create_monetization.py,id_map.json}`; the reference `create_backrooms.py` had the right contract and the port had drifted off it | pending |
| 2026-07-31 | docs | **Roblox monetization: a DEVELOPER PRODUCT is create-once — no delete AND no update endpoint.** Seven icon/update candidates were probed live in the reference project and none was adopted; it went on creating dev products icon-less afterwards, which is the evidence they were abandoned rather than pending. So name, description and price are FINAL at the POST, and dev-product icons must be set by hand in the Creator Dashboard. A GAME PASS is the opposite: one `PATCH /game-passes/v1/universes/{u}/game-passes/{id}` carries price + `isForSale` + icon (multipart part named exactly `File`), partial PATCH accepted, and `isForSale=false` is the only undo that exists for either resource. Consequence for any template shipping a monetization catalogue: the product-vs-pass decision must be made BEFORE the first create, and the tool should say so at the point of no return. | EatACake `tools/monetization/`; reference `fix_passes.py` / `fix_magnet_products.py` / `icon_probe2.py` | pending |
| 2026-07-31 | docs | **`isRegionalPricingEnabled` must be FALSE at create, and the first create must be PROVEN before the rest.** With it true, Roblox treats `price` as a seed for its own regional ladder and the base price does not come back equal to what was asked — the reference created 53 items that way, found every price wrong, and could only repair the passes. Creating with `false` pins the exact amount (VERIFIED 2026-07-31, 11/11 read back correct) but was untested before this task, so the tool mints the CHEAPEST item first, reads the live price back from the listing, and ABORTS the run on a mismatch. Generalisable pattern well beyond pricing: when a batch of irreversible writes rests on one unverified assumption, order the batch so the cheapest item tests it, and make the read-back a hard gate rather than a log line. ⚠ The price field to read back differs per resource: products `priceInformation.defaultPriceInRobux`, passes `userBasePriceInRobux` (`price` is null when off sale). | EatACake `tools/monetization/create_monetization.py` `create_items`; generic to any irreversible batch create | pending |
| 2026-07-31 | fix | **A session cookie set as `session.cookies[name] = value` has NO domain and is sent to every host the session touches.** EatACake's monetization tool set `.ROBLOSECURITY` that way and then fetched the icon bytes from an arbitrary rbxcdn URL on the same session — leaking a full account session to a CDN. Use `session.cookies.set(name, value, domain=".roblox.com")`, and fetch third-party URLs with a separate plain client. Also worth a template rule: a tool whose flags include destructive writes must gate ALL of them on the same `--apply` — this one's `--icons` PATCHed `isForSale=true` plus a price onto already-adopted live items during a DRY RUN, silently re-enabling anything deliberately retired. | EatACake `tools/monetization/create_monetization.py` `Client.__init__` / `game_icon` / `--icons`; generic to every Python tool using `requests.Session` with credentials | pending |
| 2026-08-01 | feature | **Tonal-hierarchy analyzer + skill — objective "where does the eye go" measurement for ANY UI.** `tools/tonal-hierarchy/tonal.py` (PIL+numpy only): CIELAB L* value bands (squint test as a file), multi-scale center-surround Lab saliency (catches BOTH failure directions of "hue is not hierarchy": saturated patches that value-blend AND value-matched gold that vanishes in grayscale), per-region attention ranks vs INTENT annotations (levels 1-4 + roles), residual metrics for containers (children masked — without this every panel false-fires as a sink), median-gated inversion findings (saliency is max-normalized per image; absolute floors do not transfer), findings-aware compare verdict (clearing CRITICALs = IMPROVED even when the trend score is flat), 13-check selftest. Plus `dump_regions.luau` (Studio rect dumper → `tonal.py listen`) and the skill (`.claude/skills/tonal-hierarchy/`) with capture recipes + per-finding fix recipes. Wired into the ui-kit ship checklist. Proven: found and fixed 4 real defects in the shipped shop, gated IMPROVED. | EatACake `tools/tonal-hierarchy/`, `.claude/skills/tonal-hierarchy/`, flow `2026-08-01_tonal-hierarchy-toolkit.md`; fully game-agnostic (works on any screenshot) | pending |
| 2026-08-01 | fix | **Kit tokens: QUIET means close in VALUE, not merely low chroma — the template kit has the same dark "muted" idle/chrome styles.** Three concrete fixes, all measured: (a) `ShopTabStates.idle` was a desaturated-but-DARK slate (dL* −58 on the white panel — the three idle tabs tonally out-shouted the selected gold one 3.6x; the comment even said "deliberately LOW contrast"); now a light wash of the panel (~dL* −12..−17). (b) `ShopTabStates.selected` face = own deeper-gold literal (Legendary hue, V ×0.85) — bright gold sat in the panel's own L* band and vanished in grayscale; the active state needs a VALUE anchor, not more brightness. (c) `Theme.Scrollbar` thumb AND track ring were button-blue/near-black wells that out-shouted card TITLES in every scroll window; now a light slate family (chrome recedes). Also `ShopHero.ArtFaceGradient` optional override (antique-gold plate — a full saturated accent field behind hero art out-shouts the buy shelf) + `ShopHeroItem` chip face one band above the card body. Rule added to style-rules §4. | EatACake `src/shared/UIKit/Theme.lua`, `ShopHeroCard.lua`; template `RobloxTemplate/src/shared/UIKit/` carries the same idle-tab/scrollbar styles | pending |
| 2026-08-01 | docs | **A tonal fix that passes the analyzer can still fail AFFORDANCE — the user read measured-quiet tabs as LOCKED, info chips as BUTTONS, and a value-tempered decor plate as DIRT.** Three rules came out of round 2, now codified (style-rules §2c + tonal skill iron rule 8): (a) quiet an INTERACTIVE element by lightening its OWN hue family while keeping the pressable cues (rim flash + bottom lip) — draining saturation lands in the kit's locked/disabled language; (b) a PASSIVE tag never wears the pressable recipe — flatten to one fill (slab=button / frame=card / FLAT=tag); (c) a decorative backing with no layout job (a plate under a SINGLE item — nothing to size-normalise) is REMOVED, not recolored — a useless surface recolored is just a new wrong color. Also a gate-fidelity lesson: a preview mock that renders a section production drops (empty section => headerless tab) manufactured the only persistent CRITICAL — make the capture match production before trusting the compare gate. | EatACake `Theme.lua` ShopTabStates/ShopHeroItem/ShopHero.ArtPlate, `ShopHeroCard.lua`, style-rules §2c, tonal SKILL rule 8; template kit has the same chip/tab recipes | pending |
| 2026-08-01 | docs | **Removing a UI surface is only half the job — RE-CUT the zone arithmetic or the freed space becomes a layout hole.** Dropping the hero's art plate left the 168px gift floating in the 206px zone the plate had filled: dead space, small art, small CTA — the user called it "very bad sizes and positions". The fix was a Phase-C re-cut (ShopHero 870x260 -> 870x300, art grown to the plate's own footprint, price shelf 302x64 to match its button's aspect, sums closing exactly). Second lesson from the same round: **sticker-text outlines never soften** — white glyph + FULL-dark outline is self-contained on any face; softening the outline "to match a quiet surface" is what washed the chip/tab labels ("hard to read"). Flatness lives in the fill, never in the text. | EatACake `Theme.lua` ShopHero/ShopHeroItem/ShopTabStates, flow `2026-08-01_tonal-hierarchy-toolkit.md` round 3; template hero has the same geometry | pending |
| 2026-08-01 | feature | **Modal scrim behind every open panel + tap-outside-to-close (`Theme.PanelScrim` + one AppRoot TextButton at zIndex 40).** Panels floated over the FULL-BRIGHTNESS world; measured on live captures, the busy scene + colorful HUD icons out-shouted panel content in every view — the single biggest readability lever found in a five-round shop audit, and the template''s AppRoot has the same hole. Gotchas baked in: full-screen overlays that carry their OWN scrim must be excluded (double-dim), and the scrim TextButton also blocks HUD input while a panel is open (correct — and it makes occluded HUD `ignore`-grade for tonal audits). | EatACake `AppRoot.lua` Scrim + `Theme.PanelScrim`; flow `2026-08-01_tonal-hierarchy-toolkit.md` rounds 4-5 | pending |
| 2026-08-01 | contract | **The genre''s currency loop must be VISIBLE: a broke player needs a one-tap path from "can''t afford" to "buy currency".** Four-lens review called it the worst omission in the shop: 4 gray 500-gem shelves, 186 gems, and no route to the Gems tab. Shipped as (a) `balances[].jumpTabId` — the header currency pill grows a green "+" and the whole chip taps through to the named tab (graceful if the tab is hidden: ShopPanel falls back to tab 1); (b) `ShopPriceStates.unaffordable` split from `unavailable` (was one shared table!) with a RED price on the grey shelf — grey-with-price reads "sold out", red reads "too expensive". Also: OWNED is a flat STAMP not a raised button (false affordance, 3/4 lenses), and a grid must let the next row PEEK above the fold or players assume the visible rows are the whole catalog (passes went 3-across 338px -> 4-across 264px for a 66px peek). | EatACake `ShopPanel.lua` balances, `Theme.lua` ShopPriceStates, `LocalShopService.lua` passes kind; template shop shares all of it | pending |
| 2026-08-01 | feature | **The SQUINT TEST as a tool + skill: heavy-blur the capture (gray AND color) and measure what survives — the interface a NON-READING child actually navigates.** `tonal.py blur` + per-region blur-survival metrics (`dl_blur`/`chroma_blur`, VISIBLE/GONE column, `blur-invisible` finding — CRITICAL on cta/tab-active; text roles exempt because text ALWAYS dissolves, the ELEMENT must survive) + skill `.claude/skills/squint-test/` with the icon-first iron rules: icons carry meaning and text reinforces; state reads glyph-first (owned=check, cannot-afford=red price+currency glyph); fix levers are COLOR/TONE/SIZE, never outlines or text; one glyph per concept across HUD/shop/rewards. Shipped against it: tab glyphs (ShopTab optional iconName — ⚠ ShopPanel liveTabs REBUILD silently dropped the new field, classic rebuild-strips-fields trap), OWNED check glyph, a faint light disc under price glyphs (IconPlateTransparency — a dark currency mark on a same-value shelf vanishes), hero chip icons 28→34. Physics note for the template: a 2.5%-of-dim blur mathematically dissolves any ~40px element — small chrome NEVER survives, so put the meaning in the big masses. | EatACake `tools/tonal-hierarchy/`, `.claude/skills/squint-test/`, `ShopTab/PriceButton/ShopPanel/LocalShopService`; fully game-agnostic | pending |
| 2026-08-01 | feature | **One-time ONBOARDING scaffold: a `tutorial` profile section + a client-owned step machine + three reusable surfaces.** Shape worth copying wholesale: the SERVER owns exactly one boolean (`{done}`) pushed once via `PushInitialState` and set by one unauthenticated-but-validated remote (safe precisely because the flag GRANTS nothing — it only suppresses UI, which a Skip button already offers); the CLIENT owns the whole step machine, because a round trip per step buys nothing on a flow whose job is to feel immediate. Tri-state `serverDone` (nil = not told yet) so "skip" and "the flag arrived" can race in either order. New kit pieces are all game-agnostic: `TutorialSlides` (2x2 story board), `TutorialHint` (instruction card), `InputGlyph` (vectored mouse / on-screen-button miniature, branched by the caller from `IS_TOUCH`), `HintArrow` (world-tracking objective marker), `Button.pulse`, and `AppRoot.Get(key)` for reading a fact ANOTHER sub owns instead of duplicating its derivation. | EatACake `TutorialSection.lua`, `TutorialSubs.lua`, `TutorialSubsClient.lua`, `UIKit/Components/Tutorial*`/`InputGlyph`/`HintArrow`, `features/tutorial.md`; the template has no onboarding at all | pending |
| 2026-08-01 | contract | **A tutorial popup that TEACHES an input must not consume that input — and the trap is invisible in the component.** Roblox delivers `gameProcessed = true` to `UserInputService.InputBegan` for any click landing on an `Active` GUI surface, so the standard full-screen click-catcher (the kit's own reveal overlay uses one) silently blocks the exact left-click a "click to eat" popup is pointing at. Rule: an instruction surface for a world action carries NO scrim and NO catcher — only its CTA is a button — and it should ALSO auto-dismiss when the taught action first succeeds, so the player who worked it out never has to close it. Corollary for the inverse case: a modal that SHOULD block (a story board) gets the catcher deliberately, and must hide the whole HUD LAYER rather than element-by-element (three separate HUD elements leaked through a 76%-opaque scrim before that) — while any HOLD button keeps its own `visible` gate too, because the press primitive releases a hold on `enabled` flipping, not on an ancestor's `Visible`. | EatACake `TutorialHint.lua` header, `AppRoot.lua` Hud gate + `eatButtonVisible`, `CakeSubsClient` InputBegan; the template kit has the same catcher pattern in `PetRevealOverlay` | pending |
| 2026-08-01 | docs | **A PROXIMITY zone and a FOOTPRINT zone are different shapes — never gate a step on one and trigger it from the other.** EatACake's tutorial advanced to its last step on "standing inside the checkpoint plate's XZ footprint + 0.4 studs" but completed on the station's ProximityPrompt — a 10-stud sphere with `RequiresLineOfSight = false` whose activation reaches ~6 studs back onto the cake, where the footprint test still reads FAR by explicit design. Every player who pressed E on the way in had their completion silently dropped and replayed the whole tutorial next run. General rule: when a state machine's ENTER condition and its EXIT trigger come from different geometry, either derive both from the same test or accept the trigger from ANY live state — and a discarded trigger must log (R8), which this one did not. | EatACake `TutorialSubsClient.lua` PromptTriggered, `MapService`/`MapConfigData` prompt ranges vs `Theme.AppHud.CheckpointHideMarginStuds`; generic to any prompt-vs-region flow | pending |
| 2026-08-01 | fix | **Screen-space markers must do their maths in PIXELS, and a behind-camera target must be PUSHED to the edge, not clamped.** Two defects in one 20-line function, both invisible until reviewed: (a) deriving the edge-pin angle from viewport FRACTIONS scales x and y by different lengths, so the arrow missed by up to ~20° on a wide viewport (worst near the diagonals) — take `atan2` in pixel space; (b) `WorldToViewportPoint` mirrors a behind-camera target through the viewport centre, and a target straight behind lands EXACTLY on the centre, so per-axis `math.clamp` leaves it there and the marker parks mid-screen pointing at nothing (`atan2(0,0)` = 0). Correct pin is a ray-box push-out along the point's own direction, with a degenerate-direction fallback. Also worth a template rule: `WorldToViewportPoint` is the space of a FULL-BLEED ScreenGui — pairing it with an inset gui drifts everything down by the topbar. | EatACake `UIKit/Components/HintArrow.lua`, ADR-0016; generic to every objective/quest marker | pending |
| 2026-08-01 | contract | **A per-frame WORLD-TRACKING widget owns its own RenderStepped — pushing screen coords through the app state re-renders everything at 60 Hz.** In a single-React-root app, routing a marker's screen position through the state bridge reconciles the ENTIRE composed tree (including a ~700-element memoized shop panel) every frame, for as long as the marker is up — four times worse than the bite-rate re-render the kit already memoizes around. The sanctioned shape: the component creates the connection in its own `useEffect` keyed on `visible`, writes `Position`/`Rotation`/`Visible` on ref'd children, and React writes each of those exactly ONCE with a module-level constant (the same pass-a-CONSTANT branch that already sanctions the kit's zero-fill and knob-initial values). The world target arrives as a CALLBACK read through a ref, so a caller passing a fresh closure every render reconnects nothing. Boundary that keeps the "subscriptions own events" rule intact: this observes no GAME event — it is the component rendering itself against the camera. | EatACake ADR-0016, `UIKit/Components/HintArrow.lua`; template has ADR-0006 but no world-tracking case | pending |
| 2026-08-01 | docs | **An authored world asset can be perfectly wired and still invisible — verify it ON SCREEN, not by property dump.** EatACake's guidance beam checked out on every readable signal (enabled, opaque, attachments set, in workspace, texture preloads) and could not be found in a screenshot: the authored `Beam` is a 3-stud WHITE line, and the guidance run is 80+ studs across a pastel sky over a near-white loaf. Isolation that found it: fatten to width 20 + strip the texture (it rendered → wiring fine), then restore the texture at width 20 (near-invisible → hue is also wrong) — changing one variable at a time in play mode. Fix belongs on the CLONE (R5 keeps the authored template pristine) with the overrides nil-able so a future re-author wins. Template rule: legibility of a world VFX is a property of the SCENE it runs in, so any cloned-template visual needs config-level width/colour overrides, and "the properties look right" is not verification. | EatACake `TutorialConfig.beam.width/.color`, `TutorialSubsClient.ensureBeam`; generic to every cloned Beam/Trail/ParticleEmitter | pending |
| 2026-08-02 | feature | **Analytics as a BUDGETED CATALOG — the template has none, and the naive implementation fails silently.** Roblox analytics is quota'd (VERIFIED 2026-08-02 against create.roblox.com/docs/production/analytics + devforum 3084051): **120 + 20×CCU events/min per SERVER** (a solo server ≈ 140), **100 custom event names per experience ever**, **10 funnels × 100 steps**, **3 custom fields / 8000 unique values**, server-only and published-only. Every one of those is enforced by a SILENT server-side drop — no error at the call site, just a metric that never appears. The shape worth copying wholesale: (a) ONE catalog module declaring funnels/steps/event names/quotas, with a `Validate()` that logs occupancy at boot; (b) "every tap" as ONE event name (`ui_press`) carrying the control id in a custom FIELD — Roblox's own docs say to prefer fields over names; (c) a token bucket at the live allowance with three priority tiers and RESERVES, so a tap storm cannot eat the budget a once-only funnel step needs two seconds later; (d) coalescing identical tuples into the event's `value` (the batching trick the docs name) — 40 taps cost 1 event; (e) refusals COUNTED and reported in-band. | EatACake `src/shared/config/AnalyticsConfig.lua`, `src/server/common/subscriptions/Analytics/{Sink,Session,Ingest}.lua`, ADR-0017, `features/analytics.md`; fully game-agnostic — the template ships an `AnalyticsSubs` stub with hand-written event names and no budget at all | pending |
| 2026-08-02 | fix | **"AnalyticsService threw" has TWO meanings and the template stub conflates them.** The existing pattern (EatACake's and the template's) disables telemetry for the whole server on the FIRST pcall failure. That is correct for an unpublished place, where every call throws — and catastrophic live, where "You have sent too many events" is a routine rate limit on a POPULAR server: one busy minute would silence the metrics permanently, and the more successful the server the sooner it goes dark. Fix: match the message. A rate limit empties the token bucket for 60s and keeps running; anything else needs THREE consecutive failures before disabling, with one warn. | EatACake `Analytics/Sink.lua` `invoke()`; template `AnalyticsSubs.lua` `safe()` has the single-failure disable | pending |
| 2026-08-02 | contract | **A funnel that spans a teleport needs its session id in TeleportData — as an ARRAY.** In a two-place universe (ADR-0009) every lobby↔game handoff ends the player's server session, so a per-server funnel breaks in half exactly where onboarding gets interesting: the lobby half ends at "teleport started", the game half opens at "arrived" under a fresh id, and nothing joins them. Carry `{ {u=userId, s=sessionId, c=cohort, i=flowIndex}, … }` on the launch AND return payloads; the destination adopts and suppresses steps at or below the carried index. ⚠ It must be an ARRAY, not a userId-keyed map — RemoteEvent/TeleportData serialization stringifies numeric keys, so the lookup would miss silently. Related trap: ProfileStore's `SessionLoadCount` is a PROFILE-load count and a teleport is a load — it reads 2+ for a brand-new player in the destination place, so "is this their first session" must be carried too, never recomputed there. | EatACake `Analytics/Session.lua` HandoffPayload/Adopt, `LobbyQueue/Launch.lua`, `GameRound/Return.lua`; generic to any multi-place universe with cross-place funnels | pending |
| 2026-08-02 | feature | **Count every kit press from ONE injection, with the id derived from the Instance Name.** `Interaction.SetTrackHandler` mirrors the existing `SetSoundHandler` shape: the kit stays client-free, a subscription injects the handler once, and `usePressable` reports on the AGGREGATE press edge (multi-touch and drag-off are one tap). Identity falls back to the pressed Instance's own `Name`, with the parent prefixed when the name is generic — so a button is instrumented because it EXISTS, not because someone remembered it, which is the only way "track every tap" survives contact with a 40-component kit. Second half worth copying: a DISABLED button still reports, through `React.Event.InputBegan` — which fires regardless of `Active`, unlike `Activated`/`MouseButton1Click`, and that difference IS the signal being recorded ("the player pressed and the game did not answer"). No `Active` value is changed, so nothing sinks differently. | EatACake `src/shared/UIKit/Interaction.lua` + `init.lua`, `AnalyticsSubsClient.lua`; the template kit has the same `usePressable`/`SetSoundHandler` primitive and no tracking hook | pending |
| 2026-08-02 | docs | **Instrument the CHOICE, not just the submission — and never contend for a callback key another sub owns.** Two lessons from wiring a lobby funnel. (1) A selector panel keeps its selections in local React state and sends NOTHING until the submit button is pressed and accepted, so "chose a difficulty and then walked away" is invisible to the server — the most interesting drop-off in the whole flow. Fix: observation-only props (`onSelectDifficulty`/`onSelectPlayers`/`onTabChanged`) that change no behaviour and exist purely to be seen; and log the submit press CLIENT-side before validation, so the gap between "pressed" and "server accepted" is a measurable set rather than a bug report nobody filed. (2) `AppRoot.SetCallbacks` merges by key with LAST-WRITER-WINS and subs Start alphabetically — a second sub registering `onPanelChanged` would have silently unplugged the audio layer's panel whoosh. Where a signal is slow (a panel lives for seconds), POLL the getter on an existing throttled loop instead of contending for the key. | EatACake `MatchmakingPanel.lua`/`ShopPanel.lua` observation props, `AnalyticsSubsClient.lua` panel poll; the template has the same single-callback-table AppRoot | pending |
| 2026-08-02 | feature | **A headless scenario can verify instrumentation that CANNOT run in Studio.** Analytics events are server-only and published-place-only, so every call throws in Studio — the one subsystem that is completely untestable where everything else is verified. Running the real Sink/Session/Ingest under the Luau CLI against a recording AnalyticsService stub asserted 53 behaviours that no amount of reading would catch: budget exhaustion, priority reserves, 40-taps-into-one-event coalescing, the client trust boundary, teleport continuity, stall/abandon timing, recurring-funnel attempts, and the unpublished-place shutdown. The harness changes that made it possible are themselves generic: per-scenario module sets, a per-module `script` proxy (so a module reaching SIBLINGS via `script.Parent:WaitForChild` resolves), fake Players, and — the load-bearing one — a **`typeof` shim**, because the CLI's builtin answers "table" for stand-ins while real code guards on `typeof(x) == "Instance"`; without it every such guard refuses silently and a whole subsystem "passes" by doing nothing. | EatACake `tools/headless-sim/{analytics_scenario.lua,build_sim.py,harness_head.lua,roblox_stub.lua}`; the template ships headless-sim with a single hardcoded module list | pending |
| 2026-08-02 | fix | **A per-visit funnel must reset its seen-set on its FIRST step, or it reports one visit forever.** Deduping funnel steps per attempt is necessary (Roblox ignores a repeated step but still charges it against the rate limit), but the seen-set has to be scoped to the ATTEMPT. Keying it per player meant the first shop visit's steps suppressed every later visit's, so the shop/gym/upgrades funnels would have shown one visit per session and a meaningless conversion rate. The fix belongs in the funnel engine — landing on a `visit`-mode funnel's first declared step opens a new attempt — not in every caller, which will eventually forget. Caught by a catalog cross-check script, not by review. | EatACake `Analytics/Session.lua` `Funnel()`; generic to any recurring-funnel implementation | pending |
| 2026-08-02 | fix | **Telemetry in a HOT LOOP must accumulate, not log — and its own drop-report is the easiest thing to get catastrophically wrong.** Three defects from one adversarial review, all generic. (a) A gym drain running at 8 Hz logged 2 events per tick: 16/s for ONE player against a whole-server allowance of ~2.5/s, so a ten-second burn emptied the budget and starved every funnel step on the server for the rest of the minute — the instrumentation causing the exact undercount it existed to prevent. Rule: any per-tick metric accumulates and emits once per bounded EPISODE (and flooring once at the end also stops sub-1 deltas logging `amount = 0`). (b) The "we dropped N events" report handed `LogCustomEvent` a POSITIONAL array where every other path built a `CustomField0N` dict; Roblox's reflection throws on that cast, and three throws trip the "unpublished place" disable — so the honesty mechanism would have switched all telemetry off two minutes into every busy server, blaming Studio in the warn. (c) The same report cleared its counters BEFORE checking it could send, and drops only happen when the budget is empty — so it was discarded exactly when it mattered. It must RESERVE a token and keep the counts if it cannot send. | EatACake `Analytics/Sink.lua` ReportDrops, `BodySubs.lua` beatBank/flushBank; generic to any budgeted telemetry sink | pending |
| 2026-08-02 | contract | **Never let a client-supplied string become a `Log.Once` KEY — and length-clamping is not cardinality-clamping.** Two unbounded-growth holes in one ingest path, both from an unauthenticated remote. (a) `Log.onceFired` is a module table that is never cleared, so keying a warn on attacker-chosen text is unbounded server memory AND an unbounded warn stream: one client at the legal rate adds N permanent entries and N `warn()` calls a minute, forever, burying the R8 boot report. Refusal keys must be a fixed string plus, at most, the player's own id — and the offending text must not reach the message either, or the attacker is writing the console. (b) Clamping a field value to 40 chars does nothing about a value that is short but always NEW; Roblox's analytics custom fields share an experience-wide 8000-unique-value budget and everything past it becomes "Other" permanently for the retention window, so one client can collapse every honest breakdown in the game in about half an hour. Cap DISTINCT values per player and fold the excess into a literal `other`. | EatACake `Analytics/Ingest.lua` `refuse()`/`bounded()`, `AnalyticsConfig.beat.maxDistinctValuesPerPlayer`; generic to any high-volume client→server telemetry remote | pending |
| 2026-08-02 | contract | **A per-funnel client allow-list is not a trust boundary — allow-list per STEP.** Funnels end in conversion steps (`bought`, `completed`, `won`), so "the client may report shop events" quietly means "the client may report its own purchase". The fix is one line of shape (`{ shop = { open=true, tab=true, card=true } }` instead of `shop = true`), but the lesson generalises to every capability list keyed on a namespace rather than an action. Corollary learned immediately afterwards: TIGHTENING an allow-list silently breaks the callers that were relying on the loose one — the beat is still written, sent and refused, with nothing but one warn to say so. A static cross-check that walks every call site and asserts both directions (key exists in the catalog; key is allow-listed for the side asserting it) caught three matchmaking steps within a minute of the tightening. | EatACake `AnalyticsConfig.clientFunnels`, `Analytics/Ingest.lua`, `tools/headless-sim/catalog_xcheck.py`; generic to any client-assertable event vocabulary | pending |
| 2026-08-02 | fix | **`Net.Remote` in a client module's `Init` can hang the whole bootstrap — and `pcall` does not save you.** `Net.Remote` is `WaitForChild` with no timeout; `pcall` catches errors, not yields. A module resolving a remote in `Init` therefore blocks every later module `Init`, every subscription `Start`, and the `ClientReady` fire the SERVER holds all initial state behind — turning one missing `.model.json` into a client that never finishes loading. Doubly wrong for a telemetry module whose header promises it can never take a gameplay path down. Resolve with `FindFirstChild` + `Log.GraceOnce` (Rojo content can legitimately replicate late), and queue outbound work regardless of whether the remote has arrived yet. | EatACake `LocalAnalyticsService.Init`, `src/shared/Net.lua`; the template's `Net` has the same untimed `WaitForChild` and the same bootstrap ordering | pending |
| 2026-08-02 | docs | **A stub that records arguments verbatim is a test that cannot fail.** 53 green headless checks missed a malformed `customFields` table because the fake `AnalyticsService` stored whatever it was handed; the real engine casts that parameter to a Dictionary and THROWS on a Lua array. General rule for any API stub used as a test harness: it must enforce the CONTRACT the real implementation enforces (types, key shapes, required fields), not merely capture the call — otherwise the harness's coverage number is measuring the stub, not the code. Same class as the `typeof` shim in the same harness: a stub that is too permissive makes broken code look like passing code. | EatACake `tools/headless-sim/harness_head.lua` `assertFields`; generic to every hand-written API stub | pending |
