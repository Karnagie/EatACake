# Upstream queue — candidates for RobloxTemplate

> Capture (rule **U1**): one row per candidate, added as part of the Post-Task
> Checklist. Judging happens at harvest, not at capture — when unsure, capture
> anyway.
>
> **This file holds PENDING rows ONLY.** A settled row MOVES to `ARCHIVE.md`
> with its outcome (`harvested <date> <sha>` / `rejected: <reason>`). Depth is
> the trigger signal (**U1b**) and it can only fall if settled rows leave.

## Row contract

| Column | Rule |
|---|---|
| `ID` | `EAC-NNNN`, sequential, **never reused**. This is how a harvest in the template repo marks the row — never match by claim text. |
| `Date` | capture date, `YYYY-MM-DD` |
| `Type` | `fix` (bug also present in the template) · `feature` (game-agnostic feature/util) · `contract` (API/GUI/logging contract) · `recipe` (pattern used 2+ times) · `research` (evidence from `research-scout`) · `docs` (rule/gotcha) · `tool` · `skill` |
| `P` | `P1` = a defect or missing safety the template ships to **every** future game (harvest these first) · `P2` = a real reusable asset · `P3` = nice-to-have / watch |
| `Claim + detail` | opens with the claim in **bold**, ≤ 120 chars, then ≤ 80 words of detail. **Hard cap 700 chars** — rows written before 2026-08-02 predate this cap and are over it. |
| `Evidence` | the files where it actually ran + why it is generic |
| `Status` | `pending` only |

Depth check (U1b, every task) and harvest triage never read this file:

```bash
grep -c '^| EAC-[0-9]' docs/upstream/QUEUE.md
```

| ID | Date | Type | P | Claim + detail | Evidence | Status |
|---|---|---|---|---|---|---|
` and preserve each line's own `

| EAC-0267 | 2026-08-08 | recipe | P2 | **Texture-audit recipe: render every catalog id at size before shipping; names lie; review sheets by projection-crop.** Audit = texture ids on part TOP faces in labeled 3D grids (in-game tint AND white, exposure −1), shot top-down via screen_capture; candidates via search_asset → LoadAsset decal→image (resolution also exposes candidates that ARE an already-shipped image). CoreGui ScreenGui does not capture; SurfaceGui label text captures as mush — crop tiles from tmp-capture-storage wob files by pinhole projection (known cam/FOV) and label in PIL. Found 16/42 slots were cartoons/product photos. | EatACake flow `2026-08-08_layer-texture-audit.md`; CakeLayersConfig swap verified in Studio; generic to any Roblox art pass | pending |
| EAC-0268 | 2026-08-09 | fix | P1 | **A full-bleed root ScreenGui is only HALF a contract: every surface that PLACES a control still needs its own safe-area inset, and `GetGuiInset()` is the legacy number that under-reports the modern unibar.** TPL-0014 harvested the full-bleed root (so scrims cover the topbar) and shipped the incomplete rule — the template's own overlays place their edge controls by raw viewport fraction. Measured live: the hex tree's calories chip sat half under Roblox's unibar chip (ours y 28.6..89.7 vs chip x 16..204 y 10..58) and its zoom button 43x56 px inside the touch JUMP button. Fix shape: one `Theme.SafeArea` table + one resolver (`max(GetGuiInset().Y, GuiService.TopbarInset.Max.Y)` — TopbarInset is a **Rect**, Max.Y = strip bottom, Min.X = where Roblox's chip ends — clamped, plus a PIXEL pad because a fraction-of-remaining-height margin collapses from 23 px at 1080p to 8 px on a phone), handed down as `…Px` for inset layers and `…01` for full-bleed ones. Two traps for the porter: `Camera.ViewportSize` is `(1,1)` for a session's first frames (divide by it and a control lands 40,000 px off-screen — read the root frame's own AbsoluteSize), and Roblox's touch jump button is sized `min(0.20*shorterAxis,120)` off the SHORTER axis, so a control tuned at one aspect only clears it on a wide window. | EatACake flow `2026-08-09_arrow-until-purchase-safe-area-slide-entrance.md`; measured in a Studio playtest against real CoreGui/TouchGui rects; generic to every Roblox game with a full-bleed root | pending |
| EAC-0269 | 2026-08-09 | feature | P3 | **Staggered ENTRANCE as a kit primitive: a fixed set of sibling cards appearing one after another, ADR-0006-safe — and two things that make the difference between "one after another" and "all at once".** (a) SCALING alone is not an entrance: a pop from 0.68 is fully opaque, so the whole board is legible from frame one and merely grows — each child must be HIDDEN until its turn (React writes a `Visible = false` CONSTANT the entrance then owns, the HintArrow START_HIDDEN pattern). (b) The stagger step must be LONGER than the pop, or several children are in flight at once and the eye reads one event (0.12 s step vs a 0.36 s pop failed a user review; 0.38 vs 0.30 passed) — which also removes the overshoot/gap coupling, since two neighbours can then never peak together. `Theme.Feel.SlideIn` (ClosedScale/Tween/LeadDelay/StepSeconds/TailDelay) + a `useEntrancePop(visible, delay)` hook returning a ref for an `Interaction.pressLayer` UIScale, one effect keyed on `visible` owning and cancelling every tween. Two non-obvious constraints worth carrying: the wrapper is REQUIRED because a bare UIScale scales a top-left-anchored frame out of its layout gaps, and `ClosedScale` is DERIVED from those gaps (Back-out overshoots ~10% of the travel, landing half on each side) rather than chosen — 0.68 → peak 1.032 → 6.8 px per side against a measured 19.4 px gap. | EatACake `src/shared/UIKit/Components/TutorialSlides.lua` + `Theme.Feel.SlideIn`; Studio-verified in slow motion (panels 3/4 and the CTA still mid-pop while 1/2 had settled) | pending |
| EAC-0270 | 2026-08-09 | fix | P2 | **Verifying an animation over the Studio MCP: pick a property the RENDERER does not own, or you will "reproduce" a bug that is not there.** A slowed-down stagger read as totally broken — every UIScale frozen at its closed value, every piece already visible — which looks exactly like a dead effect. Cause: Studio was barely drawing (measured `RenderStepped` 23 ticks/1.5 s against `Heartbeat` 91), so **TweenService was not advancing while `task.delay` was**. Generalises the existing "RenderStepped does not fire while Studio is not drawing" note: anything TweenService-driven is equally invisible, and the tell is a property split — Heartbeat-driven state (a `Visible` flip, an attribute) sequences perfectly while the tweened property does not move. Verify staggers/entrances on the Heartbeat-driven half, and force a frame (`screen_capture`) before trusting any tweened reading. | EatACake flow `2026-08-09_arrow-until-purchase-safe-area-slide-entrance.md`; measured in a Studio playtest; generic to every Roblox project verified over the Studio MCP | pending |
| EAC-0271 | 2026-08-09 | fix | P1 | **An entrance animation that starts at MOUNT plays behind Roblox's loading screen and the player only ever sees the finished screen.** A LocalScript runs long before `game.Loaded`, and a project with no `ReplicatedFirst` loading screen gets Roblox's default one covering everything meanwhile — so a first-session intro that animates on mount is invisible by construction. Reported twice as "they all show at the same time" while the stagger was provably correct (measured 0.55 s apart, in order, in Studio), which is what makes this expensive: every instinct says retime the animation, and retiming cannot fix it. Fix: gate the choreography's clock on `if not game:IsLoaded() then game.Loaded:Wait() end` inside the effect's `task.spawn`, plus a small lead delay so the first beat is not already underway on the first rendered frame. Applies to ANY first-session/first-frame UI motion, and doubly to a teleport destination place. | EatACake `src/shared/UIKit/Components/TutorialSlides.lua` + `Theme.Feel.SlideIn`; generic to every Roblox game without a custom loading screen | pending |
| EAC-0272 | 2026-08-11 | fix | P1 | **A component whose pressable is a CHILD named `HitTarget` reports EVERY press to analytics as the literal id `HitTarget` — one bucket for a whole row of options.** `usePressable` derives the control id from the pressed Instance's Name and prefixes the parent only for names in `GENERIC_NAMES` (Button/TextButton/Content/Frame/Root/**Hit**/Catcher). `MatchChoice` puts `props.name` (`Difficulty_easy`, `Players_2`) on its OUTER CanvasGroup and presses a child TextButton named `HitTarget` — `Hit` is in the list, `HitTarget` is not, so no prefix is added and no per-option id survives. Every difficulty and party-size tap collapses into one id. Fix: pass `analyticsId` explicitly in any component that wraps its pressable in a named outer frame (both new cake components do), and/or add `HitTarget` to `GENERIC_NAMES`. | EatACake `src/shared/UIKit/Components/MatchChoice.lua:49,64-65` vs `src/shared/UIKit/Interaction.lua:122-142` (verified by reading the list, not assumed); the template ships the same component and the same derivation | pending |
| EAC-0273 | 2026-08-11 | contract | P1 | **A fail-closed entitlement check is SAFE on a render path and DESTRUCTIVE on a write path — split "locked" from "could not evaluate".** An unlock predicate returning `false` for both "the player has not earned it" and "the config is broken / a dependency is missing" is correct while it only decides what to DRAW: the card renders locked and self-heals on the next push. The same `false` reused to repair a STORED choice writes into an auto-saving ProfileStore profile — so one typo in the catalogue (an unrecognised rule kind, a string where a number belongs) silently overwrites the saved selection of every player who picked that item, and reverting the config cannot undo the save. Return `(value, evaluated)` and mutate only on an explicit evaluated-false. | EatACake `src/server/lobby/subscriptions/CakeSelectSubs.lua` (`isUnlocked` / `OnProfileLoaded`), found by adversarial review before ship; generic to any unlock/entitlement system layered on ProfileStore | pending |
| EAC-0274 | 2026-08-11 | fix | P2 | **A panel's usable height is its FILL rect, not its nominal grid — budgeting content against the nominal silently buys pixels that are border ring.** A wide panel here is nominal 1000x600, but `PanelWide`'s visible body fill is y 96..573 (`FillPosition 96/600` + `FillSize 477/600`) with the dark border ring at 86..583. Content is parented to the panel ROOT at zIndex 5, so it draws straight over that ring — a content box grown to y..576 and documented as "bottom margin 24" actually overhung the body by 3 nominal px (~5 real px at 1080p), putting a button's bottom edge on the frame. It renders as a subtle misalignment nobody flags, and the check-sum comment "proves" it correct because the arithmetic closes against 600. Fix: derive the floor from `FillPosition.Y + FillSize.Y` and state it in the layout comment; every peer layout in this kit independently stopped at 564 or above, which is the real budget. | EatACake `src/shared/UIKit/Theme.lua` (`MatchmakingLayout` vs `PanelWide`), found by adversarial review, then measured live (fill bottom 573, content 576 before / 564 after); the template ships the same PanelWide geometry and the same "content at zIndex 5 over the border" composition | pending |
| EAC-0275 | 2026-08-11 | fix | P1 | **A custom scrollbar is not touch-complete when only its track handles `InputBegan`; its thumb must own one initiating finger.** `MouseButton1Down` never starts a touch thumb drag, while clearing drag state on any global Touch `InputEnded` lets a second finger cancel the first. Start from `InputBegan`, retain the initiating Touch `InputObject`, and accept movement/end only from it. If Thumb is nested under Track, guard ancestor presses inside its bounds or each grab first jumps the canvas. | EatACake `UIKit/Components/ScrollPane.lua`; adversarial review of the shared template component, verified by input-event contract and compile | pending |
| EAC-0276 | 2026-08-11 | fix | P1 | **Never defer a session-scoped selector reset to `useEffect` while its submit button is live in render.** A mounted-hidden selector committed the previous pad's Hard/4 choices before its effect restored Easy/1; entering a solo-only pad therefore exposed one frame where START could submit an option that was not even offered. Store `{sessionKey,value}` and synchronously read the validated default whenever the stored key differs; keep the effect only for observation/analytics. | EatACake `UIKit/Components/MatchmakingPanel.lua`; adversarial review of the shared template match selector, compile + state-preview verification | pending |
| EAC-0277 | 2026-08-11 | feature | P2 | **One custom scrollbar can support X and Y safely when geometry and input are expressed on one scalar axis.** Keep Y as the default, choose window/canvas extents and pointer coordinates through axis helpers, and derive thumb length/travel from the same ratio. Separate `canvasWidthScale` / `canvasHeightScale` preserves deterministic layouts; axis-specific theme insets avoid silently rotating corner geometry. Reset, track press and the initiating touch all use the selected axis, so behavior cannot drift between two implementations. | EatACake `UIKit/Components/ScrollPane.lua`; Luau/Rojo checks, focused adversarial review, and Studio preview measured exact X maximum and thumb travel | pending |
| EAC-0278 | 2026-08-11 | fix | P1 | **Disabling a modal's X is not a busy lock while its shared tap-outside scrim can still run the close contract.** A launch set busy and disabled the panel, but the global scrim still hid it and sent leave. Configure was already ordered first, so the server rejected that late leave and continued an invisible countdown. Put the busy guard in the named closer shared by X, scrim and future gestures—not only in one button—and keep the modal visible until the operation reaches a cancellable state. | EatACake `AppRoot.closeMatchmaking`; integrated adversarial review traced configure/leave ordering through lobby client and server lifecycle | pending |
| EAC-0279 | 2026-08-11 | fix | P1 | **`Selectable = true` is a false gamepad promise when a GuiButton listens only to `MouseButton1Click`.** Roblox controller A/cross dispatches `GuiButton.Activated`; a mouse-only shared hook made every selected cake, setup button, CTA and close X inert on console. Use `Activated` for the platform-neutral release callback, while avoiding duplicate pointer cue/analytics already recorded on down. Conversely, mark pointer-only scrollbar tracks/thumbs `Selectable = false` until they have a controller action, or navigation lands on dead chrome. | EatACake `UIKit/Interaction.lua` + `ScrollPane.lua`; official Roblox GuiButton API and integrated accessibility audit | pending |
| EAC-0280 | 2026-08-11 | fix | P1 | **A transparent pointer-capture surface over interactive scroll content must classify the gesture before dispatch, or card dragging leaks pointer-down sound, analytics, and activation.** Capture mouse/touch above the cards, claim a drag only after a threshold, and dispatch a normalized canvas point only for a confirmed tap: live tap cues/activates once, locked/busy tap reports dead once, drag reports nothing. Keep the surface `Selectable = false` and retain the real buttons underneath for controller navigation/activation. This reuses `HexTreeOverlay`'s proven input-capture-layer pattern rather than replacing semantic buttons with inert card art. | EatACake `UIKit/Components/ScrollPane.lua` (`contentDrag`) + `MatchmakingPanel.lua`; exact rail geometry/reset verified in Studio fallback and Luau compiled; Play-mode injected gesture still pending | pending |
| EAC-0281 | 2026-08-11 | fix | P1 | **Logical modal visibility and rendered visibility diverge during an exit tween, so disabling only on unmount leaves a live invisible-action window.** When `visible` flips false, a panel shell may deliberately keep drawing until its close animation completes. Disable every semantic/controller target on that same render and add a transparent non-selectable blocker above the still-visible panel; otherwise a late tap can mutate state or launch work after the modal has logically closed. Gesture surfaces must also clear any pointer owner on both sides of an interaction-state transition, or a press begun disabled can release live. | EatACake `UIKit/Components/MatchmakingPanel.lua` exit blocker + `ScrollPane.lua` transition cleanup; adversarial review traced the failure; Studio fallback measured blocker Z105 above every disabled control at 50ms mid-close and clean restoration on reopen | pending |
| EAC-0282 | 2026-08-11 | recipe | P2 | **Decorative plates inside an already-framed control should be style-optional, not deleted globally.** Landscape card Faces already communicate one hit target; extra art wells and reward pills read as nested buttons. Keep the parent/hit target, expose a narrow style flag, and provide direct-content geometry. This preserves portrait variants that still need normalized art windows. | EatACake `CakeCard.ShowArtPlate` + `MatchDifficultyChoice.ShowRewardPlate`; Studio verified matchmaking plates absent and portrait plates retained | pending |
| EAC-0283 | 2026-08-11 | fix | P2 | **A material squish multiplier is inert unless the deformation path actually consumes it.** Declaring `squishMult` in material config made surfaces look tunable, but the renderer used one fixed displacement. Consume it at the render boundary and assert the behavior; gate rollout per variant when legacy materials already shipped under the uniform fallback, or activating the field silently retunes them all. | EatACake `CakeLayersConfig.lua`, `CakeConfig.lua`, `CakeRenderer.lua`, and `cake_variant_scenario.lua`; exposed while implementing the soft rainbow cake and generic to configurable material feel | pending |
| EAC-0284 | 2026-08-11 | research | P2 | **Young-player item selectors should be art-first galleries; repeated metadata rows turn recognition into reading.** Roblox recommends highly visual UI for younger players and grid layouts for item collections, while tables suit tabular data. Use equal picture-first tiles, one compact state glyph, and a single contextual area for long requirements; a clipped next row can teach direct scrolling without extra chrome. | Roblox `design-for-roblox` + `grid-table-layouts`; EatACake `MatchmakingPanel` gallery, researched against Pet Simulator 99 and Adopt Me, then Tonal/Squint reviewed | pending |
| EAC-0285 | 2026-08-11 | fix | P1 | **Global pointer continuation for reusable gestures must be subscription-owned, not one engine connection pair per mounted component.** A drag may need move/end after leaving its GuiObject, but connecting `UserInputService` inside each React pane violates R4 and scales listeners with mounts. Use one client subscription to fan inputs through a callback bridge; components register plain callbacks and use declarative `React.Change` for GUI properties. | EatACake `UIKit/InputBridge.lua`, `UiInputSubsClient.lua`, `ScrollPane.lua`; fixes copied UIKit architecture while preserving release-outside behavior | pending |
| EAC-0286 | 2026-08-11 | fix | P2 | **A tiny launch catalogue should show every current choice; let only FUTURE overflow teach scrolling, and share one incomplete-row origin between drawing and hit testing.** Centering/clipping the third of three items made it read as a dangling bonus and forced children to discover a hidden launch choice. Size the first row to the shipped catalogue, reserve the clipped continuation cue for row two, and route both card placement and tap classification through one row-origin helper so a visual alignment change cannot create ghost hitboxes. | EatACake `MatchmakingPanel.galleryRowX` + `Theme.MatchmakingLayout`; three cards measured equal/same-row in Studio, Tonal/Squint 72.9 with zero criticals | pending |
| EAC-0287 | 2026-08-11 | fix | P1 | **A rendered busy prop is one commit late, so operation locks must be synchronous and cover every alternate transition path.** Set a per-session ref before dispatching a launch, consult it in repeat-submit, choice, pointer/controller, and close callbacks, and release it only after authoritative busy has been observed and completed. Named closers alone are insufficient when background controller focus or world triggers can replace the modal; those routes must consult module-current busy state too. | EatACake `MatchmakingPanel.launchLockRef`, `AppRoot.currentMatchmakingBusy`, and chocolate Shop trigger; adversarial multitouch/controller trace, Luau/Rojo checks, and active-Studio source sync | pending |
| EAC-0288 | 2026-08-11 | fix | P1 | **A submit handler must not read render-captured form choices when another pointer can change a choice and submit before the concurrent redraw commits.** Keep rendering in React state, but mirror the exact session-keyed submission payload synchronously in each choice handler; submit validates and reads that mirror. Otherwise the UI may redraw to Hard/3 while the operation already launched the prior Easy/1 pair. | EatACake `MatchmakingPanel.choiceRef`; adversarial multitouch trace, targeted Luau compile, and exact two-argument contract review | pending |
| EAC-0289 | 2026-08-12 | fix | P2 | **For TextScaled labels, omit `TextWrapped` unless true; explicit false can collapse text toward default TextSize.** A shared text primitive adding optional wrapping first wrote false to every old label: headings, choices and START all shrank in the live capture although their boxes were unchanged. Use `TextWrapped = if optIn then true else nil`, so React leaves established single-line behavior untouched while selected blocks may wrap. | EatACake `UIKit/Components/OutlinedText.lua` + `CakeCard.lua`; before/after Studio capture exposed the regression; generic to shared Roblox text primitives | pending |
| EAC-0290 | 2026-08-12 | recipe | P2 | **Compact outlined text must budget shadow offset and both stroke envelopes, not only its label frame.** A nominally contained count still painted onto the button lip because `OutlinedText` offsets its shadow by 10% of label height and both main/shadow labels add strokes. Solve the visible envelopes against the raised Face, then place adjacent icon art in the remaining space. | EatACake `Theme.MatchPartyChoice`; exact main/shadow stroke envelopes documented beside the tokens and verified in the live matchmaking render; generic to compact outlined controls | pending |
| EAC-0291 | 2026-08-12 | fix | P2 | **New optional style tokens need neutral defaults at the component boundary because old layout tables cannot inherit fields.** Adding a pulse-headroom multiplier to the default Theme made custom layouts render-error when the component multiplied and divided by their missing token. Resolve once to `token or 1` and use that value symmetrically, preserving old geometry. | EatACake `UIKit/Components/MatchmakingPanel.lua`; adversarial legacy-layout review found the nil arithmetic, followed by 233-file parse and three builds | pending |
| EAC-0292 | 2026-08-12 | contract | P2 | **A Studio content override must be separate from the production default and lose to explicit match metadata.** Use one data-configured variant id only for Studio direct/fallback launches, validate it through the normal catalogue, warn when active/invalid, and leave authoritative teleport payloads untouched. This makes deep content directly testable without changing saved choices or accidentally shipping a different public default. | EatACake `CakeConfig.studioVariantId`, `CakeCycleService`, `GameRoundService`, and both-path headless scenarios; generic to games with lobby-selected content and a separate play place | pending |
| EAC-0293 | 2026-08-12 | fix | P1 | **A fixed-topology heightfield renderer must isolate sampling and display state per local mask.** Reusing the maximum footprint for narrower strata averages high in-mask cells with lower out-of-mask cells; connecting those valid averages produces giant diagonal triangles, while a shared ooze cache reintroduces the defect after bites. Carry each stratum's mask into vertex adjacency, rim projection and target/display state; force XZ rewrites when a pooled mesh changes masks, and degrade rather than pretending one slab can express several rims. | EatACake `CakeRenderer` seven-terrace correction; 143/143 real-module checks, 3,064-edge geometry audit and three Rojo builds; generic to stepped terrain/cake/water heightfields that reuse one mesh topology | pending |
| EAC-0294 | 2026-08-12 | fix | P1 | **A sampled state transition must enumerate every crossed boundary, not compare only its old and final bucket.** Several mutations inside one scan can jump across multiple mandatory gates; firing only the final destination permanently skips intermediate work, and a terminal transition may overwrite the one gate that did start. Queue all crossed gated destinations FIFO, consume them sequentially, and refuse the finale until the queue drains. | EatACake rainbow multi-layer clear regression: one 29-layer scan crosses seven zones, runs gates 1–5 in order, then starts the Guardian; generic to checkpoints, staged rewards and sampled progression | pending |
| EAC-0295 | 2026-08-12 | fix | P1 | **A debug state mutator must suppress downstream rewards before it changes authoritative state.** A Studio layer-skip hook was labelled unpaid but exposed buried finds through the normal collection tick, silently crediting +87 gems to the tester's persistent profile. Arm a data-owned, bounded suppression latch before the mutation; preserve visuals, explicitly skip reward/progress/analytics/save paths, test zero side effects, and reset the latch at the next natural lifecycle boundary. | EatACake `DebugClearLayer` + `debugSuppressFindRewards`; real-subscription headless test proves visuals remain while five mutation counters stay zero | pending |
| EAC-0296 | 2026-08-12 | fix | P2 | **Code-positioned authored geometry must derive its anchor from the resolved clone's actual extent.** Computing a platform centre from configured width and later sizing its inner edge/legs from the authored Part or Model makes any Studio resize shift the functional edge into or away from gameplay. Resolve first, measure `BasePart.Size`/`Model.GetExtentsSize`, then calculate the anchor; keep config only as a missing-asset fallback. | EatACake `MapService` checkpoint centre; focused adversarial geometry review and three Rojo builds | pending |
| EAC-0297 | 2026-08-13 | fix | P1 | **A modal scrim closes the POINTER boundary only — controller selection is a second channel, and one kit button never set `Selectable`.** Every pressable ties `Selectable` to `enabled`; `HudMenuButton` set it nowhere, so its TextButton defaulted to true. A D-pad could reach a HUD button under an open panel's own scrim — which is deliberately `Selectable = false` precisely so focus does not land on it — and activate it. Worst case here: swapping `openPanel` out from under a window the player never closed and arming a camera freeze + movement lock behind it. Fix: set `Selectable` on every kit pressable, and have any coverable HUD pass `selectable = <no panel open>` down. | EatACake `UIKit/Components/HudMenuButton.lua` vs `Button/ShopCard/EatButton/PriceButton`, found by adversarial review; the template ships the same component and the same scrim contract | pending |
| EAC-0298 | 2026-08-13 | fix | P1 | **A gamepass perk reaches a player THREE ways and only one is an attribute — re-applying that half leaves every PUSHED snapshot stale until the next place transition.** READ-per-use stats need nothing. PUSHED snapshots (pet `slots` on PetsUpdate, `capacity` on StomachUpdate) and APPLIED values (Humanoid WalkSpeed) do not re-derive themselves, so a VIP buyer saw '3 / 3' slots and an un-doubled belly bar all session. Same bug on JOIN: the ownership fetch YIELDS on its first `UserOwnsGamePassAsync`, so every other PushInitialState hook already ran against an EMPTY cache. Fix: one `pushDerivedPerks` used by both entry points, reusing the boost re-derivation routine, not a second copy. | EatACake `PassOwnershipSubs` + `BoostSubs.Apply` + `ShopSubs.PromptGamePassPurchaseFinished`; the template ships the same pass/perk split | pending |
| EAC-0299 | 2026-08-13 | fix | P1 | **A join-time ownership fetch must never downgrade a runtime `true` to `false`: it yields for seconds, and `UserOwnsGamePassAsync` is eventually consistent right after a purchase.** The fetch retries 3x with backoff per pass (up to ~4.5 s each, 6 passes) and then writes every key into the same table `PromptGamePassPurchaseFinished` writes the truth into. A purchase completing inside that window is not visible to the query yet, so the blind write turns the paid perk OFF for the session — attributes cleared, snapshots re-pushed un-perked, the cell flipped back to unowned. Nothing legitimately revokes a pass mid-session, which is what makes `if owns or map[key] ~= true` safe. | EatACake `PassOwnershipSubs.applyOwnership`; latent until buying a pass mid-run became routine; generic to any eventually-consistent entitlement cache | pending |
| EAC-0300 | 2026-08-13 | contract | P2 | **An attention PULSE's second UIScale goes on the ROOT only when the component owns its Position — under a `UIGridLayout` it must ride an inner centre-anchored frame.** The kit's shipped recipe (Button, HexNode) puts the looping UIScale on the root TextButton, which grows about its AnchorPoint. A grid cell's Position is owned by the layout, so AnchorPoint is (0,0) and the root recipe swells the icon down-and-right into its neighbours instead of breathing in place. Nest a full-size centre-anchored frame INSIDE `pressLayer`'s Content and put the pulse UIScale there: the two scales compose (press pop x attention breathe) and every zone fraction inside is unchanged. | EatACake `UIKit/Components/HudMenuButton.lua` `pulse`, measured live (`PulseScale` 1.0722 mid-breath, neighbours at 1.0000); the template ships the same Button/HexNode recipe and the same grid menu | pending |
| EAC-0301 | 2026-08-13 | fix | P2 | **Making a screen CHEAP to open changes its analytics cost model: per-open beats that do not coalesce can burn the whole per-player admission.** One panel open/close is `panel` open + `panel` close + a funnel step — none coalescing (only press/dead-press/error are countable, and a per-visit funnel step is deliberately not deduped). Fine when opening the screen cost a walk to a ProximityPrompt; not when it becomes a HUD icon that BREATHES to attract taps, in a game for children. ~2 toggles/s passes a 240 beats/min admission and every REAL beat that player makes is refused for the rest of the minute. Fix: per-panel re-open debounce measured from the last REPORTED open. | EatACake `AnalyticsSubsClient.pollPanel` + `AnalyticsConfig.client.panelReopenSeconds`; verified live (3 open/close cycles in ~2.4 s -> 1 open, 1 funnel, 1 close) | pending |
| EAC-0302 | 2026-08-13 | docs | P2 | **Adding a SECOND entry point makes previously unreachable guards reachable — re-audit every 'this cannot happen' branch on the OLD path.** Two shipped examples from one button. (1) A world-prompt sweep's `map == nil` branch was dead while the only opener lived INSIDE that map; a HUD button opens on the first frame, before the map clone replicates — and leaving the prompts on means the E-to-close press also fires whatever prompt is in range, one of which opens a Robux dialog. Its `Log.Once` also LATCHED, swallowing the real failure forever. (2) An R8 message offering 'use the prompt instead' was false: every early return that nils the callback also skips the prompt connect. | EatACake `UpgradesSubsClient.setCheckpointPromptsEnabled` / `AppRoot` upgrades dispatch, both found by adversarial review; generic to any feature that grows a second opener | pending |
| EAC-0303 | 2026-08-13 | feature | P2 | **A pooled SCREEN-SPACE confetti burst is the cheapest big-celebration FX a simulator can own — and it must be measured in screen HEIGHTS, never pixels.** One extra ScreenGui one DisplayOrder BELOW the React root, N pooled ImageLabels cloned once at Init, and a per-frame integrator stepped from the existing RenderStepped block. Sprites launch from below the bottom edge, arc to mid-screen and fall off. Two rules make it portable: positions/velocities are screen fractions and screen-heights/s (a pixel-tuned arc clears a phone and dies halfway up a monitor), and sprites size on `SizeConstraint.RelativeYY` so a 21:9 monitor cannot stretch them. | EatACake `client/common/modules/FoodBurst.lua` + `JuiceConfig.foodBurst`, live-verified (apex y 0.33-0.34, fully gone in 2.1 s, `ActiveCount()` back to 0); nothing in it is cake-specific except the icon list | pending |
| EAC-0304 | 2026-08-13 | contract | P1 | **Editing an English string in code does NOTHING in game until the cloud table is re-pushed AND pruned — `FormatByKey` returns the CLOUD row, which still holds the old wording.** Measured: after renaming `cake-boss` in `LocaleData.strings`, the live HUD still read the old text for an `en-us` player, while brand-new keys (absent from the cloud) correctly served the new English. Row identity is `(key, context, source)`, so a changed source is a NEW row and the old one lingers as an orphan serving the same key. Any rename task must end with `pull` -> `push --prune`, and the feature doc must say so. | EatACake `LocaleData.T` (translator-first), `tools/robloxloc/robloxloc.py` `ident_of`/`diff`; template ships the identical resolver + workbench | pending |
| EAC-0305 | 2026-08-13 | recipe | P2 | **A randomised UI phrase must travel as a KEY through state, never as resolved text — otherwise it rerolls on every re-render.** A HUD that re-renders at bite frequency (~14x/s) plus a locale-ready repaint will deal a new phrase several times while one banner is on screen. Roll ONCE at the event, store the key, resolve it in a memo keyed on `(key, localeReady)`. The count of each variant list lives next to the strings (`cheerCounts`) so adding a phrase is one file, and the roller takes a fallback key so an empty list degrades instead of printing a raw id (R8). | EatACake `LocaleData.RollCheer` + `AppRoot` celebration memo + `CakeSubsClient.pushCelebration`; generic to any game with repeated beats (wave cleared, level up, combo) | pending |
| EAC-0306 | 2026-08-13 | contract | P2 | **One-shot celebration animation in a kit component: CanvasGroup for the fade, a `seq` identity prop to retrigger, and only React-untouched properties tweened.** `Interaction` covers press/hover; a slam-in / breathe / launch-out timeline had no home. Recipe: CanvasGroup root (one `GroupTransparency` fades everything), the UIScale ON the CanvasGroup so the overshoot cannot be clipped by its own bounds, one `useEffect` re-keyed on `seq` (comparing TEXT swallows two identical events in a row), cancel the infinite breathe before the exit tween, and reset transparency in the cleanup so a cancelled tween cannot strand the splash on screen. | EatACake `UIKit/Components/CelebrationBanner.lua` + `Theme.CelebrationBanner`; ADR-0006 (React must not write a tweened property) is the template's own rule | pending |
| EAC-0307 | 2026-08-13 | docs | P3 | **Two readouts of one number is one too many — delete the world-space bar, keep the HUD one.** The boss carried a billboard HP bar AND fed the HUD's top-centre bar with the same value, so the duplicate sat over the thing the player is meant to watch. The zone-gate monster was worse: HUD bar, billboard bar, AND its own size shrinking with HP. Removing a billboard is not a pure delete — check what else rides it (the gate's plate keeps the ZONE NAME, which nothing else carries, and its clearance math still needs the gui reference) and what still consumes the setter (its `SetHp` still drives the shrink). | EatACake `BossView` / `MiniBossView`, user-requested; the template ships the same two-view boss pattern | pending |
| EAC-0308 | 2026-08-13 | fix | P1 | **A screen-space particle that spawns OFF-SCREEN must only be retired while it is moving AWAY — a position-only exit test deletes part of every burst, more of it on faster hardware.** Sprites launched `spawnBelow` under the bottom edge, which is already past an exit line of the form `y > 1 + margin`, so any sprite whose FIRST integrated frame did not clear the line was killed before it was ever drawn: ~21% of a burst at 60 Hz, 57-63% at 144 Hz, silently, and never reproducible on a locked-60 dev machine. Gate the test on the velocity sign. Second half of the same class: `ScaleType.Fit` on a deliberately non-square frame throws squash & stretch away (it draws at the shorter side, preserving the SOURCE aspect) and shrinks every sprite ~25%. | EatACake `FoodBurst.Step`/`Init`, both found by adversarial review, re-measured at 144 Hz after the fix (27-28 of 22-28 drawn, was ~11) | pending |
| EAC-0309 | 2026-08-13 | contract | P2 | **A one-shot entrance pose MUST be written in `useLayoutEffect`; a passive `useEffect` paints the element at its property DEFAULTS for a frame first.** React commits a fresh CanvasGroup/UIScale at Scale 1 and GroupTransparency 0 — i.e. full size, fully opaque — and flushes a passive effect only after that frame paints, so a "slam in from 0.35" animation flashes full-size first. Worst on a beat that repeats (28-42 times per run here). The template's kit already uses the layout effect in four components and documents the symptom in two, but nothing states the RULE where a new component author would read it: it belongs in the ui-kit skill's motion section next to ADR-0006. | EatACake `UIKit/Components/CelebrationBanner.lua`; template ships `TutorialSlides`/`Badge`/`Toggle`/`ScrollPane` with the fix and no rule | pending |
| EAC-0310 | 2026-08-13 | docs | P2 | **`UIAspectRatioConstraint.DominantAxis` does nothing unless `AspectType = ScaleWithParentSize` — at the default `FitWithinMaxSize` BOTH axes bound and the smaller wins.** A component passing `DominantAxis = Width` and a Theme comment saying "width is what sizes it" were both wrong: height was binding, and raising the width constant would have changed nothing. Measured live (banner 467x134 in a 1003x482 viewport: 134 = the height fraction, 467 = 134 x aspect). The template's Theme already documents the FitWithinMaxSize behaviour for Button, so the trap is known and the wrong pattern still shipped — worth a line in the kit's style rules and in whatever component passes DominantAxis. | EatACake `UIKit/Components/CelebrationBanner.lua` + `Theme.AppHud.Celebration*`; template ships the same constraint idiom in Button/Card | pending |
| EAC-0311 | 2026-08-14 | feature | P2 | **A global leaderboard needs one OrderedDataStore service that is a write-only PROJECTION of the profile — that is what makes republishing idempotent and keeps session locking intact.** ProfileStore cannot rank across servers (session-locked, no sort). One store per board keyed by userId, written only FROM profile values and never read back, so a repeat/late publish is a duplicate write and nothing else, and a `min`-style stat cannot be made worse out of order. Template ships no leaderboard at all and its P5 forbids DataStoreService outright — it needs the carve-out written down, not discovered per game. | EatACake `GlobalLeaderboardService`/`Data`/`Subs` + ADR-0022, live in the lobby place; generic except the three stat ids |
| EAC-0312 | 2026-08-14 | fix | P1 | **Never publish a ZERO to a leaderboard: on an ASCENDING board every account that has never scored ties for FIRST place, permanently.** A `bestTime = 0` default means "no record", and an ordered store has no null — so a naive publish puts every fresh player above the actual record holder and the board is wrong from the first join. Drop non-positive values at the publish boundary; absence IS the no-score state. Same trap for any "lowest wins" stat (deaths, attempts, time). The template has no leaderboard yet, so this belongs in whatever leaderboard recipe/feature it gains. | EatACake `GlobalLeaderboardService.Publish`; proven live — a joining player's three zeroes wrote nothing, one 25-gem code then published exactly one value |
| EAC-0313 | 2026-08-14 | contract | P1 | **Do NOT flush per-player state on `PlayerRemoving` when another subscription unloads the profile on the same event — handler order is not a contract.** The obvious "save their final numbers on leave" hook races `PlayerLifecycleSubs`' `PersistenceService.Unload` and reads a nil profile some fraction of the time. Do the work on the next profile LOAD instead: the profile that loads carries exactly what the last session ended with, so coverage is identical with no race. Generic to any leave-time flush (analytics totals, leaderboards, session summaries) and the template's lifecycle sub is where the trap lives. | EatACake `GlobalLeaderboardSubs` (publish on `OnProfileLoaded` + tick, deliberately not on leave) vs template `PlayerLifecycleSubs.PlayerRemoving` |
| EAC-0314 | 2026-08-14 | docs | P2 | **A child's `Size`/`Position` SCALE inside a `ScrollingFrame` is relative to the CANVAS, not the window — so a row template's scale silently changes height when you resize the canvas for N rows.** Derive both from the authored template instead of hardcoding: `rowFraction = template.Size.Y.Scale * authored canvas.Y.Scale`, then `canvas.Y = N * rowFraction` and `row.Y = 1/N` reproduces the authored row for any N. Two more clone-only fixes in the same breath: a `UIListLayout` writes `Position`, so an `AnchorPoint` of 0.5,0.5 shifts every item half its size up-left, and dynamic text (names, scores) needs `AutoLocalize = false` or the cloud table rewrites it. | EatACake `LobbyLeaderboardSubs.buildRows`, measured live: rows exactly 36.0232 px, canvas returns to the authored {5,0} at N=50 |
| EAC-0315 | 2026-08-14 | fix | P1 | **A DEBUG/QA shortcut that skips real play must be gated out of every PERMANENT record, not just the obvious ones — a `min`-style record set from a debug run can never be displaced.** A latch already suppressed find/analytics writes for a debug-skipped level, and the new best-time write simply did not check it: Studio runs against the LIVE store, the shortcut reaches the end in ~1 min vs ~35, and an ascending leaderboard keeps the minimum forever — only wiping the store removes it. Rule: when adding ANY write to a completion path, grep for the project's existing debug-suppression latch and honour it. Generic to any template with a QA skip hook. | EatACake `CakeCycleSubs.rewardPlayers` vs `CakeSimulationSubs` `debugSuppressFindRewards`; caught by adversarial review, not by testing |
| EAC-0316 | 2026-08-14 | contract | P1 | **A batched remote/store write must report WHICH items committed, not HOW MANY — a count makes a partial failure indistinguishable from success and strands the lost item.** Returning `written > 0` and then marking the whole batch as sent loses any value that does not change again on its own (a first-ever personal record never does). Return a `{ [id] = true }` committed set and mark only those. Generic to every write-through cache / dirty-flag / batched-publish shape, which the template's push hooks and any future leaderboard both are. | EatACake `GlobalLeaderboardService.Publish` + `GlobalLeaderboardSubs.publishPlayer`; adversarial review |
| EAC-0317 | 2026-08-14 | fix | P2 | **A `useMockInStudio`-style sandbox flag must cover EVERY DataStore user, or it silently half-applies the day someone flips it.** The template's persistence flag promises "nothing is written to live DataStore keys" in Studio but is read only by the profile layer; any second store added later (leaderboards, analytics rollups, global counters) writes live while the profile is mocked — worst case a debug-shortened run poisoning a permanent public record. Make the flag a shared predicate the template exposes, not a field one service reads. | EatACake `GlobalLeaderboardService.mockGuardActive` vs template `PersistenceData.useMockInStudio` / `PersistenceService` |
| EAC-0318 | 2026-08-14 | contract | P2 | **A busy latch guarding an async tick needs a start STAMP and a force-clear, or one wedged pass stops the feature forever while the console stays quiet after the first line.** `if busy then Log.Once(...) return end` is the natural shape and it converts a transient throttle into permanent silence — exactly the silent-skip R8 exists to prevent. Stamp `busy-since`, and past N periods warn and clear. Also: resolve a per-collection warning ONCE per collection, never inside the item loop (a renamed label warned 50x per board, 150x per boot, burying the boot report). | EatACake `GlobalLeaderboardSubs.publishTick` / `LobbyLeaderboardSubs.refreshTick` / `Rows.valueLabelName`; adversarial review |
