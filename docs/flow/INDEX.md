# Flow index

> One line per completed task — append on every task (D2). Agents: find rows
> whose tags match your feature, open at most those 1-2 docs. Never read the
> flow/ directory wholesale.

| Date | Doc | Tags | Summary |
|---|---|---|---|
| 2026-07-11 | `2026-07-11_schema-driven-persistence.md` | persistence, bootstrap, skeleton | ProfileStore vendored, schema sections, repo skeleton, ADR-0001 |
| 2026-07-12 | `2026-07-12_daily-rewards-economy.md` | daily-rewards, economy, reward-grants, net | First feature triads, grant registry (ADR-0002), ClientReady handshake, respawn-proof client |
| 2026-07-12 | `2026-07-12_console-transparency.md` | logging, bootstrap, persistence, ui | Log layer + rule R8, DataStore-state report, GraceOnce for late GUI |
| 2026-07-12 | `2026-07-12_agent-docs-optimization.md` | docs, map, registries, flow-index | MAP → routing index, registries → uniqueness indexes, flow INDEX, D1-D3 rewritten |
| 2026-07-12 | `2026-07-12_task-shaped-agents.md` | agents, tooling, review, verification | 3 task-shaped subagents: scout, adversarial reviewer, studio-verifier (R8 boot contract) |
| 2026-07-12 | `2026-07-12_self-improvement-loop.md` | self-improvement, upstream, agents, template | U1-U4 rules, upstream QUEUE, harvest gates, changelog, research-scout |
| 2026-07-16 | `2026-07-16_feature-library-batch.md` | time-rewards, group-reward, shop, promo-codes, settings, app-root, economy, ui-kit | Feature library: time/group/shop(+passes)/codes/settings-persistence + composed AppRoot (HUD, badges); 28-finding review pass incl. 2 money bugs |
| 2026-07-15 | `2026-07-15_ui-kit-port.md` | ui-kit, ui, skill, packages, bootstrap | Candy-style ReactRoblox UI kit (Theme + 16 components + demos), UiRoot, npm react deps, roblox-ui-kit skill, kit-first UI workflow |
| 2026-07-15 | `2026-07-15_ui-skill-design-step.md` | ui-kit, skill, design | Skill rebalanced after shop-clone failure: mandatory design step, window-archetypes reference (genre structures + worked Shop), list-in-ScrollPane pattern, inventing components declared the norm |
| 2026-07-15 | `2026-07-15_settings-window-react-check.md` | settings, ui-kit, react, packages, bootstrap, r8 | React vendored as ReactLua-Packages.rbxmx (replaces npm/optional-path that infinite-yielded); first real kit feature: startup Settings window (Music / Sound Effects) as React smoke test |
| 2026-07-15 | `2026-07-15_ui-skill-method.md` | ui-kit, skill, design, method | method.md: the author's actual process as mandatory phases — written brief, check-sum zone arithmetic, ratio-transfer element derivation, render-skeleton-first, visual iteration loop (2-4 passes normal), ship gate with iteration report; SKILL.md rewritten (NUL cleanup + phase routing) |
| 2026-07-15 | `2026-07-15_outlined-text-uistroke.md` | ui-kit, text, performance | OutlinedText: 8 clone labels → 1 shadow copy + scaled UIStrokes (StrokeSizingMode ScaledSize, 0.08 main / 0.06 copy, color 27,42,53 as Colors.TextOutline); legacy outline props ignored; callers cleaned; Studio verify pending |
| 2026-07-16 | `2026-07-16_eat-the-cake-v1.md` | cake-sim, cake-cycle, treasures, body-gym, upgrades, pets, rebirth, quests, juice, economy, app-root, map | Eat the Cake v1: heightfield cake + full GDD loop (eat→gym→upgrades→boss→pet→new cake), 12 kit components, Studio-verified end-to-end; ADR-0003/0004; template AppRoot nil-clear bug found+fixed |
| 2026-07-17 | `2026-07-17_cake-grounding-fixes.md` | cake-sim, map | Floating cake (MeshPart renders raw mesh coords from part origin — no bbox recentering), corner see-through slits (analytic ring → 3×3-neighborhood faces), candles moved out of the loaf; "Precise freezes edits" conclusion REVERSED (Automatic's stale LODs were the bug) |
| 2026-07-18 | `2026-07-18_layer-meshes-crust-feel.md` | cake-sim, juice, config | One MeshPart per layer via lazy FixedSize-clone pool (dynamic EM reserves worst-case budget — root cause of laptop fallback), crust+cracks painted into per-layer textures, NEW CakeFeelSubsClient (jumpMult/bounce/landing cracks), degradation ladder slabs→palette→keycaps, thicker layers; live-verified |
| 2026-07-18 | `2026-07-18_wax-shell.md` | cake-sim, config, juice | Always-visible wax SHELL (CakeWaxShell): one MeshPart of organic Voronoi pieces coating the whole cake, riding the surface, SMOOTH at rest, cracking (dent+tilt+spread, cake shows) only underfoot, hiding where eaten, clipped clean to the loaf edge; slabs now render body; async-build re-entry guard; live-verified |
| 2026-07-18 | `2026-07-18_eat-in-front-ball-roll.md` | cake-sim, body-gym, juice, config | Eat the cake IN FRONT (dropped pointer aim); LOCAL hand-piece eat gesture (cake→hand→mouth Bézier + Motor6D chew arm, speed ∝ eat-rate) `EatGestureController`; full belly BLOCKS eating (`StomachService.IsFull` + client gate, glutton moved to the filling bite) and morphs the rig into a rolling cake BALL `BallRollController` (LocalTransparencyModifier cross-fade, physics-true roll, visual only) |
| 2026-07-19 | `2026-07-19_checkpoint-platform.md` | checkpoint, body-gym, cake-sim, map, ui-kit, net | Return-to-gym CHECKPOINT: platform on 4 legs beside the cake, top tracks the current layer (steps down via ScanStats topBandIndex), carries the gym (floor zone removed); F key + HUD "BURN FAT" button → server-authoritative `ReturnToCheckpoint` teleport |
| 2026-07-19 | `2026-07-19_hex-upgrade-tree.md` | upgrades, ui-kit, checkpoint, map, persistence, stats, hexutil | Hexagon TIER tree (honeycomb overlay) replacing the flat upgrades panel: 3 nested categories (eating/body/gym) of tier-chains, opened from a checkpoint "computer" ProximityPrompt (E/Close), states gray/gold/blue + hover tooltip. Progression repackaged 15-40 linear levels → ~5 tiers/stat (value=old formula at boundary, cost=Σ replaced levels), UpgradesSection v2 migration; hex sprite + HexUtil axial math; ADR-0005 |
| 2026-07-19 | `2026-07-19_layer-gate.md` | cake-sim, cake-cycle, config, ui | Eat top-down ONE layer at a time: bites clamp to the active band's floor (server-authoritative `activeFloorUnits`, advances via `ScanStats` topBandIndex/auto-sweep); client mirrors the floor in prediction + pops "Eat the top layer first!" (`announce-layer-locked`) when aiming at a locked lower layer; `CakeConfig.layerGate` flag; `activeBandIndex` on snapshot meta + `CakeCycleUpdate` |
| 2026-07-19 | `2026-07-19_gym-fat-drain-rework.md` | body-gym, upgrades, gym, checkpoint, ui-kit, economy | Fat burning: timed MASH → fill-DRAIN session. Press prompt → belly drains from a captured baseline (`burned01` 0..1, belly=start×(1-b)), banking `floor(startStored×gymEff×b)` monotonically; passive `burnSpeed` + per-tap `burnPerTap` (base 0.10=10 taps) advance it, `instantBurn` seeds it (final tier=whole belly on press); leaving `NearGym` STOPS it. 3 new gym upgrades; overlay tap button moved right (phone thumb) + eased "fat left" bar; HUD "BURN FAT" → "TO CHECKPOINT" |
| 2026-07-19 | `2026-07-19_easy-mode-balance.md` | cake-sim, cake-cycle, upgrades, body-gym, config, map, economy | Easy-mode retune (sim-grounded): fix "1 bite = full" via capacity base 150→2600 (belly = ~50–160 bites) + flattened eating power (biteDepth 26→1.8, ~4× total not ~2000×) so eating is ~94% of playtime, not running. Bigger loaf (28/19→30/26, 90×78 studs, maxHeight 90) + NEW `perPlayerScale` co-op height lever wiring the ignored `playerCount` → solo ~40 min, 4p ~18. Cake tray + candles moved for the bigger footprint. No profile migration (tier count unchanged) |
| 2026-07-19 | `2026-07-19_hud-juice-and-menu-grid.md` | ui-kit, app-root, ui, juice | Left icon menu → 2×4 `UIGridLayout` (config `MenuColumns`) so it stops mid-screen; kit-wide press/tween feedback from one primitive `Interaction.usePressable`/`pressLayer` (center-origin `Content`+`UIScale` bounce), panel open/close pop (`PanelShell`), badge pop-in, belly/cake fill glide, settings toggle knob slide. `Theme.Feel` holds all timings; ref-owned-tween-vs-React contract (ADR-0006); Studio-verified |
| 2026-07-19 | `2026-07-19_hud-ui-polish.md` | app-root, ui-kit, checkpoint, body-gym, ui | HUD polish: cake % bar hidden during eating (kept for boss/spawn/reward via CakeBar `visible`); left menu → bare icon+label-below buttons, no background (new `HudMenuButton` + `Theme.HudMenuButton`, placeholder icon), bigger for phones; "TO CHECKPOINT" button proximity-gated (BodySubsClient reads the live plate footprint, pushes `checkpointFar` on change) |
| 2026-07-19 | `2026-07-19_cake-rebuild-mobile-spike.md` | cake-sim, performance, mobile, editable-mesh | Mobile ~86 ms/new-cake CPU spike: the per-band cake `EditableImage` was VESTIGIAL (uniform `bodyColor` fill since crust moved to CakeWaxShell) yet cost a 384² paint + GPU upload per band per rebuild AND a `DrawRectangle` per eaten cell per bite. Multi-band slabs now render flat `part.Color`; only the palette fallback keeps a texture. Studio-measured: non-first rebuild ~153 ms→**6–7 ms**, per-bite texture churn gone; visuals identical (verified). Studio-only 1 Hz server bleed noted as a non-issue for real clients |
| 2026-07-20 | `2026-07-20_editable-scene-assets.md` | map, checkpoint, assets, mapservice | Static scene (candy room + checkpoint) moved from procedural `Instance.new` build to CLONING editable place-authored models in `ReplicatedStorage.Assets` (Environment + Checkpoint); `MapService.GenerateAssets` self-heals defaults, `Build` clones + resolves by name/`BiomeRole` attribute, checkpoint stays dynamic (named parts positioned by code), biome recolor opt-in. Live-verified: clones identically, edits flow through, opt-in recolor, checkpoint functions. ADR-0007. User saves the place to persist assets |
| 2026-07-20 | `2026-07-20_mobile-eat-spike-physics.md` | cake-sim, performance, mobile, physics, collision | Mobile eat spike (>50 ms, recovers 5–10 s) ROOT-CAUSED to physics: eating resized hundreds of CanCollide collision-column parts across the whole cake per frame → physics broadphase re-index (`Workspace.Heartbeat` 60 ms). Fix: `updateCollisionNearPlayer` refreshes only columns within `updateRadiusStuds` of the player (distant ooze needn't move any collider); mesh still updates everywhere. Physics 60.6 ms→**6.4 ms**, no fall-through. Confound found: 99.96% belly blocked bites — drained at the gym to profile real carving. Residual Studio spikes = server-1Hz bleed (Studio-only) |
| 2026-07-20 | `2026-07-20_collision-rework.md` | cake-sim, body-gym, config, collision, mobile | Straight-line eating: ONE walkable surface (server safety slabs → MIN height, never poke above the fine client columns), rate-limited column RISE (bite drops snap, refills rise gradually → stay buried + jump to climb out, new cake snaps), and FLAT-while-eating (suppress landing bounce + cap jumpMult via shared `LocalEatState`; toned per-layer bounce/jump otherwise). Studio-verified: stable standing, join lands on cake, drops snap, rise capped; feel-tuning knobs in config |
| 2026-07-20 | `2026-07-20_touch-eat-button.md` | cake-sim, app-root, ui-kit, input, mobile | Dedicated TOUCH **EAT** hold-button replaces "touch anywhere = eat" (the joystick ate). New `UIKit/EatButton` (Epic-pink round, thumb zone, touch-only) drives eating via AppRoot `onEatDown`/`onEatUp`; `usePressable` gains finger-aware `onPressStart`/`onPressEnd` HOLD callbacks (refcounts touches → multi-touch-correct, drag-off + hide-mid-hold release, no stuck-on); PC mouse-hold unchanged. Live-verified (hold eats / release stops / tap bites / backward-compat) |
| 2026-07-20 | `2026-07-20_taller-lazy-layers-wrapper.md` | cake-sim, config, map, upgrades, economy | Cake ~3× TALLER (maxHeight 90→270, totalHeight ×3) with ~10–14 chunky layers; renderer draws ONLY the current + next band as slabs (`assignWindowBands`/`windowFor`/`rewindow`) and a NEW textured `CakeWrapper` wall (random of 3 asset ids/cake) hides the ungenerated bulk below — layer count no longer capped by the vertex budget. Same-pace rebalance (proportional): biteDepth ×3, capacity ×3, calories ÷3 → same clear-time/income. New `filling` layer + image textures on frosting/chocolate/filling (`Content.fromUri`). Room walls 110→300. ADR-0008; Studio verify pending |
| 2026-07-20 | `2026-07-20_clean-eat-textured-wall.md` | cake-sim, config, rendering, gameplay | Follow-up: wall now covers ALL but the top layer (`windowFor` → 1 slab) and is a plain textured **Part** (Block + tiling `Texture` instances) instead of an EditableMesh — cheaper AND the texture actually displays (URI `TextureContent` on the mesh showed none); square-corner trade. CLEAN-CUT eating: `CakeOps.ApplyBite` clears each cell TOWARD the layer floor (one side clears, other stays full — clean edge) not a shallow crater; `ScanStats` sweeps active-floor slivers (`sim.sliverSweepStuds`); `biteRadius` 3→1.7 to hold pace. ADR-0008 revised; Studio verify pending. (Texture ids were dead DECALS → resolved to Image ids via `InsertService:LoadAsset`.) |
| 2026-07-20 | `2026-07-20_two-layer-wax-cleaner-cut.md` | cake-sim, config, rendering, map, gameplay | Round-3: render NEXT layer too (`windowFor` → 2 slabs again, wall below the next band); WAX coats EVERY layer (`CakeWaxShell` hide ref → active-band floor, so cleared spots show the next layer's wax skin); cleaner cut (SettleStep GUARD won't ooze into the cleared zone → no puddles, sweep reverted to aggressive); layer textures TILED (`render.layerTextureTiles`, single-mode → flat color) to fix blur; REMOVED all code-driven `Lighting.*` from MapService. ADR-0008 iterated; Studio verify pending |
| 2026-07-22 | `2026-07-22_wax-holes-remnant-sweep-treadmill.md` | cake-sim, body-gym, checkpoint, config, rendering | Per-layer wax (brighter), wax vanishes in eaten holes, thin-remnant sweep, treadmill fat-burn run (mount+anchor+run anim) |
| 2026-07-22 | `2026-07-22_eat-beneath-cleaner-crater.md` | cake-sim, config, rendering | Eat beneath the player too; eaten-zone cleanup sweep + wax recedes from craters (fan-triangle area test) for a clean eaten middle |
| 2026-07-22 | `2026-07-22_lobby-game-place-split.md` | persistence, bootstrap, teleport, shop, rebirth, map, architecture, split | Split single place → LOBBY + GAME (one universe): partition-aware bootstrap, PushInitialState hooks, registry-lookup sub coupling, src → common/lobby/game + 2 project files, Option A ProfileStore teleport handoff (+intentionalRelease flag, milestone saves, autosave doc fix), PassOwnershipSubs, LobbyMapService. ADR-0009. Server split done; place-aware client + PlaceIds + published-place verify pending |
| 2026-07-22 | `2026-07-22_lobby-matchmaking-rounds.md` | lobby, matchmaking, teleport, persistence, cake-cycle, ui-kit, shop, split | Three independent GroupToucher queues: kit mode/1–4 selector, 30s world countdown/count, reserved fixed-roster game rounds, boss win/loss return, exact ProfileStore release proof + preserved-destination retries, chocolate Shop trigger, lobby gameplay HUD/input gating. ADR-0010 |
| 2026-07-26 | `2026-07-26_squishy-retheme-shop-grid.md` | ui-kit, shop, pets, app-root, theme, icons | Squishy re-theme: 147-icon registry (`Theme.Icons` + `Theme.Icon()` R8 resolver) from 4 sprite packs; SHOP rebuilt portrait-1-column → landscape sectioned scroll with per-category grids (hero banner, 3-col pass/product tiles, 4-col gem packs w/ BEST VALUE, perk copy, owned/SOON states) on a DETERMINISTIC canvas — no UIListLayout/AutomaticCanvasSize/UIGridLayout, all three grid pitfalls structurally impossible; pets → squishies (display-only rename, ids are DataStore keys; roster 12→30 by ADDING ids so no migration); `Rarity.Common` de-aliased from `Theme.Button` → foam cream, `.Secret` −42° off Epic; 8 identical HUD menu placeholders → 8 distinct icons; opt-in squash on the Interaction primitive (ADR-0006-safe, rides `Content.Size`) |
| 2026-07-26 | `2026-07-26_cake-pacing-rebalance.md` | cake-sim, cake-cycle, upgrades, body-gym, economy, config, game-round, rebirth | 40-min Easy solo: pacing moved onto per-band `scoop`+`density` (ADR-0011), remnant-sweep rim rule fixed (was forfeiting 25% of every cake), difficulty/co-op scale WORK not height, boss made a real finale; REBIRTH REMOVED; upgrade tree moved to common + HUD entry so it is reachable in-match |
| 2026-07-26 | `2026-07-26_audio-sfx-music.md` | audio, juice, ui-kit, settings, app-root, cake-sim, pets, shop, lobby, config | Game got its AUDIO: authored `ReplicatedStorage.SFX` (+9 Pro Sound Effects bite/chew/goo/slump samples added) and `SoundService.BackgroundMusic` become the sample source, `AudioConfig` the 49-key map (asset name + volume/pitch multipliers + `cut` + `throttle`); `SoundPool` rewritten around the authored bank (20 voices, dedicated clones for effect-carrying templates, generation-stamped cut), new `MusicService` (shuffled playlist, fades, first-note gate) + `AudioSubsClient`; ONE kit hook (`Interaction.SetSoundHandler`) sounds all 11 pressable components; panel whoosh from AppRoot's `openPanel`; cues wired across all 16 client subs; the `music-enabled`/`sfx-enabled` toggles finally DO something (they only logged). Live-verified in the lobby place |
| 2026-07-30 | `2026-07-30_run-scoped-progression-pacing-ui.md` | upgrades, economy, cake-sim, cake-cycle, app-root, ui-kit, pets, persistence, config, balance, tooling | Upgrades + calories became RUN-scoped (ADR-0013, reset on every profile load via a new `OnProfileLoaded` lifecycle hook) and the tree re-priced ~20x down so all 44 tiers are owned by 46% of the cake at a 38.9-min clear; head-on eating FIXED (the aim point sat on the crater floor, so the layer gate skipped the bite entirely — now it searches forward for standing cake); boss fight advertises the squishy at stake (`PetService.Roll` split into pure `Preview` + `Grant`, per-recipient `pendingPet`); root ScreenGui made FULL-BLEED so modal scrims cover the topbar strip (+ `Hud` inset layer, inset-agnostic ScrollPane); Upgrades button REMOVED from the HUD entirely (the checkpoint `UpgradeStation` prompt was always the opener — a stale doc line said otherwise and I believed it over the code); game HUD pills onto the icon registry. Found + fixed a 2.25x units bug in `pacing_scenario.lua` that had made every published food/gym number wrong |
| 2026-07-26 | `2026-07-26_buried-item-finds.md` | treasures, cake-sim, juice, audio, assets, config, analytics, map, tooling | Finds became BURIED authored models you DIG OUT (median 2 layers) and that auto-collect with a pop/spin/magnet flight — `Workspace.Items` → prepared-clone library, height/tilt sizing, monotonic cover test, glint marker, first-discovery moments, rarity pinning. Plus: cake 330→170 studs + seamless wall photo (silhouette), `sweepBandFraction` (waste 8.3→6.8%), `layer-cleared` beat, FINDS goal bar + leaderstat set, `AnalyticsSubs` retention funnel, and `tools/headless-sim/` (runs REAL modules without Studio). ADR-0012. 18 passes, live-verified in Studio throughout; closed with a fan-out adversarial review (8 findings → 4 fixed, 4 refuted with reasons), then a live-place pass that caught Rojo silently serving stale code, made find rarity legible without new art, added a `DebugUncoverFind` dev hook that made find visuals verifiable at all, and — from the competitor study — made DEPTH PAY (rare-or-better 14.3% surface -> 33.7% floor) |
| 2026-07-30 | `2026-07-30_squishy-followers-shop-cards-purchases.md` | pets, shop, ui-kit, monetization, persistence, tooling, split | Equipped squishies FLY BEHIND you in both places on the authored 3D models (the step lived in a sub that early-returns in the lobby); shop cells stop being buttons — new `ShopCard`/`ShopHeroCard` with a white inner border, a per-item accent face and the Starter Pack's bundle spelled out as chips; and the money path made safe for real Robux (ProcessReceipt moved COMMON, receipt ledger, no-grant-mid-teleport, per-kind descriptor validation — ADR-0014) with an Open Cloud tool ready to create the 15 ids |
| 2026-07-31 | `2026-07-31_shop-cards-tabs-redesign.md` | shop, ui-kit, theme, app-root, monetization | Shop cards stop being buttons and the shop stops being one 5.6-screen scroll. ROOT CAUSE named: `style-rules.md` §2 is a BUTTON recipe ("bottom always ~2x thicker") and the kit had no CARD recipe, so every cell wore an 8-top/30-bottom outline — 3.75x, the kit's strongest "pressable slab" signal — under a flat colour field at 0.95 aspect. New rule in Theme: a button is a slab (one field, bottom-weighted outline), a card is a FRAME (even outline + internal ZONES). Card rebuilt as ART WINDOW / title / perk / PRICE SHELF at 282x338, outline 1.50x measured, icon 13.5% -> 22.0% of the cell; per-item colour CONTAINED to the art window (one neutral navy body) so a row has hierarchy instead of six equal shouts; banner + hero + hero chips re-cut in the same language. Panel: four TABS (Offers/Passes/Boosts/Gems) — worst tab 1.9 screens, two need no scroll — balance chips moved into the empty header band to pay for the tab row, section header suppressed inside single-section tabs, content centred when it fits, canvas top pad so ribbons stop clipping. Also: "⬡ R$ 199" duplicate glyph, illegible perk copy (TextScaled binds on WIDTH — limits now documented), gem packs given a perk line. Verified with a new edit-mode React preview harness + a live playtest through all four tabs |
| 2026-07-31 | `2026-07-31_gem-boosts-and-feature-cuts.md` | shop, economy, boosts, treasures, daily-rewards, quests, time-rewards, pets, audio, monetization, ui-kit | Gems got a SINK and boosts got a price: finds now pay gems ONLY, the four 15-min boosts (calories/bite/speed/capacity) become a 500-gem row, and QUESTS + TIME REWARDS are deleted outright (orphaned profile keys are harmless — `applySchema` iterates the schema and preserves unknown top-level keys, so no migration). New ADR-0015: a SECOND checkout path whose failure semantics are the OPPOSITE of `ProcessReceipt` — Roblox re-delivers a failed receipt forever, nothing re-delivers spent gems — so the gem path validates the whole grants list BEFORE spending, spends atomically, refuses (not defers) mid-teleport-release, and refunds on a late decline; `currency` is the discriminator because every dev-product id is still 0. New COMMON `BoostSubs`: a stat READ per use needs nothing, one PREDICTED client-side is mirrored through the `BiteRadiusMult` attribute, ones PUSHED once (walkSpeed/capacity) must be rewritten at an expiry that fires no event. Two things review caught: the monetization tool BRICKED on the deleted keys (its price reader exits before every branch, so four stale entries disabled the only way to create the ids the shop is blocked on), and co-op finds paid a QUARTER of what the balance rule assumed (find count is roster-independent and a find is taken by whoever gets there first → per-head `coopFinds`, 496/402/371/355 gems per player at 1-4p, difficulty premium deliberately excluded). Also: `GrantBoost` EXTENDS instead of resetting (the reset ate a 500-gem re-buy and nothing in the UI shows a boost is running — still true), follower yaw 180 → -90 (the authored face is on -X), daily week retuned to 250 gems + 2 boost days, HUD menu down to 5 |
| 2026-07-31 | `2026-07-31_monetization-ids-live.md` | monetization, shop, tooling, publish | The 11 Robux ids are LIVE on universe 10593425705 and wired into `ShopData` — the shop stops rendering a wall of "SOON" buttons. `tools/monetization/` had never actually been run (`id_map.json` was `{}`, `tools/` untracked, its "verified 53/53" header inherited from another project); audited it against the `reuploader` reference that the verification really came from and fixed 19 defects before touching the API. The load-bearing one: the reference created with `isRegionalPricingEnabled=true`, found every price wrong, and could only repair the PASSES — **a developer product has no update endpoint at all** (7 candidates probed there, none adopted), so its name/description/price are final at create. Creating with `false` was untested, so the tool now mints the CHEAPEST product first, reads its live price back and ABORTS the run on a mismatch; it came back exact, and that is now a verified fact. Also: pagination was `cursor`/`nextPageCursor` where the endpoints take `pageSize`/`pageToken`/`nextPageToken` (silent truncation → permanent duplicates); idempotency moved off name-matching onto the id LEDGER and an unrecognised 200 from a listing is now FATAL rather than "the universe is empty"; `--icons` used to PATCH price+`isForSale=true` onto adopted items on a DRY RUN; the cookie was domain-less and went to the CDN; `entry_span` ran to EOF for the last entry of each table. New `--preflight`/`--fix-prices`/`--retire`/`--only`, real price auditing in `--verify`, exit codes. 11/11 verified live, 6 pass icons attached, the 4 gem boosts confirmed id-less |
